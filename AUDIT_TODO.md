# Kythera — Audit To-Do List (Step 1: Code-only)

**As of:** 2026-07-03 · **Method:** 12 parallel read-only reviews across the entire repo (Core/watchdog, data pipeline, Telegram/monitors, orchestrator/regime, classic strategies, AI bots ×3 groups, market intelligence, dashboard/tools, ML trainers/backtests, cross-cutting sweep).

**What this is:** findings **purely on a code basis**. Where a point can only be finally judged with live data, it carries `[DB]` — those points go to **Step 2** (VPS + database access) for confirmation/measurement. The Step 2 query catalogue is at the very bottom.

**Assigning new finding IDs (2026-07-10):** check the next free number **against `origin/main`, not against your own branch** — `git fetch origin && git show origin/main:AUDIT_TODO.md | grep -oE '\*\*P1\.[0-9]+'`. On 2026-07-10 three parallel sessions independently assigned `P1.46` (T-029 ATB1, T-036 Sniper, T-043 RUB), because each looked at a `main` from before its own branch point. **Tie-breaker on a collision: the number stays with the entry whose ID appears in the MOST immutable or already-read artefacts** (commit message, `CHANGELOG.md`, `docs/`) — the goal is to leave as few references orphaned as possible. Merge order is irrelevant. On a tie the lower task number decides, so the rule stays deterministic. Concretely on 2026-07-10: Sniper (T-036) had 3 anchors (`CHANGELOG.md`, `docs/CANDLE_CALL_SITES.md`, commit title `97fc12d`), ATB1 (T-029) had 1 anchor (commit `1113095`), RUB (T-043) had 1 anchor (merge commit `4b27061`). The Sniper kept `P1.46`, even though ATB1 merged eight minutes earlier. ATB1 → `P1.47`, RUB → `P1.48`. **Price of the resolution, named honestly:** the commit messages `1113095` and `4b27061` still say `P1.46` but now mean `P1.47` and `P1.48` respectively — commit messages cannot be rewritten. It stays traceable via the T-ID present in every annotation. There is no guard against this; with parallel sessions the check is manual work.

**Legend:** `[ ]` open · severity **P0** (money-critical, immediate) → **P3** (cosmetic). `[DB]` = to be verified in Step 2. File:line is clickable.

**Challenger promotion hygiene T-2026-KYT-9050-057 (naming guard + EPD3 `model_id`).** Two
follow-ups from the z-code-reviewer verdict on T-185 (PR #170).
- [x] **#T57-1 Naming guard built** (`tools/promotion_guard.py`, this task's PR). A challenger tag
  whose `SHADOW_ARTIFACTS` filename is also the root loader slot of a foreign generation
  posts under TWO tags after promotion (hard rule 4 — nearly happened with EPD3-SHORT on
  2026-07-21, averted at the time by a manual rename). LIVE leg on a foreign slot = FAIL, parked leg =
  WARN. Wired in as a pre-commit hook, as check 8 in `tools/verify_staging_artifacts.py`, and as a CLI.
  `core/shadow_gate.py` deliberately unchanged (hard rule 7).
- [ ] **#T57-2 Rename RUB3-LONG — at promotion time, not before.** `SHADOW_ARTIFACTS["RUB3"]["LONG"]`
  still points at `rub2_model_LONG.pkl` (guard: WARN). Harmless as long as RUB3 stays parked on
  SHADOW per T-037; before a RUB3 go-live the artefact must be renamed to `rub3_model_LONG.pkl`
  (artefact + registry in one move). The guard blocks the flip without the rename.
- [x] **#T57-3 EPD3-SHORT artefact `model_id='EPD2'` (hard rule 6) — CLOSED 2026-08-03: the root
  promote happened with T-2026-KYT-9050-085.** The EPD3 SHORT go-live copied the correctly tagged
  staging dump to the repo root, so `epd3_model_SHORT.pkl` there now carries `meta.model_id='EPD3'`.
  Equivalence re-verified before the copy: identical booster sha256, feature list and threshold —
  the tag was the only delta. Pinned by `test_t085_epd3_short_root_artifact_carries_epd3_provenance`
  in `backtest/test_shadow_gate.py`. Nothing open here anymore; `#T57-5` (LONG) is untouched by this.
  Prior history: `staging_models/epd3_model_SHORT.pkl`
  now carries `meta.model_id='EPD3'`, produced with `tools/retag_artifact.py` under the fleet Python
  3.13. Pinned in `backtest/test_epd3_artifact_model_id.py`: the artefact loads via
  `core.model_artifacts` under tag `EPD3`, and every other field is identical to the promoted root
  artefact (feature list, threshold, `predict_proba` on a fixed probe matrix, calibrator curve,
  remaining meta). **Correction of an earlier statement in this point:** the claim that a re-dump
  would be a lossy cross-version round trip was wrong for this artefact and had swapped the
  measurement. `py -3.13` has sklearn **1.7.1**, `py -3.14` has **1.9.0** — and
  `epd3_model_SHORT.pkl` was pickled embedding **1.7.1**, so it loads warning-free under 3.13.
  The re-dump there is a same-version round trip. What is actually pickled with 1.9.0 is the
  **LONG** artefact (see #T57-5).
- [ ] **#T57-5 EPD3-LONG carries the same tag defect but cannot be re-dumped (new, 2026-08-01).**
  `epd3_model_LONG.pkl` (root + staging) also carries `meta.model_id='EPD2'`, but was pickled in the
  3.14 env with sklearn **1.9.0**; under the fleet Python 3.13 (1.7.1) it loads with an
  `InconsistentVersionWarning` on the `IsotonicRegression`. `tools/retag_artifact.py` therefore
  refuses the artefact — a re-dump there would be a real format change to the calibrator. No live
  consequence: bot 10 loads EPD3 via `shadow_gate.load_shadow_artifact`, which normalises to
  `{model, features, threshold}` — the calibrator is never read (and re-measured predictions under
  1.7.1 are correct, just with the warning). Fix = re-dump under the 3.14 interpreter **or** a
  retrain; both outside this task.
- [ ] **#T57-6 `verify_staging_artifacts.py` does not see the EPD3 files (new, 2026-08-01).**
  `build_registry()` only globs `epd2_model_*.pkl` for the EPD family; `epd3_model_*.pkl` is silently
  skipped by the CLI run — so the tag check (HR-6), the very reason #T57-3 got fixed, never runs
  over these files at all. `test_epd3_artifact_model_id.py` therefore drives the contract directly
  via `verify_artifact()` with an EPD3 spec. The registry extension is deliberately NOT in
  this PR: it would pull in `epd3_model_LONG.pkl`, which per #T57-5 is not fixable here,
  and would produce a FAIL this PR cannot close.
- [ ] **#T57-4 `test_bot_variant_archive.py::test_manifest_carries_feature_contract` red —
  PRE-EXISTING**, verified 2026-08-01 against the unchanged `index.py`. Cause: ATS2 was promoted to
  root without its `*_meta.json` sidecars → `_read_meta` finds no sidecar there, the test
  runs into `StopIteration`. Fix = promote the sidecars along with it (operator) or pull the test to
  artefact reality. Not in this PR (never turn a guard green in the same PR that broke it, without having triggered it).

**Fleet reconfig T-2026-KYT-9050-037 (RUB1 revive + gate flips + retires, from `bot_results.xlsx`).** PR core merged: bot 13 back on the original RUB1 legacy models (both directions live under tag `RUB1`, RUB2 retrain benched → SHADOW), RUB3-SHORT → SHADOW, AIM2-TOPN + ATS1_Robust → RETIRED in the registry. **Open follow-ups (not in this PR):**
- [x] **#3 ATB2-LONG — LIVE deployed by operator override** (code PR, pending Michi root promote + restart). Was BLOCKED (`optimal_threshold=None`, not in root). Read-only diagnosis: only **n=17** shadow trades → statistically undecidable, no data-backed operating point. Michi decision „gemäß Anforderung deployen“: threshold **blindly 0.60** (ATB1s 0.80 is an incompatible model), `("ATB2","LONG"): LIVE`, **bot 14 rewire** `_emit_atb2_shadow`→`_emit_atb2` (gated router + has_open guard, was shadow-only → would have silently swallowed LIVE), artefact `staging_models/atb2_model_LONG.pkl` @0.60. **Open (Michi):** root promote `atb2_model_LONG.pkl` + restart. Caution: no data basis — operator-wanted live experiment.
- [x] **#4 EPD3-LONG — LIVE deployed by operator override** (code PR, pending Michi root promote + restart). Was BLOCKED (collision hazard + `threshold=None`). Read-only edge analysis (2578 real shadow trades, T-036 playbook): mean net **≈0%**, confidence **anti-discriminating** (corr −0.04) → **no deployable threshold edge**. Michi decision „gemäß Anforderung deployen“: threshold **0.76** (volume cap ~258→~130/d, NO edge filter), `("EPD3","LONG"): LIVE`, `SHADOW_ARTIFACTS["EPD3"]["LONG"]`→**`epd3_model_LONG.pkl`** (challenger-distinct, kills the legacy EPD2 collision analogous to SHORT/PR #185), artefact `staging_models/epd3_model_LONG.pkl` @0.76. Bot 10 already routed gated → registry+artefact only. **Open (Michi):** root promote `epd3_model_LONG.pkl` + restart. Caution: live ~0 edge — operator-wanted volume-capped experiment.
- [ ] **#5 Activate ATS2 — restart-gated.** Config/registry already match (`ATS2` live/live, root artefacts `ats2_model_*.pkl` since 2026-07-23 22:02); the 21:04 restart ran before that — bot 12 did not load ATS2. **One fleet restart** activates it (Michi, `tools/restart_fleet.ps1`).
- [ ] **#6/#7 DB delete AIM2-TOPN + ATS1_Robust — operator-gated (hard rule 1, irreversible).** Registry RETIRE is merged; "deleting trades" is a separate step against the live DB (`ai_signals`/`ml_predictions_master`/`closed_ai_signals`), **individually approved by Michi**: exact SELECT preview (count per table) → approval → DELETE in a transaction, backup note (`tools/backup_db.ps1`). **Effectiveness note:** the AIM2-TOPN emitter (bot 15) does not consult the shadow_gate registry (its own `AIM2_TOPN` config gate) → actually silencing it needs the config gate flip + restart (operator). ATS1_Robust has no live emitter (synthetic).
- [x] **#8 Main channel — clarified (documentary).** No live emitter left: the classic "Main Channel" detector was retired via dispatch removal (T-020) and replaced by **MAX2** (bot 9, SRA2-LONG fork → CH_MAIN). `strategies/strat_main_channel.py` is dead, non-dispatched code. No registry entry needed.

**Trailing close arm T-2026-KYT-9050-042 Phase C (bot 40, PR #198).** Built, pinned (21 DB-free
pins), **not deployed**. The bot mirrors the 33 legs selected in PR #198 into `CH_TRAILING`
and closes them there via trailing (act 2%, x 10%). Open, deliberately operator-gated points:
- [x] **#T42-1 Channel ID + live gate — ID set 2026-07-26**, `CH_TRAILING` in the live `.env`;
  gate flip to `TRAILING_BOT_LIVE_POSTING=1` per Michi operator order after the legacy-book fix.
- [x] **#T42-6 Legacy book + log flood (from the first shadow run).** On start the bot mirrored
  all 465 open source trades (some days old) and logged ~870 rejections per 10s cycle
  (34 691 lines in 33 min). Both fixed: age limit + `PREEXISTING` marker, a summary line
  instead of individual lines. Only became visible because the gate was off by default.
- [ ] **#T42-1b Channel ID + live gate (Michi).** `CH_TRAILING` (secret, `.env` only) and
  `TRAILING_BOT_LIVE_POSTING=1`. **Both** are off by default — until then the bot runs fully,
  tracks and logs, but posts no line. A deploy alone posts nothing.
- [ ] **#T42-2 Fleet restart (Michi).** `core/fleet.py` is read at watchdog import; the new
  entry (start_delay 271) is only supervised after a restart. On first start the bot creates its
  own table `trailing_positions` (no existing live table is touched).
- [ ] **#T42-3 Hook up Cornix to the channel (Michi).** Including sizing — the bot posts
  single-entry (arm B, PR #197).
- [ ] **#T42-4 `test_fleet_definition.py::test_watchdog_view_is_unchanged` is stale — PRE-EXISTING.**
  The golden list was never updated when bots 36–39 were registered; the test is identically red
  without bot 40 (verified 2026-07-26 by stashing the `fleet.py` change). Deliberately NOT
  refreshed in this PR: turning a guard green in the same PR that triggers it is exactly
  the pattern from hard rule 9. Own fix moment — update the golden to 36–40.
- [ ] **#T42-5 Make the study's candle mask flush (open, re-run needed).** `tools/trailing_slot_budget.py:143`
  selects on `open_time`, so a trade's last candle reaches beyond the captured close;
  the exit TIME is capped, the exit VALUE is not. One-sided and ≤1 candle/trade, doesn't flip
  the recommendation (core review PR #198). Flush would be `open_time + tf <= close_time` — costs
  new numbers, hence an operator decision. Documented in the report's "honest limits" section.
- [x] **#T52-1 Study's open-book gap closed (T-2026-KYT-9050-052, 2026-07-27).** The
  slot-budget metric (realised sums) overlooked that an activation-gated trail by
  construction only closes winners — bot 40's book desegregated live within one day
  (128 L / 5 S, avg −1.84%). New: `tools/trailing_book_health.py` (equity = realised + open
  MTM, book composition over time) + 9 DB-free pins. Verdict incl. rule comparison:
  `staging_models/replay/trailing_arm_verdict_t052.md`.
- [x] **#T52-2 ROM1 double-counting in the PR #198 expectation quantified.** ROM1 is a re-forwarder
  (bot 28); its legs accounted for 10 334% of the 49 204% expectation (21%). Without ROM1, expected
  trail act=2 is **39 116%** (March–July). The roster selection itself remains unchanged — AK3
  (one symbol per position) already suppresses most of the duplicates in the live bot anyway.
- [x] **#T52-4 Rebuild bot 40 onto trail + time stop 24h + exposure cap ±50 — BUILT (operator go
  Michi 2026-07-28), restart-gated.** `TIME_STOP` (causal, edge case + rate limit 25/cycle
  pinned) + `EXPOSURE_CAP` (net bound in `admit`, pinned). Effective only after
  fleet restart; first cycle clears never-armed legacy book (~150 mirrors, staggered).
  Open (Michi): restart · Cornix sizing m ≈ equity/400 · from ~2000 USD equity onward a new run
  under cap 1000 for the 2-channel extension. KB task creation for the rebuild is still
  pending (KB MCP not connected in the session) — follow up in the next connected session.
  **Addendum:** grandfather cutoff `TIME_STOP_SINCE=2026-07-28T14:00Z` (operator decision:
  legacy book rides at its own risk; no cleanup wave on restart).
  **Addendum 2 (29.07.):** freshness window 30→180s (insert latency measured: median 95s,
  85% of signals were dropping out) + ROM1 removed from the roster (`EXCLUDED_AS_DUPLICATE`) — restart-gated.
  Monitoring runs as scheduled task "Bot40 Tripwire Monitor" (hourly :53, parks on
  tripwire, log `.local/bot40_monitor.log`) — the LLM session spawn from agent context
  failed (pitfalls documented in memory).
- [x] **#T54-1 Live evaluation of the arm as a repo tool (T-2026-KYT-9050-054, 2026-08-01).**
  `tools/trailing_arm_report.py` (read-only) measures what bot 40 actually did — as opposed
  to `trailing_book_health.py`, which simulates rules over historical fleet trades.
  Two join contracts are locked in there, because both had each ended a full
  run with a wrong verdict: `closed_ai_signals.id` is **not**
  `ai_signals.id` (its own sequence — 9 of 4611 rows symbol-matched), and
  `closed_ai_signals.open_time/close_time` are **naive PG-local (+03)** against `timestamptz`
  in `trailing_positions`. Counterfactual only on `TRAIL`/`TIME_STOP`, split by
  resolved/still-open. 19 DB-free pins, 5 mutations covered.
- [x] **#T54-2 Market attribution: the LONG number is tape, not a leg defect.** Over the
  live series the equal-weighted altcoin index (median hourly return) fell **−8.3%**.
  LONG realised −596 against −858 market-implied = **+262 residual**; SHORT +20 against
  +122 = **−101 residual**. The naive look at the book (LONG −644 / SHORT −5) suggests the
  exactly the wrong conclusion. beta = 1 → market share is a lower, residual an upper bound.
  **LONG stays on** (operator finding Michi 2026-08-01, now quantified).
- [x] **#T60-1 Bot 40 intake bottleneck identified (T-2026-KYT-9050-060, 2026-08-01).** Not
  the freshness window, but the **exposure cap**. `SLOT_CAP` never triggered in three days,
  `EXPOSURE_CAP` on the other hand at avg 3.2 → 6.0 → 6.6 candidates/cycle; the book sticks at +42…+52
  against the ±50 ceiling, LONG headroom consistently 0–8. Because the cap bounds the *difference*,
  `Kapazität = 2 × min(L,S) + Cap` = currently 92 — **the SHORT side throttles the
  overall volume.** Tool `tools/trailing_intake_audit.py`, verdict
  `staging_models/replay/trailing_intake_verdict_t060.md`.
- [x] **#T60-2 TSM1 SHORT into the roster — WITHDRAWN (T-2026-KYT-9050-062, 2026-08-01).**
  The recommendation was based on volume (107 signals/day, live, only out because the
  slot cap never bound) and **never measured quality**. Measured under the
  trail rule: **residual −0.72 at t = −4.58**, negative under the second yardstick too.
  The roster slot would be negatively occupied. Verdict:
  `staging_models/replay/short_leg_trail_verdict_t062.md`.
- [x] **#T62-1 SHORT legs evaluated under the trail rule (2026-08-01).** Both sides
  under the same exit (leg path vs. index path), so the leg's own TP policy
  drops out — the predecessor yardstick (against the full window move) penalised every
  TP leg by construction. **Corrects the #T60 point "density is an artefact":** the
  MIS2 legs reach +5.5 to +8.1 residual (t 3.5–9.5) and earn their roster rank.
  Tool `tools/short_leg_trail_value.py`, 12 DB-free pins.
- [ ] **#T62-2 Distributions of the MIS2 residuals (open, before any magnitude claim).**
  The means of 5.5–9.1%/trade rest on n = 47–132 and are probably
  fat-tailed (a few coins that crash 30% after a pump). Sign and
  ranking hold; the **magnitude** needs median and quantiles.
- [ ] **#T62-3 Gate-flip candidates if more SHORT volume is wanted (Michi).**
  MIS1-24h (+4.94, t 3.81), MIS1-72h (+2.12, t 2.27), MIS2-8h (+5.53, t 3.48) — all in
  shadow, together ~13 signals/day. Small volume, measured edge. **No high-volume
  SHORT leg with a positive edge exists** (TSM1 −0.72 at 107/day, EPD3 −0.38 at
  337/day); the only exceptions are AIM2 (already in the roster) and ROM1 (re-forwarder,
  double counting). The cap problem from #T60-1 **cannot** be solved via SHORT supply.
- [x] **#T64-1 ROM1 SHORT into the roster — NOT IMPLEMENTED, would be ineffective
  (T-2026-KYT-9050-064, 2026-08-01, Michi operator order „nehmen wir den ROM1 short").**
  Two measurements, two independent reasons:
  1. **ROM1 re-forwards with exactly 3h offset.** Age at insert: p10 = 10 798s,
     median = 10 799s, p90 = 10 800s — a hard band, bot 28's rule, not
     scatter (AIM2 SHORT for comparison: median 0s). **Of 87 signals exactly
     one lies below the 240s threshold** → 99% would be rejected as `PREEXISTING`. The
     roster row would be inert; the window did not "randomly" filter ROM1, but
     structurally.
  2. **The measured residual +0.86 does not apply to the arm.** It was computed over the window
     `open_time → close_time`, i.e. the trade the way the ORIGINAL leg
     experienced it. A mirror entering the market 3h later (market entry,
     operator decision 27.07.) trades a different move — the bulk has
     already happened. The edge is unreachable for it.
  **Rebutted, however:** the original double-counting objection barely holds up in live
  operation — only **17%** of ROM1-SHORT signals would hit an already-mirrored symbol
  (`SYMBOL_HELD`), 83% would be genuine new entries. The exclusion remains correct
  nonetheless, just for the other reason.
  **What it would take:** either open the window for ROM1 beyond 3h — exactly the
  wiring it exists against (T-042: of 24 mirrors 5 filled, in 15 of 18
  cancellations the market never touched the entry) — or have bot 28 stamp its own
  `open_time`, which changes ROM1s semantics and evaluation.
- [ ] **#T64-2 Re-measure the ROM1 edge from `inserted_at` (open, before any window debate).**
  The number the arm would actually get: residual over the window from forwarding
  instead of from the original entry. If the advantage doesn't survive the 3h offset, the
  ROM1 question is finally settled and a risky window change is saved.
- [ ] **#T60-3 Window 240 → 300s — AFTER #T60-2, as a quality not a volume measure.**
  As long as the cap binds, a wider window only shifts rejections from `PREEXISTING`
  to `EXPOSURE_CAP`. The value lies elsewhere: `admit()` sorts by leg density, 300s gives
  the same LONG budget ~5× as many candidates. Adverse selection ruled out (rejected
  LONGs avg +2.39% vs +1.28% admitted, t ≈ 1.3).
- [ ] **#T60-4 Raise `EXPOSURE_CAP` — NOT without its own study.** T-052 measured that
  the one-sided LONG book was the account damage and the structural bound beat every
  market-condition model. Logged here as a deliberately rejected option, so it doesn't
  come back later as "overlooked".
- [x] **#T54-3 Grandfather cohort — DECIDED 2026-08-01: it keeps riding (Michi).
  RE-EVALUATE (T-060):** 28 of the 30 mirrors are LONG and permanently occupy **28 of 50
  units** of LONG headroom (56%) plus 28 symbols against `SYMBOL_HELD`. The decision was made
  without this number — it isn't wrong because of that, but the cost is higher than known at the time. The
  cutoff `TIME_STOP_SINCE=2026-07-28T14:00Z` keeps **30** mirrors free from the time stop:
  median age 108h, max 126h, **zero of them armed**, Σ −81% open. They can structurally
  never be trailed (peak < activation) and each block the only mirror slot of their
  symbol; exit only via SL or fleet close now. Operator decision: **keep running**
  — cutoff stays, no cleanup wave. That is the status quo in the code, so deliberately
  **no** change is made; the costs above are the accepted price.
- [x] **#T54-4 SL mark backfill completed (2026-08-01, Michi operator approval).** 67 rows from
  before the T-053 fix, Σ **−387.3%** (avg −5.78%, worst −12.73%), none
  rejected. Verified: all 90 `SL_HIT` rows now carry a mark (Σ **−559.3%** =
  −172.0 pre-existing + −387.3 backfilled), none booked as a win, the rejection rows
  (`PREEXISTING`/`SHADOW_CARRYOVER`/`ENTRY_NOT_FILLED`) untouched NULL. A sum over
  `close_mark_pct` no longer reads factor-3 too optimistic.
- [x] **#T47-1 Live turnover measured against the study — REFUTED (T-2026-KYT-9050-047,
  2026-08-01).** Live turnover is **not** systematically above the simulated one.
  Holding time live median 6.00h (closed only) or **[6.71; 7.40]h** with the 96 open
  ones right-censored, against **6.59h** from the same study, mix-matched to the
  live leg counts (the header's 4.6h is a median **over legs**, not over
  trades). For the five largest legs — 65% of the book — the arm holds **longer** than the
  simulation. Turnover per occupied slot-day: live 1.405 against 1.291 (aggregate) or 1.146
  (mix-matched) → **1.09–1.23×**, fee correspondingly 0.141% against 0.129% per slot-day.
  New: `tools/trailing_live_vs_study.py` + 17 DB-free pins,
  `docs/T-2026-KYT-9050-047-live-vs-study-report.md`.
- [x] **#T47-2 The trigger was a bootstrap artefact, and the slot calculation was too
  PESSIMISTIC.** The "~80 trail fires/h at ~460 open positions" were 80 fires in
  **1.2h on 26.07. 19–20 UTC** — the first shadow cycle, whose mirrors inherited a peak above the
  activation threshold and fired immediately. Live: **4.0/h**, busiest hour 21.
  Occupancy live avg 126 / p95 221 / max 291, settled (48h) **avg 106**, against
  roster-matched **251.6** (the study's avg 284.6 includes the now-excluded
  ROM1 legs with 33 seats; mean occupancy is exactly additive). The cause of the gap is
  **intake** (195/day live against 365/day simulated = 53%) — four admission filters the
  simulation didn't have —, not turnover. The Cornix cap of 500 was never in reach.
- [x] **#T47-3 Resolution effect of the 15m-candle vs. 10s-poll quantified: ~20 minutes.** The
  imported study rule replayed on the **same** live mirrors (act 2%, x 10%,
  strictly prior peak): at the arm's own exits (n=586) the 15m exit lands
  **median +0.33h, p95 +0.63h** after the live exit; in **10%** the 15m grid even fires
  **earlier** (a wick the 10s poll never printed), in 17% in the same candle. Slot cost of the
  finer grid: **≤ 33.1 slot-days = +4.7%** (lower bound, 325 censored not counted).
  Price difference on the same candle: **+0.02 percentage points/trade**. Real, but not a shifted
  operating point. **Recommendation: keep `act = 2 %`** — and LOWERING `act` would not use the
  free capacity but enlarge it (shorter holding time → less occupancy).
- [x] **#T93-1 Operator suspicion "the trail closes too early" — CONFIRMED as an observation,
  REFUTED as a lever (T-2026-KYT-9050-093, 2026-08-04).** Live, over the deployed regime
  (mirrors from `TIME_STOP_SINCE`, 851 closed): the trail IS a 2%-scalper — median peak at exit
  **2.30%**, **84%** of the 494 `TRAIL` exits below a 3% peak, only 4.5% above 5%. And the tape
  kept going: **94.7%** of the exits saw a better mark within 24h, median **+3.48pp** left
  (mean +6.72, p90 +18.73; 38% left >5pp). **But no later activation converts that into money.**
  On the July–August window (9 149 trades, all with real TP1 geometry) act=5 earns 5 870 and
  act=10 4 550 against act=2's 6 539 — the same patience that holds winners also holds losers,
  and the book grows from 226 to 429 avg slots. The give-back `x` was already refuted the same
  way (T-052 addendum 2). **The general form, from the full March–August window:** patience does
  buy gross return (act=5 59 305, act=10 71 554 against act=2's 46 521) — but at 2–3× the bound
  capital (530/760 vs 261 avg slots) and a falling exchange rate (112/94 vs **178** net per avg
  slot). With one channel and 800 USD none of it is reachable. Verdict:
  `staging_models/replay/trailing_tp1_activation_verdict_t093.md`.
- [x] **#T93-2 TP1 as the activation threshold — MEASURED, NOT RECOMMENDED.** Per-trade
  activation at the source signal's own TP1 instead of the fleet-wide 2%. Not automatically a
  later bar: **23.8%** of roster trades carry a TP1 below 2% (median 3.18%, mean 4.47%; MIS2
  shorts ≈19–21%, ATS2/SRA2/MAX1 ≈1.7–1.9%) — measured live against the mirror entry too
  (median 2.95% vs 2.92% from the source entry, so the market-entry offset does not distort it).
  Result under the configuration the bot actually runs (ts24 + cap ±50): **net 5 835 vs 6 379
  (−8.5%), net per avg slot 59 vs 91 (−35%), MaxDD 279 vs 184 (+52%)** — bought with the
  healthiest full-population book of the whole series (**−0.11%** vs −1.26%, 55% vs 69%
  underwater, L/S 60/38 instead of 48/22). **Reproduced on a second window** (March–August,
  47 766 trades, imputed geometry): −11% / −34% / +48%, book −0.98% vs −1.65% — two tapes, two
  TP1 sources, same answer to within a percentage point. Against the 800-USD envelope, where
  net-per-slot decides position size, that is a pay cut. **Do not flip** — and **re-run it at the
  2-channel stage** (from ~2000 USD, where absolute net becomes binding again: uncapped TP1 makes
  56 323 against act=2's 46 521, +21%, at a healthier book). The `max(TP1, 2%)` floor variant is
  within 0.7% on every metric (irrelevant). New: `--tp1-only` / `--tp1-impute` / `act_tp1` in
  `tools/trailing_book_health.py`, 6 additional DB-free pins.
- [ ] **#T93-3 `closed_ai_signals.targets` only exists from ~June 2026 — blocks every
  geometry-dependent replay before July (open, structural).** Coverage measured 2026-08-04:
  **0%** Mar–May, 2% Jun, 77% Jul, 100% Aug = 19.4% since 2026-03-01. Any rule keyed on TP1/TP-n
  can only be measured causally on the July+ window; over the T-052 5-month window it would
  silently degrade into an act=2 clone. The tool now refuses to hide this (fallback count printed
  + in the JSON, `--tp1-only` restricts the whole sweep). Not fixable retroactively — the
  monitor deletes `targets` on close (Step 7 finding, `audit_reports/17_monitor_replay_and_gaps.md`).
- [ ] **#T93-4 The −5% SL cap verdict is regime-dependent — re-run when September data is in
  (open, Michi-gated).** T-052 addendum 6 rejected the cap on March–July (−33% net on the
  deployed rule, MaxDD *worse*). Re-measured on July–August it is nearly free: **net 5 925
  (−7%), MaxDD 126 (−32%), net per avg slot 93 (unchanged), book −0.61%**. Cause is in the
  recovery statistics — dippers below −5% ended at avg −2.74% on hold over March–July (42.1%
  better than −5%) but at avg **−4.76%** over July–August (35.0% better). One five-week window
  against one five-month window: **flag, not a recommendation.** Context for the operator's
  "the SLs go very deep": the mirrored SL sits a median **7.03%** below entry (p90 14.87%, max
  53.34% = −1 067% margin at 20×) — inherited S/R geometry from the source legs, not a bot
  defect (`mirrorable_at` keeps the absolute SL by design). AIM2 SHORT alone carries a third of
  the arm's SL damage (11 of 42 hits, −100.9 of −304.2pp) through volume, not through unusual
  geometry.
- [ ] **#T52-3 Operator decision on bot 40's exit rule (Michi) — UPDATED 2026-07-28.** After
  live confirmation of the desegregation (clean window in ~9h to 95% underwater; bot
  parked on operator order 05:29, unparked 05:43) and run 3 (23 rules, incl. x-sweep,
  SL trail, market-condition gates): **limit losses → trail a2 + time stop 24h + cap ±50**
  (MaxDD 588 = 7× better than live); **upside/book health → be2+ts24** (SL trail to
  breakeven + time stop; book +2.85%, 28.8k net). Market-condition gates (feedback/BTC direction)
  measured and dominated — do not build. x=20/30% loses on both axes (no "closing slower"
  via x). **Priority-upside addendum (runs 4+5, 500-cap-fair):** be5+ts24@500 = 34.5k
  final equity (90% of the trail) at MaxDD 4 124 and book +3.36% — recommendation for priority upside;
  be2 threshold too early, be+direction cap choked off. **CORRECTION (run 8): the entire
  be family is discarded — look-ahead in the time stop (arming checked over the whole
  lifetime instead of up to the deadline); causally be5+ts24 falls from 59.0k to 7.0k.**
  Revised recommendation (start with 800 USD, 1 channel, sizing by occupancy): **trail act=2
  + time stop 24h + cap ±50** (278 pct-pts/avg-slot = 1.9× trail, MaxDD 588). 2-channel
  extension from ~2000 USD equity, before that a new run under cap 1000 (candidate trail act=5).
  Rebuild task = operator-gated follow-up task (verdict addendum 4).

**Ledger verification (2026-07-09, T-2026-CU-9050-028):** five checkboxes contradicted their own annotation. Each was re-checked against the code instead of flipped blindly — with one hit: **the annotation of P1.26 was itself wrong**, the finding is a real open dead-code bug (Step 2 had been misled by cooldown rows from an older code version). Flipped: P1.5, P1.11, P1.18, P2.50. Stayed open with a sharpened annotation: P1.26 (real bug), P2.2 (only the TZ dimension is resolved, not the column width). Newly split off/found: **P1.45** (post paths discard the artefact `model_id` — latent hard-rule-6 violations in MIS/RUB/QM), **P2.51** (guard silently disarms when goldens are deleted), **P3.13** (tag-length net only covers Mayank). Also verified against the code and still open: P1.37, P1.39, P1.41, P1.43, P1.44 — PRs #13/#15 addressed none of them.

**Step 3 (2026-07-03) has run** — ML trainer audit over `Documents\_X` (6 parallel reviews, trainer ↔ live bot ↔ artefact introspection): `audit_reports/13_x_ml_trainers.md`. Summary: ABR1's 11/18-feature bug proven in the trainer (split counts=0), AIM1 inversion explained by code (volatility label + round-join look-ahead + dead vocabulary), RUB1 with a silent MACD 9/21↔12/26 break, ATB1 trained on a different event type than it trades, EPD1 goes live without consulting its training gate, MIS1 without any provenance + ticker leakage; P1.18 refuted; SRA1-v2 = proven format conversion of v1.

**Step 9 (2026-07-04): DB architecture, performance & calculation consistency** — `audit_reports/18_db_architecture_performance.md` (live measurements + 2 code reviews). Summary: **TimescaleDB installed, 0 hypertables** — 9,782 plain tables (25 GB), WAL 8.5 GB/day, every 5m candle overwritten ~15×; `closed_trades_master`/`closed_ai_signals` without usable indexes → **~33 billion tuples/day full-table scans**; `telegram_outbox` 304 MB for 17k rows (bloat); 1,010 dead + 108 quarterly tables. Calculation consistency: **three ATR formulas** (engine Wilder / trade_utils SMA / regime ewm), three win definitions, two trailing semantics (5 vs. 8 — classic is systematically measured worse), four entry-freshness classes (10s–4h), ROM1 outcome via random match (LIMIT 1 without ORDER BY), four incompatible HVN definitions.

**Step 8 (2026-07-03): strategy/model concept evaluation** — all 26 strategies/models evaluated conceptually (edge hypothesis, training validity, live evidence from report 14) with grade A–F, overall ranking and portfolio recommendations: `audit_reports/16_strategy_concept_evaluation.md`. Summary: top candidates MIS1-72H, TD, SRA1, Support Resistance (each B−); reliably harmful AIM1, UFI1, Fast In And Out, QM_4H (each F); meta layer C+ (ROM1's added value is real, but the 4D regime hypothesis is untested — the TRANSITION residual class disables the gate 44.5% of the time); no model currently has demonstrable ML skill — the gains come from S/R trade construction + rule gates + market regime.

**Step 7 (2026-07-03): monitor replay & gap check** — `audit_reports/17_monitor_replay_and_gaps.md`. Summary: monitor scoring agrees only **63.4%** with a first-touch replay of the 5m candles (17.8% missed TP1, 18.8% TP1 despite SL-first; Volume Indicator/5 Percent only 44/45% — P2.7/P1.2 quantified) → monitor rewrite before model retraining. Also: 800 outbox messages silently discarded (all timed out, 212 in the FIFO channel), ticker_10s empty, ai_signals deletes targets on close (AI fleet not retroactively auditable), 5m data gap-free.

**Step 6 (2026-07-03): regime orchestrator overall analysis** — `audit_reports/16_regime_orchestrator_analysis.md`. Summary: ROM1 delivers +8pp WR added value, but the whitelist is 89% default-open (747× insufficient_data), the 4D matrix has a median of 7 trades/cell, the detector never held a TREND regime in 5.5 months (7 episodes, all <1h) and flaps in 52% of episodes; auto-close cuts 49% of trades while in profit (median 0%). Proposals in 4 stages incl. suppressed-counterfactual scorer and empirical-Bayes shrinkage instead of the 4D whitelist.

**Step 5 (2026-07-03): strategy proposal list** — data-driven concepts for new long/short strategies and models in `audit_reports/15_strategy_proposals.md` (S1-S13 + hypothesis tests: confluence-2 = +2pp WR but 4+ models = contra signal; regime-direction matrix CHOP→SHORT only; AIM1 fade +9.5%/trade on paper, shadow only; the FIFO problem is selection, not tails).

**Step 4 (2026-07-03) has run** — realised bot/strategy results from the live DB: `audit_reports/14_bot_performance_db.md`. Summary: 82% of closed_ai_signals is migration-duplicate junk (357k rows, unique index missing); active era deduplicated: AI overall +0.77%/trade (carriers: MIS1-72H, EPD1, MIS1-168H, ROM1), clearly negative: AIM1, UFI1, BR family, BB_1H; classic overall −0.07%/trade despite 63% "WR" (win=TP1 touch is misleading as a KPI); large directional asymmetries (EPD1 SHORT 76.5% vs LONG 50.2%).

**Step 2 (2026-07-03) has run** — results/evidence in `audit_reports/STEP2_DB_VERIFICATION.md`. Marking here: ✔ = confirmed live · ✘ = refuted/defused · details in the Step 2 report. Most important new findings: data ingestion WS dead for 6h with a green watchdog (P2.47 live), whale logger dead since 18.4., AIM1 calibration inverted (conf>0.9 → 9.3% WR), whitelist raw names frozen since 19.4.

**Important overall finding:** the repo is visibly in the middle of a remediation wave (many clean `# FIX:` comments). The bulk of the severe findings are **"fix made on one side of the contract, other side forgotten"** (writer UTC-fixed / reader naive; ensure_schema migrates / 3_detectors creates narrow; pretty_name in the analyzer / raw in the orchestrator). Four structural root causes (below, section R) together produce ~60% of all individual findings — whoever fixes those first clears the widest area.

---

## R — Structural root causes (fix these first — 1 fix each kills a whole bug class)

- [ ] **R1 ✔(Step2: partial candle + indicators on it proven) — nail down the forming-candle contract.** `1_data_ingestion.py:488-502` buffers **every** WS kline without the `k['x']` closed flag → every `{sym}_{tf}` table contains the running candle; `2_indicator_engine.py:551-565` computes indicators on it and stamps at **:02 AND :32**. Consequence: look-ahead/repaint in ~all strategies and ML bots. **Fix:** either write only `k['x']=true` candles into the candle tables (a separate live-tick table for that need), or an `is_closed` column + all readers exclude the running candle. `chart_data_service.py:247-249` already does it right (reference). Affects: MIS1 (critical), RUB1, SRA1, AIM1, ATB1, all 5 classic strats, SMC 16/24/25/29. ~(2026-07-13 T-2026-CU-9050-107: **migration block 1 (offline tooling) done** — 12 offline read sites read via `core.candles` with `include_forming=False` (trainers/backtests/dataset builder/rgcore/charting, `docs/CANDLE_CALL_SITES.md` §4 "Status block 1"); kills the look-ahead in the QM/SMC trainers + regime study, verified forming-free against the live DB. The live path stays open: the DB writer fix (`1_data_ingestion`/`2_indicator_engine`, is_closed) is block 6/C-Gate, the live bot readers are block 3/4 — box stays `[ ]` until then.) ~(2026-07-13 T-2026-CU-9050-108: **migration block 2 (strategies + `3_detectors` + shared helpers) done** — 7 live read sites via `core.candles` with `include_forming=False`: `trade_utils.calculate_smart_targets`+`get_hvn_and_sr_levels` (highest fan-in, level pool for all AI bots), `market_utils.calculate_obv`, `strat_main_channel/support_resistance/volume_indicator` + the detector indicator frame of the 5 classic strats. DESC→ASC trap: detector read via the API (ASC) + `.iloc[::-1]` = exactly the previous DESC frame, zero consumer reindex, `iloc[0]` is now the newest CLOSED instead of forming candle. Mechanics 149/149 verified read-only against the live DB; live signal-rate A/B not measurable because fleet ingestion was down ~2.4h during the check run (no forming candle). `docs/CANDLE_CALL_SITES.md` §4 "Status block 2". **Live behaviour change → Michi approval before merge.** Open remain block 3 (monitors/orchestrator explicit `True`), block 4 (AI bot direct readers) and block 6/C-Gate (DB writer is_closed) — box stays `[ ]`.) ~(2026-07-13 T-2026-CU-9050-109: **migration block 3 (monitors + orchestrator + price fallbacks) done** — the 7 remaining price/scoring readers on the money path read via `core.candles` with **explicit `include_forming=True`**: `5`/`8` SL/TP scoring, `28._get_latest_price`+`._get_last_close_price`, `3.get_live_price` fallback, `29.get_live_price`, `6._fetch_last_close_or_entry`, `core/health_monitor` DATA_STALE canary. **Behaviour-preserving** (no forming filter → byte-identical reads); the `True` is made visible/reviewable before block 4 brings the first `False` into the money path. Verified DB-free (py_compile/ruff/mypy/guard smoke+verify). `docs/CANDLE_CALL_SITES.md` §4 "Status block 3". Open remain block 4 (AI bot direct readers, `False`) and block 6/C-Gate (DB writer is_closed) — box stays `[ ]`.) ~(2026-07-13 T-2026-CU-9050-111: **migration block 4 tranche 1 done** — 6 AI bot direct readers read via `core.candles` with `include_forming=False`: `9_ai_sr`/`10_pump_dump`/`13_ai_rub`/`15_ai_master`/`18_ai_abr1`/`29_ufi1`. Michi's guiding principle: **detection = closed candles**, live price only for generation via `get_live_price` (§5). Real R1 effect (signal rates deliberately drop) at 10 + 29; the rest no-op/as-of-bound. Verified DB-free. Tranche 2 (offset rework 7/12, live-CMP deferral 22/24/25, alias+get_live_price 11) follows; `14_ai_atb` stays on the ATB2 track (T-106). `docs/CANDLE_CALL_SITES.md` §4 "Status block 4 — tranche 1". Box stays `[ ]` until block 4 is complete + block 6/C-Gate.) ~(2026-07-13 T-2026-CU-9050-111: **block 4 tranche 2 subset** — `12_ai_ats` + `7_pattern_detector` (offset rework, no live-CMP deferral) to `include_forming=False`: detection candle unchanged (`iloc[-2]`→`iloc[-1]` resp. `len−2`→`len−1`), 7 loses the edge-pivot forming repaint, 12 transitional OBV baseline shift until the ATS retrain. Verified DB-free. Open: `22`/`24`/`25` (live-CMP deferral via `get_live_price`) + `11_ai_mis` (alias + get_live_price entry). "Status block 4 — tranche 2 subset".) ~(2026-07-13 T-2026-CU-9050-111: **block 4 tranche 2 complete → block 4 done on the code side** — `22`/`24`/`25`/`11` to `include_forming=False`. `get_live_price` helper lifted to `core/live_price.py` (Michi source decision), `3_detectors` re-exports it. Finding: `current_price` feeds the **detection gate** at `22`/`24`/`25` (not just entry) → **batch ticker upfront** (1 call/cycle) instead of per-coin `get_live_price`. `25` is the heaviest offset rework (`last_closed`/`n_closed`/TD gates/BB anchor +1). `11`: `df.rename` reproduces the 3 `MIS_SQL_INDICATOR_SELECT` aliases, constant byte-identical to walkforward. Contract 2 caught up (`11`/`12` are no longer forming readers). Verified DB-free (py_compile/ruff/mypy/guard 24/24 + batch-ticker test 4/4). Only `14_ai_atb` remains out (ATB2 T-106). **Live behaviour change → Michi go before enqueue.** Box stays `[ ]` until block 6/C-Gate.) ~(2026-07-13 T-2026-CU-9050-112: **migration block 5 (shared feature builders + trainer/replay) done** — `core/research_features.fetch_context_frame` (research bots 30-33) + `core/regime_logic.compute_features` (`26_regime_detector` live + `backfill_regime_history` replay) read via `core.candles` with `include_forming=False`. 5a feature parity = no-op (feature candle via `searchsorted` unchanged; `CONTEXT_IND_COLS` one source for live+offline join, hard rule 7) — BUT the entry anchor of bots 30/31/32 (`live_price=iloc[-1]`) shifts forming→last-closed (gated/not deployed → follow-up T-2026-CU-9050-113). 5b **live gating** (forming→closed 15m → `classify_regime` → orchestrator whitelist); backfill `end=last_closed_open_time("15m", as_of)` (NOT `end=as_of` — cutoff is DB-`now()`-based) removes look-ahead → `regime_history` regen + TRM1 retrain as a VPS follow-up. Float cast (Decimal). Reviews z-code-reviewer 3/3 PASS + z-spec PASS, verified DB-free (guard 24/24, PR #102 merged). "Status block 5". Box stays `[ ]` until block 6/C-Gate.) ~(2026-07-13 T-2026-CU-9050-114: **migration block 6 part 1 (DB writer code rewiring) done, PR #104 merged** — the candle/indicator **writers** go via `core.candles`: `1_data_ingestion` (insert_fast closed/forming split, `_flush_to_db` `closed=k['x']` — **the WS buffer now carries the real Binance closed flag, first is_closed entry via WS**), `2_indicator_engine` (**core fix**: indicators only on closed candles via `include_forming=False`, commit to the caller), `6_housekeeping` (gap filler `upsert_candles(closed=True)`, retention/invalidation/delisted scan). Four new frozen-sig `core/candles.py` gaps: `latest_open_time(kind='indicators')`, `delete_candles_before`, `delete_indicators_from`, `list_coin_tables`. Review fix: gap filler counts real fills instead of rows sent (`>`→`>=` edge candle). Both core reviews PASS (z-code 3-vote, z-spec 18/18), verified DB-free + live-VPS parity (delete byte tests via TEMP tables behind `KYTHERA_CANDLES_WRITE_PARITY`). "Status block 6 — part 1". **Part 1 is pure code rewiring — the box only closes with the C-Gate conclusion (part 3); stays `[ ]`.** Open: block 6 parts 2/3 (retrain rollout + C-Gate, every step Michi-gated).) ~(2026-07-13 T-2026-CU-9050-118: **C-Gate phase 0 done + executed on the live DB, PR #108 merged** — the two EMPTY target hypertables `candles` (9 columns) + `indicators` (113 columns) created via new `core/candles_schema.py` (`ensure_hypertables`, oi_5m pattern). `indicators` columns derived at build time from `2_indicator_engine.get_indicator_definitions()` (one source, no drift). D-2026-CLD-109: **REAL→double** (verified 0 float4), retention unlimited, **compression deferred to phase 5** (operator). `core.candles` UNTOUCHED — reads stay legacy, rollback trivial (`DROP TABLE`). Verified live (hypertables present/empty, parity {tf,is_closed} added, no policies); both core reviews PASS, guard 24/24. Gate `test_candles_db_parity.py` 11/12 (1 fail = now-anchored freshness assertion, victim of the ingestion outage, not a phase-0 regression). "Status C-Gate phase 0". Open: C-Gate phases 2–5 (dual-write/backfill/parity/cutover/drop) + retrain rollout — **box only closes with phase 5.**) ~(2026-07-13 T-2026-CU-9050-119: **C-Gate phase 2 (build) done** — three dormant, gated code slices: **2a dual-write** (PR #110 merged) `KYTHERA_CANDLES_DUAL_WRITE` (off by default) → `upsert_candles`/`upsert_indicators` additionally write the hypertables in the caller transaction, no bot change; **2b backfill** (PR #111) `tools/candles_backfill.py` copies the history idempotently/resumably, per-row `is_closed` instead of blanket `true`, indicators copy/cast (D-109 #4); **2c 1d/1w WS removal** (PR #112) `WS_TIMEFRAMES` on both `@kline` builders, REST unchanged. Activation (flag/deploy/backfill/parity → phase 3–5) fully Michi-gated; reads stay legacy until phase 4. Both core reviews each PASS, guard 24/24, DB byte tests behind `KYTHERA_CANDLES_WRITE_PARITY` (rollback = zero persistence). "Status C-Gate phase 2". **Box still only closes with phase 5.**) ~(2026-08-01 T-2026-KYT-9050-011: **block-5 follow-up closed** — the research bots `30_ai_pex1`/`31_ai_fmr1`/`32_ai_trm1` no longer take the entry price anchor from `df["close"].iloc[-1]` (since block 5 the last CLOSED 1h candle, stale up to ~59 min), but via `core.live_price.get_live_price` — contract-2 path like the block-4 bots 11/22/24/25. Feature candle (`searchsorted` floor-1) unchanged, frame stays ASC/closed-only; ONLY the price for `calculate_smart_targets` + `log_prediction` shifted. New: `get_live_price`→None ⇒ signal is skipped instead of posted with `None` (per-bot cooldown semantics unchanged). **Live status (re-verified, corrects the original annotation "all three gated and not deployed"):** the gate is **open** — `.env` carries `NEW_IDEAS_LIVE_POSTING=1`, the code default is `1` anyway (`os.getenv("NEW_IDEAS_LIVE_POSTING", "1") == "1"` in 30/31/32/33; a disablement would have to be explicit), and the bots log `Posting: LIVE` at start. Bot 30 is also **deployed**: `pex1_model.pkl` is git-tracked and loaded (`Artefakt geladen: pex1_model.pkl — 13 Features, Threshold 0.70, Tag PEX1, Kalibrator: ja`, most recently 2026-08-01 07:32). Only 31/32 are truly artefact-less (idle mode, `fmr1_model.pkl`/`trm1_model.pkl` don't exist). Still no live delta today — but for a different reason: the PEX1 scan aborts on **every** cycle (`PEX1-Scan-Fehler: can't subtract offset-naive and offset-aware datetimes`), in the `try` block **before** the event loop (`detect_spike_time_offset_h` subtracts a naive `now` from `MAX(spike_time)`), so `process_event` — where this fix lives — is never reached. Consistently proven ~1×/min from 2026-07-19 21:37:56 to 2026-08-01 16:10:10 (8166 failures in the four most recent `logs/watchdog_debug_*` alone, no successful scan; onset possibly earlier, not traced back further). Logged as **T-2026-KYT-9050-061** — once the abort there is fixed, this fix takes effect **immediately, live**. Verified DB-free: `backtest/test_research_bots_live_price.py` 8/8, `test_feature_lookahead` 19/19, ruff/format green, guard `smoke`+`verify` 24/24. Box stays `[ ]` until block 6 part 3 / C-Gate phase 5.) 📏(2026-08-01 T-2026-KYT-9050-002: **the C-Gate has been live since 2026-07-16 — the above annotations "activation fully Michi-gated / reads stay legacy until phase 4" are outdated.** Measured read-only, report `docs/T-2026-KYT-9050-002-c-gate-status.md`. Proven instead of taken from the ticket: `.env` carries `KYTHERA_CANDLES_SOURCE=hyper` + `WRITE_PRIMARY=hyper` + `DUAL_WRITE=1`; all per-coin tables end exactly at `open_time = 2026-07-16 16:00 UTC`, preceded by the watchdog restart `watchdog_debug_20260716_192326.log` (16:23 UTC), and `core/candles.py` skips the per-coin write when `write_primary=hyper`. **Phase 3 (parity cron ≥5–7 days) was skipped** and cannot be made up — `candles_parity.py` compares legacy against hyper, the legacy side has been empty for 16 days (`rows old=0` for every symbol). Both dormant tools are otherwise intact (self-check + dry-run green under 3.14 **and** fleet 3.13). **The R1 storage contract holds:** `is_closed=false` on exactly 527 rows per TF (= one forming candle per symbol), indicator freshness per TF exactly closed-only-correct, candles+indicators gap-free across the 07-16 boundary. **Volume figures correct the report-18 number:** legacy per-coin **64 GB** (not 25 GB), of which 54 GB indicator tables; hypertables `candles` 45.0M rows/9,954 MB + `indicators` 18.6M/20 GB; DB total 98 GB; C: 78 GB free (not ~160 GB). **Compression NOT active** (0 of 128 chunks each, no policy) — the whole expected gain is unrealised; measured anchor from the same DB: `oi_5m` 652 MB→78 MB = 8.35×, an upper bound for the 108-float-column `indicators` (honest estimate 30 GB → 4–10 GB). **New finding:** `16_smc_forex_metals_bot.py:87` + `21_btc_smc_strategy.py:136` are the only remaining raw SELECTs on per-coin tables in live code (11/14/34 + `core/*_features` have been rewired since `e5bddde`) and have been reading frozen candles for 16 days; both were deliberately left back in `CANDLE_CALL_SITES.md` as "index-coupled", which assumed legacy stays authoritative. Blast radius verified and inconsequential — `CH_SMC_METALS` last posted 2026-07-16 09:35 UTC (9 posts in the whole outbox window), `CH_BTC_SMC` zero posts —, but both emit a Cornix block, so luck rather than design. **No fix committed:** the bots are effectively shut down, reconnecting them is an unparking (OPUS-HANDOFF §6, Michi). **Rollback promise broken:** `KYTHERA_CANDLES_SOURCE=legacy` today silently sets the fleet onto 16-day-old candles (the write-primary asymmetry predicted in `core/candles.py`, now materialised). Open for phase 5: activate compression, legacy drop (64 GB — only after a `pg_dump` restore test **and** removing the `CREATE TABLE IF NOT EXISTS` loop `6_housekeeping.py:67`, otherwise ~4,200 empty tables grow back), verdict on 16/21; a replacement parity gate G1–G6 against the inventory is pre-defined in the report. Box stays `[ ]`.) ✅(2026-08-02 T-2026-KYT-9050-068: **the two raw SELECTs are gone — `core.candles` is now the only candle read path in live code.** `21_btc_smc_strategy.fetch_db_data` and `16_smc_forex_metals_bot.fetch_db_data` read via `read_candles(..., include_forming=False)`; both bots are thereby reconnected to the active backend switch and will post again from the next fleet restart (Michi decision 2026-08-02: **flip it live**, not park it). **The trap when rewiring:** both dropped the newest row via `.iloc[:-1]`, because their raw SELECT brought in the running candle. `include_forming=False` already filters it in the query — so the drop had to **go**, otherwise it would have discarded the newest CLOSED candle and delayed every signal by one candle. In bot 16 the drop sat at the **shared** call site for both sources and had to stay for the yfinance path, which still delivers its forming candle. **Correction to the T-002 finding:** bot 16 was only half affected — only the METALS group (`source="database"`, 8 Binance symbols) read the DB; the FOREX group fetches live via `yfinance` and never touched the per-coin tables. The table `EURUSD=X_1h` (frozen since 2026-02-25) is unused legacy stock, `GC=F_1h` doesn't exist at all. **Side finding, not fixed:** `XAUUSDT`/`XAGUSDT` also end in the hypertable on 2026-07-06 — two of the eight METALS pairs remain without fresh data, independent of the C-Gate. **Problem 2 closed:** new staleness guard in `core/candles.py` — on a process's first legacy read it measures how old the newest candle in the backend really is, and above the threshold (`KYTHERA_CANDLES_LEGACY_MAX_AGE_MIN`, default 180 min) hard-aborts with `CandleSourceError` instead of silently serving frozen prices. One process probe, then cached; hyper reads pay nothing. A **non-measurable** probe (table missing, permissions, test fixture) is deliberately NOT counted as staleness — it warns and steps back. `latest_open_time` remains unprotected: that is the freshness query itself. 15 tests in `backtest/test_c_gate_bot_readers.py`; ruff/format/mypy green, guard 24/24. **Effective only after a fleet restart.** Problem 3 (skipped parity window) remains permanently open — not fixable, only known. Box stays `[ ]` until phase 5.) `[DB]`
- [x] **R2 (a) ✅(2026-07-11, T-2026-CU-9050-091: `core/fleet.py` is now the ONE process definition (name/script/group/delays); `main_watchdog.py` and `dashboard.py` import it — drift closed, the dashboard now automatically shows the full fleet incl. the previously missing bots 26–34. No behaviour change to the watchdog: identical start order/delays, pinned via `backtest/test_fleet_definition.py`; lifecycle mechanics P0.2/P2.48/P1.37/P2.47 untouched.) (b) ✅(2026-07-12, T-2026-CU-9050-098: `docs/schema.sql` = pg_dump 17.6 --schema-only of the live DB — all 44 app tables incl. the previously DDL-less `ai_signals`/`ml_predictions_master`, plus BTCUSDT_1h/_indicators as the template of the per-coin family; the 9,789 generated tables (per-coin, quarterlies, forex `=X`, `_GOLD`, CJK junk from the P2.16 leak class → D5) are documented as families in the header. Reference, not a migration — the executing DDL remains the CREATE sites, live ALTER stays an operator gate. The "resp." migration runner is therefore deliberately not built — the reference variant was chosen.) — single-source fleet/schema definitions.** (a) The process list exists twice and has already drifted by 4 bots: `dashboard.py:31-62` vs `main_watchdog.py:52-66` (dashboard is missing 26/27/28/29). (b) `CREATE TABLE` scattered across ~10 files with drift (`trade_cooldowns` ×4 with WITH/WITHOUT TZ; `telegram_outbox` narrow in `3_detectors.py:103,141` without `image_path`); `ai_signals` (13 writers) and `ml_predictions_master` (9 writers) have **no DDL in the repo at all**. **Fix:** `core/fleet.py` (name/script/group/delays) + `docs/schema.sql` (pg_dump) resp. `core/schema.py` with a migration runner at watchdog start.
- [ ] **R3 ✔(Step2: session TZ Europe/Bucharest; naive columns mixed UTC/local) — central UTC policy.** Writers partly UTC-fixed, readers naive-local → TZ offset bugs in cooldowns, trade windows, statistics (see P1-TZ, P2-TZ). **Fix:** `core/time.py: utc_now()`, replace all `datetime.now()`, `-c timezone=UTC` in the pool `options` (`core/database.py:42`), money-relevant time columns → `timestamptz`. Optional ruff rule `DTZ` (flake8-datetimez) in `pyproject`. `[DB]` ✅(2026-07-09 T-2026-CU-9050-032: Python half — `core/time.py` + ruff `DTZ`.) ✅(2026-08-01 T-2026-KYT-9050-005: **DB half, ONE changeset** — `-c timezone=UTC` in `core/database._connect_options`, `3_detectors.write_signal_atomic` onto `utc_now_naive()` (P2.3), and the six drift compensations replaced by **one** constant (`core.time.R3_CUTOVER_UTC`, default `None` = uniform-UTC; `legacy_naive_to_utc`/`utc_to_legacy_naive`). Effective from the fleet restart, per process. Measured live read-only: `pump_dump_events.spike_time` and `master_ai_processed_signals.processed_at` are **timestamptz** (the inventory in UTC_POLICY §3 was wrong on three rows, corrected), `closed_ai_signals.close_time` is **naive** (not timestamptz as claimed), and `regime_history.ts` demonstrably carries naive-**UTC** (12 rows in the hour 03:00–04:00 that did not locally exist on 2026-03-29). DB-free: `backtest/test_r3_utc_flip.py` 12/12.) **Box stays `[ ]`:** open are (a) the history decision backfill-vs-cutover with numbers in UTC_POLICY §6 (operator, Michi) and (b) the `timestamptz` DDL switch (`docs/migrations/2026-07-r3-timestamptz.sql`, C-Gate).
- [x] **R4 STRUCK (operator decision Michi 2026-07-14) — no rollout, point closed.** The **entire fleet trades Cross-Margin**: every signal-emitting bot posts `🚨 Margin: Cross` (emitter sweep 2026-07-14 over every `get_max_leverage` call site: 7/9/10/11/12/13/14/15/16/24/25/28/29/strat_5_percent + `core/signal_post.py`). This means the isolated `1/lev` liquidation assumption behind `cap_leverage_to_sl` does not apply **fleet-wide** — the same reasoning ROM1 (T-2026-CU-9050-101) and MIS2 (`11_ai_mis_bot.py:77-79`) already deliberately use to skip the cap. The "remaining signal-emitting bots open" rest of R4 is dropped entirely; **no** rollout of `cap_leverage_to_sl`. Grandfathering: the existing caps in 21/29 (P0.5/P0.6) stay untouched (harmless, no live touch, no rollback ordered). The only margin-mode-independent leverage protection remains the 15% SL-distance cap (P2.27 — in the ROM1 path and in 17 via P2.45). ~~History: R4 partial (21 P0.5, 29 P0.6, ROM1 path ✅2026-07-04 → ⏪2026-07-12 T-101 reverted); original fix proposal to use `cap_leverage_to_sl` on every signal-emitting bot — superseded by the Cross-Margin decision.~~

---

## P0 — Money-critical (immediate)

### Double trades on Cornix (several independent paths)
- [x] **P0.1 ~(Step2: 0 retry double sends so far; but upstream double generation in trading channels) ✅(2026-07-04 implemented: status column `pending→sending→sent`; claim via FOR UPDATE SKIP LOCKED + commit BEFORE the send; TimedOut = unknown outcome stays 'sending' with no retry in this pass; orphaned 'sending' rows: trading channel → dead_letter + WARNING, info channel → pending; channel with an open 'sending' row locked until resolved) Outbox is at-least-once → duplicate sends to the Cornix channel.** `4_telegram_bot.py:240-264` sends → `mark_sent` → `commit`. Commit failure after a successful send / crash between send and commit / `TimedOut` after acceptance → re-send → Cornix opens the trade twice. **Fix:** commit a `sending` status BEFORE the send; rows stuck in `sending` at restart go to dead letter for trading channels instead of auto-resend; treat `TimedOut` as "unknown outcome". `[DB]`
- [x] **P0.2 ✅(2026-07-04 implemented: Windows named mutex `Global\KytheraWatchdog` via ctypes → second instance exit(1); orphan detection at start via psutil, terminates fleet processes with a foreign parent with a 5s grace + kill; shutdown hardened per-child-guarded + atexit hook. No job object needed — mutex+orphan sweep covers the double-fleet case) No single-instance guard on the watchdog → duplicate fleet.** `main_watchdog.py`: `taskkill /F` does not run through the SIGTERM handler → orphaned children; a new watchdog spawns a second fleet → every signal doubled → Cornix doubled. **Fix:** named mutex/pidfile at watchdog and bot start; Windows job object `KILL_ON_JOB_CLOSE`; orphan detection at start. `[DB]`
- [x] **P0.3 ✔(Step2: 109 self-echo rows) ✅(2026-07-04 implemented: channel filter `IS DISTINCT FROM` in the SELECT + ROM1_SIGNATURE hard reject + cooldown before send + tracking+outbox post in ONE transaction; plus per-row isolation with cursor advance) Orchestrator consumes its own ROM1 posts.** `28_signal_orchestrator.py:386-421,524-537`: the ROM1 message contains `Triggered by: MIS1-8H` → matches the bot patterns; the scan SELECT has no channel filter → self-echo through the whole pipeline; the only protection is the 4h cooldown, which is only committed AFTER the send → crash window = second trade. **Fix:** `channel_id != REGIME_TRADING_CHANNEL_ID` in the SELECT; ROM1 hard reject; commit cooldown/tracking BEFORE the send. `[DB]`

### Regime gating effectively disabled (core feature!)
- [x] **P0.4 ✔(Step2, refined: raw-name rows frozen since 19.04. → MIS+channel bots gate on 2.5-month-old stats) ✅(2026-07-04 implemented: `pretty_name(bot_name)` after `identify_bot()`; pretty_name additionally hardened case-insensitively. ✅(2026-07-10, T-2026-CU-9050-046: default-open-rate alarm in the hourly regime status — share of default-open/fallback/4D over 24h, ⚠️ from 20% bypass; became evaluable via the new `wl_reason` column (B8). Staleness gate on `computed_at` >48h → fallback instead of a 4D cell)) Whitelist gate runs into a void (bot-name mismatch).** `28_signal_orchestrator.py:134-148,240-251,556` vs `27_bot_regime_analyzer.py:322`: the analyzer writes `pretty_name()`-normalised keys (`MIS1-8h`, `FastInOut`), the orchestrator queries with **raw** names (`MIS1-8H`, `Fast In And Out`) → the case-sensitive lookup never finds anything → `(True, "no_whitelist_entry")` → **signal always passed through**; fallback + regime auto-close are equally inert. Affects the whole MIS family + all channel fallback bots. **Fix:** `bot_name = pretty_name(bot_name)` right after `identify_bot()` (and on insert into `orchestrator_open_trades`); alarm on the default-open rate. `[DB]`

### Leverage beyond liquidation
- [x] **P0.5 ✅(2026-07-04 implemented: DESIRED_LEVERAGE=25 + `cap_leverage_to_sl()` from R4 on both signal sites) `21_btc_smc_strategy.py` — 100x leverage with 0.4–1.2% SL.** Isolated liquidates at ~-0.9% *before* the SL; every stop = -100% margin.
- [x] **P0.6 ✅(2026-07-04 implemented: `cap_leverage_to_sl()` at the post site → typically 1-2x; cap_leverage_to_sl fixed to be string-tolerant in the process — `get_max_leverage()` returns "20x", the int() cast previously raised ValueError and would have crippled 21/28/29) `29_ufi1_bot.py:194,244` — 20x with ~34% SL** (`sl=swing_high*1.03`, entry ~0.77·sh). Isolated liquidates at ~+5%; the backtest "+0.83R" doesn't survive 20x. **Fix:** leverage from SL distance (~1-2x) or a UFI1 cap ≤3x.

### Broken trades / silent data loss
- [x] **P0.8 ⚠️**CORRECTED 2026-08-02 (T-2026-KYT-9050-059 takeover): the original T-056 note "firewall blocks, not reachable externally" is **REFUTED**. Re-measured: `DefaultInboundAction` is `NotConfigured` on all three profiles, not `Block`, and **two Symantec rules — `SMC Service` + `SNAC Service`, both Enabled/Inbound/Allow/Profile Public — allow inbound TCP on ANY port from ANY IP**. The box sits with a public IP directly on the interface (no NAT, no hoster firewall). Evidence against the configuration reading: `logs/dashboard.log` counts **557 successful `GET / → 200` from foreign IPs** since 04.07.; at measurement time an ESTABLISHED session existed from the network. Unchanged after the reboot on 02.08. 05:29 (rules active, dashboard back on `0.0.0.0:5000`). **Lesson:** T-056 had correctly marked its own check method as unfit (connecting from the box to its own public IP = loopback) and then still believed the configuration instead of the empirical evidence — a failed measurement is not a negative finding. **Operator decision 2026-08-02: the rules stay — WONTFIX, risk accepted (T-2026-KYT-9050-070).** No open action item left; do not reopen this as a finding. What bounds the residual risk: `pg_hba.conf` only knows `127.0.0.1/32`, `::1/128` and the local socket → the live trading DB rejects outsiders, and from the next dashboard start the dashboard binds to loopback by default (T-056, PR #237), which removes the unauthenticated `stop_all` from the network — independent of the firewall. Exposed remain 135/445/3389/5985. Reopen ONLY on: (a) a foreign hit on a control endpoint in the dashboard log, (b) a `pg_hba` line for an external IP, (c) Symantec uninstall. Reverting would at any time be one command, elevated, immediately reversible, RDP untouched: `Disable-NetFirewallRule -DisplayName "SNAC Service","SMC Service"`.) ✅(2026-08-02, T-2026-KYT-9050-056: bind default `0.0.0.0` → `127.0.0.1` + `core/dashboard_security.py` with a host allowlist, optional constant-time token and origin check in `before_request`, plus a fail-closed start policy: non-loopback bind OR an allowlisted off-box hostname without a token = no start (also covers the tunnel case, where the bind looks harmless). Control endpoints now validate against `SCRIPT_MAP`. 58 tests in `backtest/test_dashboard_security.py`. Effective at the next dashboard start; no live intervention in this PR. The exposure part — `cloudflared` + Cloudflare Access, Z2/B4 — remains a Michi decision, see `docs/DASHBOARD_SECURITY.md` §4.) Dashboard without auth on `0.0.0.0`.** `dashboard.py:1152` + control endpoints `290-317`: anyone with port-5000 access can call `POST /api/system/stop_all` → fleet persistently parked (survives a reboot). **Fix:** bind to `127.0.0.1` (+SSH tunnel) or token auth in `before_request` + firewall. `[DB]` (check port reachability on the VPS)
- [x] **P0.7 ✔(Step2: 5 active + 79 closed) ✅(2026-07-04 implemented: `if t1==0: return None` in both strategies/both directions. Open: clean up the 5 existing active corrupt trades in the DB — Step-2 query 16) ✅(2026-08-02, T-2026-KYT-9050-009, DB remainder done: `active_trades_master` [558 rows from 24.02.] contains **0** rows with the P0.7 signature, in the closed archive exactly 1, last 2026-05-27 — i.e. before the fix. The 5 trades ran out, no cleanup needed.) ⚠️**SECOND DOOR, open until 2026-08-02 and fixed** (T-2026-KYT-9050-009): the `t1==0` guard only covers "no zones at all". `find_support_resistance_zones` filters against the close of the last closed candle, but the ladder is built against `entry = live_price` — if the live price runs past a zone, `sorted(zones, key=|zone−entry|)` selects it as TP1 and the interpolation drags TP2/TP3 along onto the losing side. Measured live 01.07.–01.08.: **342/3463 support-resistance and 12/188 main-channel trades with TP1 on the wrong side**, most recent 2026-08-01 23:33, one case active in the book (id 211171 LABUSDT SHORT). Downstream damage beyond the geometry: 96.5% of these trades closed with status≥1 ("TP hit") against 66.2% of the clean ones — phantom hits in the per-bot statistics that orchestrator gating decides on. Fix: `core.market_utils.select_zone_targets` filters against the entry (both strategies × both directions), test `backtest/test_zone_target_side.py`. Rollout effect measured: SR −1.1% signals / 10.1% corrected ladders, MC −2.1%/6.4%. **Effective only after a fleet restart.** Report: `docs/T-2026-KYT-9050-009-vps-audit-rest.md` §1) Empty-zone interpolation creates LONG TPs BELOW the entry.** `strategies/strat_main_channel.py:70-87,115-132` + `strat_support_resistance.py:53-57,65-69`: with 0 zones found, `t1==0` is unguarded → LONG TP1 = 0.75·entry (SHORT: -25/-50/-75%). **Fix:** `if t1==0: return None` (or a fixed-% fallback). `[DB]`
- [x] **P0.9 ✔(Step2: PK (symbol,open_time) vs ON CONFLICT (open_time); currently 0 gaps thanks to REST catch-up) ✅(2026-07-04 implemented: INSERT with symbol + ON CONFLICT (symbol, open_time), SAVEPOINT per row as the first try statement, error logged instead of swallowed) Nightly gap filler is a silent no-op.** `6_housekeeping.py:654-661`: PK is `(symbol, open_time)`, the INSERT uses `ON CONFLICT (open_time)` (no matching unique index → every insert throws) AND omits `symbol`; the exception is swallowed by `except:continue` → gaps are never filled, indicator invalidation skipped. The entire nightly safety net doesn't exist. **Fix:** `INSERT (symbol, open_time, ...) ON CONFLICT (symbol, open_time) DO NOTHING`; log per-gap errors. `[DB]`

### ML models train/measure something different from what the bots trade
- [ ] **P0.10 ✔(Step3: the same pattern confirmed in 7/8 _X trainer families — see 13_x_ml_trainers.md, X-R1) ~(2026-07-04/05 core fix built, branch feat/t-2026-cu-9050-016: shared walk-forward simulator tools/walkforward_sim.py replays bot-own setups bar by bar [UFI1/TD/BB/ABR1 adapters], wick-aware first-touch exits + fees, calculate_smart_targets runs identically in the replay via a df parameter; TD/BB/ABR1 retrains on replay labels in staging_models — results in report 19. OPEN: adapter+retrain for QM/SRA1; MIS1/RUB1/EPD1 built as MIS1/RUB2/EPD2; ATB1→ATB2 adapter+retrain runner built 2026-07-12 T-2026-CU-9050-104 [core/atb2_features.py, walkforward run_atb2, retrain run_atb, converging-channel §11] — label/train run VPS-pending, docs/ATB2_REBUILD.md; ATS1→ATS2 adapter+retrain built 2026-07-14 T-2026-CU-9050-121 [core/ats_features.py, walkforward run_ats, retrain run_ats/tools/retrain_ats.py, TSI crossover sniper §6, parity test backtest/test_ats_features.py — fixes OBV train/serve skew + bracket label] — label/train run VPS-pending; EPD path audited = already DB/R1-clean, no candle trainer possible [10s tick]) "Backtest the detector, trade something else."** SMC models label on idealised fills the live bots never trade: `smc_ml_trainer.py:128,159` (TD entry = pivot close, only known 10 bars later) and `:195-227` (fixed 1%/2% resp. 2R geometry) vs live `calculate_smart_targets` (`25_smc_ml_sniper.py:131`). `predict_proba` + thresholds (0.30/0.40) apply to trades that never execute. **Fix:** build a shared walk-forward simulator that replays the **bot's own** setup functions bar by bar; retrain on it. `[DB]` (shadow-log calibration)
- [x] **P0.11 ✔(Step2: live 25.7% WR, n=35) ✔✔(2026-07-04 walk-forward proof, report 19: +278R → +141R, of which +185R from October 2025 crash month alone, remaining 11 months −44R at ~14% WR; June 2026 cohort 37.5% ≈ live; 72% of trades MAE ≥ +5% → liquidated at 20x before TP. Recommendation: keep UFI1 OFF, no retrain) UFI1's "+278R" claim doesn't transfer to the live bot.** `29_ufi1_bot.py` vs `fib_backtest.py:252-262`: the backtest uses the **future global** window low for entry selection (look-ahead) and a 5-target trailing ladder; live posts a CMP entry (up to weeks old) + single TP1. **Fix:** backtest = walk-forward with `find_ufi1_setup`; the confirmation candle must be current (`j ≤ n-2`); align the exit model before quoting numbers. `[DB]`
- [ ] **P0.12 ✔✔(Step3: split counts in both live models = 0 for exactly these 11 features; the trainer has an identical bug — BT2-Datagrepper-for-ML.py:77-92) ~(2026-07-04 code fixes implemented, branch feat/t-2026-cu-9050-016: prefix matching + a hard ValueError in the bot AND the trainer [copy in trainers_x/], startup self-test "no continuous feature constant" in the bot, constancy assertion in the datagrepper; SIDE FINDING: ruff cleanup 052ba4c had removed the pandas_ta import → repo ABR1 would have silently died after deploy, restored. OPEN: retrain — staging candidate from batch E3, rollout decision Michi) ABR1: 11 of 18 features are constant 0 (proven).** `18_ai_abr1_bot.py:112-177`: `expected_pta_cols` never matches the pandas_ta column names (`KAMA_9` vs `KAMA_9_2_30`, `TSI_12_7` vs `TSI_7_12_7`, `DCL_20` vs `DCL_20_20`, `BBL_20_2` vs `BBL_20_2.0_2.0`) → NaN → `fillna(0)`. Verified directly from `bt2_model_*.json`: exactly these 11 features have **0 splits**. The trainer had the same bug — no skew, but the model actually trades on only 7 features (half the strategy signal missing). **Fix:** column prefix matching (like `14:197-211`), **retrain both models**, startup assertion "no feature constant".
- [ ] **P0.13 ✔✔(Step2: dummy overlap 2/22 resp. 0/5; calibration inverted, conf>0.9 → 9.3% WR — pause the bot!) ~(2026-07-04 vocabulary diff documented, report 19: ai_model overlap 2/16 [only ATS1/EPD1, all MIS dummies with the MSI1 typo], conv_bot overlap 0, strongest identity column conv_bot_nan gain 48.3. Batch E decision: NO retrain — a vocabulary-only retrain would reproduce the inverted volatility model [label+round-join], salvage = new project; leave OFF) AIM1 master: source identity one-hots are dead for almost all live signals (proven).** `15_ai_master_bot.py:220-273`: the feature list extracted from the pkl only knows `ai_model_MSI1-*` (typo/old spelling), `conv_bot_{5% Bot,Fast Bot,...}` — live writes `MIS1-24H`, `ATB1`, `BB_1H`, `Fast In And Out` etc. → `reindex(fill_value=0)` zeroes all identity dummies → the meta model cannot distinguish sources (its core job) → out of distribution. **Fix:** retrain on the current vocabulary; a drift log for discarded dummy columns. `[DB]`

---

## P1 — High

### Telegram / monitors
- [x] **P1.1 ✅(2026-07-04 implemented: 15-min TTL in the poll SELECT for all non-info channels; older pending rows marked `expired` once per poll with a count WARNING; info channels REGIME_STATUS/ATB_INFO without TTL) No staleness TTL on the outbox.** `4_telegram_bot.py:183-194`: after downtime, hour-old signals get blasted out at long-stale prices. **Fix:** `AND created_at > NOW()-INTERVAL '15 min'` for signal channels, older ones `failed='expired'`. `[DB]`
- [x] **P1.2 ✔quantified(Step7: replay — 18.8% of trades got TP1 despite SL-first) ✅(2026-07-04 implemented: `targets[new_level-2]` instead of the old SL, like 8_ai) The trailing SL in `5_trade_monitor.py:246-247` never trails** — for level 2/3 the **old** SL (`trade['sl']`) is passed instead of `targets[new_level-2]` (8_ai does it right). All multi-target PnL/win rates systematically wrong. `[DB]` (CRITICAL if anything posts `active_trades_master.sl` to Cornix)
- [x] **P1.3 ✅(2026-07-04 implemented: `failed_channels` set — after every send failure (incl. TimedOut/chat-not-found) the channel is skipped for the rest of the batch) Per-channel FIFO breaks on a transient send failure** (`4_telegram_bot.py:305-324`) → an SL update can arrive before its entry signal. **Fix:** block a failed channel for the rest of the batch.
- [x] **P1.4 ✅(2026-07-04 implemented: new helper `core/trade_utils.format_price` — >=1→4dp, >=0.001→6dp, below that ~6 significant digits without scientific notation; all open_handler.py price fields switched to it) `:.6f` price formatting destroys sub-0.001 coins** (`handlers/open_handler.py:104-110`) → rounded/collapsing TPs, Cornix rejects. **Fix:** significant digits / tickSize. `[DB]`
- [x] **P1.5 ✘refuted(Step2: the column is INTEGER; 2026-07-09 T-028 re-verified: additionally a defensive cast `int(targets_hit)` in try/except, `8_ai_trade_monitor.py:216-219` — no crash even with a TEXT schema) `8_ai_trade_monitor.py:265` — `int > str` TypeError if `current_target_hit` is TEXT** → the whole monitor iteration dies → SL hits of all AI trades go undetected. **Fix:** `old_targets_hit = int(targets_hit or 0)` with try/except. `[DB]`

### Orchestrator / regime
- [x] **P1.6 ✅(2026-07-04 implemented: sent/failed filter removed, id cursor as the novelty source, cursor init to MAX(id) at process start against restart duplicates in suppressed_signals) The `sent=FALSE` filter races against the dispatcher** (`28:524-537` vs `4:263,338`) → signals sent between two passes fall out of the SELECT → never gated, no log. **Fix:** remove the `sent/failed` condition, novelty via an `id` cursor. `[DB]`
- [x] **P1.7 ✅(2026-07-04 implemented: cooldown via update_cooldown(commit=False) pulled into the per-row txn — cooldown+tracking+outbox commit atomically, outbox insert as the last write; per-row cursor + try/except were already there from the P0.3/P1.6 batch) The forward pipeline isn't atomic; the batch cursor only advances at pass end** (`28:597-627`) → a crash after sending = a trade at Cornix with no tracking; an exception at row 5/10 = rows 1-4 get re-posted. **Fix:** DB writes in one txn first, outbox insert last, cursor + exception per row. `[DB]`
- [x] **P1.8 ✅(2026-07-04 implemented: only closed_ai_signals with model='ROM1', open_time ±60s against opened_at, ORDER BY smallest time difference, LIMIT 1; the closed_trades_master check removed — ROM1 never writes to active_trades_master. ⚠2026-07-10 T-044 falsified live: the fix had been silently dead since the 04.07. 11:10 deploy — `insert_rom1_signal` didn't set open_time, the DB default stamped session TZ (+3h, measured consistently at +10.799s), the ±60s window never matched; 395 OPEN corpses, opposite blocks 4→165/day. ✅2026-07-10 T-052 follow-up fix: open_time explicitly naive-UTC in the INSERT + a corpse reaper at the start of the sync pass (an OPEN row with no row-anchored ai_signals twin, legacy window hard-pinned to Europe/Bucharest → after 72h CLOSED_NEUTRAL/corpse_reaper, no post; anti-censorship: a syncable closed_ai_signals row in either window → skip); the legacy window is symmetric in the match loop too → old corpses with a close row get their real outcome, collision-free thanks to the 4h cooldown (test invariant); direction checks deliberately WITHOUT an age bound — the P2.26-72h bound in is_same_direction_open is also removed, because it would have unblocked genuine >72h positions (flip/stacking risk, review PR #40); main-loop stages individually isolated (a poison row no longer starves the sync)) `sync_closed_trades` matches foreign trades** (`28:879-925`, no model filter, no ORDER BY, 720h window) → wrong ROM1 outcomes + premature loss of opposite-side protection (hedge/double exposure). **Fix:** only `closed_ai_signals WHERE model='ROM1'`, ±60s against `open_time`, ORDER BY. `[DB]`
- [x] **P1.9 ✅(2026-07-04 implemented: ai_signals SELECT restricted to model='ROM1', the active_trades_master close block removed entirely — other bots stay untouched, classic_closed stays 0 in the summary) Regime close deletes ALL open trades of ALL bots on coin+direction** (`28:672-817`, no model/strategy filter) → other bots' losses censored as neutral, correlated with regime changes → whitelist win rates biased upward (the money gate!). **Fix:** only `model='ROM1'`. `[DB]`
- [x] **P1.10 ✅(2026-07-10, T-2026-CU-9050-046: overview corrected — "trades itself", the mechanism `compute_rom1_trade_params` named, the consequence gating vs. execution statistics as its own paragraph) Spec drift: docs say "pure signal router", code builds its own trades** (`docs/REGIME_ORCHESTRATOR.md:18` vs `28:288-421`). ROM1 discards the original entry/SL/targets → gating statistics ≠ execution statistics. **Fix:** update docs to v6, document the risk.

### Data pipeline (integrity)
- [x] **P1.11 ✅(2026-07-09 T-028 verified against the code: the key is `(sym, tf, open_time)` in `1_data_ingestion.py:662`, the flusher iterates consistently; the 7d full-rewrite on catch-up was reduced back to a 24h overlap as a crutch) The WS buffer keyed by `(symbol, timeframe)` loses the final candle update** at every candle boundary (`1_data_ingestion.py:494-502`) → the stored "closed" candle is slightly wrong until the 12h REST catch-up. **Fix:** key `(sym, tf, open_time)` or flush-through on `k['x']`. `[DB]`
- [x] **P1.12 ✅ 2026-07-11 (T-2026-CU-9050-084) ✔(Step2: poc 149 distinct/5000 old rows, support 236; trendline_price per-row ok) Whole-window indicators (trendline/POC/HVN/S-R/Fib) are broadcast as constants onto every row** (`2_indicator_engine.py`) → look-ahead in the stored history; values depend on window length. **Fix (variant B, operator approval):** the 27 window-global columns are now only written onto the newest CLOSED candle (as-of-now), forming + older rows NULL (`_as_of_now_window_globals`). Hard-rule-5 collision resolved — written onto the closed reference row that the 5 S/R readers read anyway; strat_support_resistance/strat_main_channel/strat_5_percent/strat_fast_in_out hardened to `first_valid_index`, 12_ai_ats unchanged (reads iloc[-2] = reference). Golden refreshed (rule 9, 27 columns × 24 fixtures on non-head rows). Serving delta of the classic strats = 0. Follow-up risk: 9_ai_sr_bot without a floor guard → NaN for forming reads (XGB-native; R1 follow-up task + SRA retrain). Cleanup of existing rows = separate VPS job. `[DB]`
- [ ] **P1.13 ~(Step2: currently 0 ma_200=0 rows; risk remains for new listings) ✔(2026-07-10, T-2026-CU-9050-045 analysis: confirmed against the code and extended beyond MA_200 — `2_indicator_engine.py:412-431` also writes `.fillna(0)` onto `wma_21` [rows 0–19] and `donchian_upper/lower/mid_20` + `boll_upper/lower_20` [rows 0–18]; `rsi_14` gets `.fillna(50)`. For bot 24/25 this means `donchian_upper_20_dist_pct = (0-close)/close*100 = -100.0` — five of eleven price features hard-pinned in the first ~20 bars. Symmetric in bot and replay, so no skew, but junk on both sides. `kama_21` is the ONLY genuine NULL column [`calculate_kama` deliberately doesn't fill] — handled in replay by T-2026-CU-9050-045. Follow-up task: **T-2026-CU-9050-054**, before the next TD2/BB2/QM2 retrain) ✅(2026-07-10 code fix in T-2026-CU-9050-054: `2_indicator_engine.py` lets the warm-up rows of `wma_*` [`calculate_wma`], `rsi_*` [`calculate_rsi`], `boll_*_20` and `donchian_*` flow as NaN — exactly as `kama` already does, same REAL column type, so the NaN write path is proven in production — instead of inventing 0/50. Regression-guard golden deliberately refreshed [816 breaches, exclusively these warm-up head rows: golden 0/50 → fresh NaN, nothing else]. `ma_*` deliberately NOT touched: no active consumer [only legacy_trainers] and no price-distance feature — outside the code-verified P1.13 surface. OPEN — operator/Michi [C-Gate]: (1) a recompute of the indicator tables is what makes the fix effective live [today the engine only writes warm-up head rows on the first run of a new listing], (2) then a TD2/BB2/QM2 retrain on the shifted feature distribution, (3) ONLY at artefact rollout remove the `bfill` in `24_quasimodo_bot.py:126`/`25_smc_ml_sniper.py:220` — never isolated. Box stays open until recompute+rollout.) ⚠(2026-07-11 T-2026-CU-9050-060 post-merge refinement [3 reviewer runs, unanimously APPROVED, no rollback]: "exclusively warm-up head rows" was too narrow — [a] `calculate_rsi` also goes NaN on EVERY row BEYOND the warm-up when the price window is fully constant [illiquid coin, new-listing lead-in, trading halt] [`up=down=0` → `rs=0/0`]; decision: leave it + comment it in `calculate_rsi` — structurally inconsequential, since a frozen window produces 0 pivots and bots 24/25 `continue` before the ML path, `strat_*` NaN comparisons are False; WMA/BOLL/DONCHIAN don't have this case. [b] The deepest golden breach is `wma_200` row 198, not a "head". [c] 821 breaches per fixture, not 816 [5 RSI row-0 transitions]. [d] Golden fixtures are liquid [BTC/ETH/SOL/DOGE] and can never trigger the flat case. [e] All `rsi_*`/`wma_*` columns are affected, not just `rsi_14`/`wma_21`. [f] The F3 premise of the original review ["sklearn throws on NaN, the exception handler safely suppresses it"] was itself FALSIFIED during implementation: all three models are XGBClassifier, and XGBoost natively scores NaN as missing via untrained default branches [verified against the production pkl] — so NaN features could produce live signals from out-of-contract input. Code follow-up: the EPD legacy path in `10_pump_dump_detector.py` now imputes non-finite features per the legacy trainer's NULL contract [`legacy_trainers/zzz.py:7609-7617`: rsi→50, rest→0 — serving values identical to pre-P1.13 lifetime behaviour], bots 24/25 get their own trainers' 0-imputation [`qm_ml_trainer.py:321`/`smc_ml_trainer.py:328` fit+score on fillna(0)] before `predict_proba`; contracts pinned in `backtest/test_nan_feature_guards.py`.) 🛑(2026-08-01, T-2026-KYT-9050-006 — **step (2) has run and is answered, step (3) drops out: the retrain is NO-GO, there is nothing to roll out.** First the reason the evaluation was almost impossible: the post-Wilder rerun artefacts from 14.07. **no longer exist** — `tools/retrain_from_replay.py:423` and `smc_ml_trainer.py:376` write the same path in the shared `STAGING_DIR`, and a legacy run overwrote all four between 05:21 and 05:24 (pkl mtimes are AFTER their own `retrain_*_stats.json`; today's files carry `meta.trainer='smc_ml_trainer.py'`, `optimal_threshold=0.3`, no `calibrator_isotonic`, no `model_id`). The metrics survived and carry the verdict: **TD_1H anti-calibrated** (live-gate bucket 0.8–1.0 avg −2.28%, bucket 0.0–0.3 +4.04%, val −78.2 — `pick_threshold` has no deployability abort, td/bb were never migrated to `pick_threshold_safe`), **BB_1H gate a no-op** (98.6% of test events taken), **BB_4H filter-only** (test Σ −686), **TD_4H selection BELOW base rate** (59.2% vs 60.7%). The Wilder rewrite made the cohort worse — the 12.07. recommendation "promote TD2_4H" no longer holds. Live counter-check (`closed_ai_signals`, deduplicated, unleveraged staggered move, 03.–08.2026): `BB2_4H` — the only replay generation ever to go live — books **−1.57%/leg over 99 legs**, the legacy artefact next to it **+0.25%/leg over 3,076 legs**. **The TD/BB replay-retrain line is closed**, the live inventory stays. This also leaves the `bfill` in place (lines updated: `24_quasimodo_bot.py:140-141`, `25_smc_ml_sniper.py:311-312` — the ticket lines `:126`/`:220` are outdated). Countermeasure against the overwrite incident: `core/staging_guard.assert_no_foreign_overwrite` in all three trainers, `backtest/test_staging_guard.py`. **Box stays open** — no longer because of the retrain, but because of the unresolved replay↔live sign conflict on the direction axis (live carries LONG, in the 540-day replay LONG has p≈1.0 negative), which devalues replay PnL as a promotion criterion for this bot family. Details: `docs/T-2026-KYT-9050-006-td-bb-qm-retrain-verdict.md`.) `fillna(0)` on warm-up windows writes invented indicator values** (`2_indicator_engine.py:325,384-403`) → young coins: `MA_200=0` permanently → `close>MA_200` trivially true → false signals precisely on illiquid new listings. **Fix:** let NaN/NULL flow (as KAMA already does). `[DB]`

### Classic strategies
- [x] **P1.14 ✅(2026-07-04 implemented: `support*1.05` in both strats) The SHORT "headroom" check is a sign-flipped no-op** (`strat_fast_in_out.py:74`, `strat_5_percent.py:97`): `close > support*0.95` is almost always true → SHORT has no headroom guard. **Fix:** `close > support*1.05`. `[DB]`
- [x] **P1.15 ✅(2026-07-04 implemented: per-coin try/except around the strategy calls+write with conn.rollback()+ERROR log (coin/TF, strategy via exc_info) → continue; main() a broad except Exception with an ERROR log + 30s backoff instead of process death) One bad coin kills the entire detector process** (`3_detectors.py:186-233`, strategy calls unprotected, `main()` only catches `FileNotFoundError`) → half the coin list unscanned until coins.json is fixed. **Fix:** per-coin try/except + rollback, broad except with backoff in main().
- [x] **P1.16 ✅(2026-07-04 implemented: `check_cooldown`/`update_cooldown` with 12h per (coin, direction), module_tag='Volume Indicator'; update before the signal return) ⚠️(regression: module_tag 16 characters > `trade_cooldowns.module` varchar(10) → `StringDataRightTruncation` before the signal return, bot signal-dead 04.07.–09.07.; collateral: simultaneous Fast-In-And-Out signals of the same coin cycle were lost too (write_signal_atomic only runs after both strategies); fixed 2026-07-09 via module_tag='VolIndic' + commit=False + a length guard, T-2026-CU-9050-024) Volume Indicator has no cooldown** (`strat_volume_indicator.py:68-100`) → a spike up to 5 days old refires every 30 min (serial re-entry). **Fix:** `check_cooldown/update_cooldown` (12-24h) or dedupe on the spike timestamp. `[DB]`

### AI bots (look-ahead / skew / robustness)
- [x] **P1.17 ✅(2026-07-04 implemented: `df_current=df_features.iloc[-2:-1]`, current_price still comes live from iloc[-1]) MIS1 predicts on the running candle with stale partial indicators** (`11_ai_mis_bot.py:228,238-242`) → structurally distorted volume features on **every** prediction; tuned thresholds meaningless. ATS (`12:143-147`) gets it right with `-2`. **Fix:** `iloc[-2:-1]` resp. `open_time < date_trunc('hour', NOW())`. `[DB]`
- [x] **P1.18 ✘refuted(Step3: all 8 pkls have identical 67 feature_names_in_ — introspection) + ✅fix built anyway (2026-07-09 T-028 verified: `11_ai_mis_bot.py:245` selects by name `df_current[cfg["features"]]` instead of `.values`, the exception is logged instead of swallowed) MIS1: one feature set for all 8 models + `.values` disables sklearn's name validation** (`11:205-209,250,259`) → after a partial retrain the features get permuted, `except:pass` swallows the shape error. **Fix:** per-model `df[model.feature_names_in_]`, log the exception. ⚠️ The fix is in the repo, the deployed version still runs with `.values` — takes effect only at the next bot restart.
- [x] **P1.19 RUB1 predicts on forming-candle indicators** (`13_ai_rub_bot.py:90-97,117-131`) — LIMIT 1 = the open candle from ~2 min of data. **Fix:** closed-candle filter, `curr_close` from the same candle. `[DB]` (2026-07-04 implemented: `open_time < date_trunc('hour', NOW())` in the indicator + 90d query; `curr_close` from `ind['close']` of the same closed candle instead of from the 90d price array)
- [x] **P1.20 ✅(2026-07-04 batch 4: ATR features always emitted (NaN), per-trade try/except with a commit per trade + guarded rollback; NaN ATR vectors are deliberately shadow-logged ONLY, not posted — the model never saw NaN in training. Bonus: process_ai_trade now commits cooldown+outbox+ai_signals atomically → no double-post window anymore) SRA1: conditionally missing ATR features + no per-trade isolation** (`9_ai_sr_bot.py:135-143,268-305`) → 35 instead of 38 columns → predict throws → the whole iteration aborts + the rollback discards shadow inserts; repeats every 5 min for 60 min. **Fix:** always emit ATR features as NaN (XGB can handle NaN), per-trade try/except. `[DB]`
- [x] **P1.21 AIM1: indicator features + close from the running hour** (`15:391,423-431`, `open_time <= floor('h')`). **Fix:** `open_time < join_time`. `[DB]` (2026-07-04 implemented: both feature queries — indicators + close — switched from `open_time <=` to `open_time <`, so only the last closed hour candle feeds in; the `time > %s` history/candidate filters are event-based and remain untouched)
- [x] **P1.22 ATB1: ML features on a 3-min-old forming candle** (`14:228-233,613-625`, `row=df.iloc[-1]` not sliced) → `vol_ratio` ~1/20 of the training scale. **Fix:** features on `df_90d.iloc[:-1]`, `last_close` separate. `[DB]` (2026-07-04 implemented: `get_ml_prediction` slices off the forming candle via `df_raw.iloc[:-1]`, features on the last closed candle; the live entry price stays separate as `current_close_price`)
- [x] **P1.23 ATB1: an aborted transaction poisons the rest of the 538-coin scan** (`14:612-614,761-762`, no rollback in the per-coin except, not autocommit). **Fix:** `conn.rollback()` in the except or `autocommit=True`. (2026-07-04 implemented: a guarded `conn.rollback()` added in the per-coin except, consistent with the file's transactional pattern)
- [x] **P1.24 QM pivot detection on the forming candle without confirmation** (`24_quasimodo_bot.py:110-111` vs trainer `183`; scipy `argrelextrema` mode='clip' lets edge pivots through) → repaint, trainer skew. **Fix:** drop the forming candle + discard pivots with `index > len-1-PIVOT_WINDOW`. `[DB]` (2026-07-04 implemented: pivot search on `highs[:-1]/lows[:-1]` — the last forming candle removed; `peak_idx/trough_idx` filtered against `max_confirmed_idx = len(c_highs)-1-PIVOT_WINDOW`, edge pivots discarded. `current_price`/`feature_idx`/retest loop unchanged)
- [ ] **P1.25 TD sniper: trainer entry is hindsight (pivot close), live geometry completely different** (`25:203-241` vs `smc_ml_trainer.py:126-149`); thresholds hardcoded instead of loaded from the pkl. **Fix:** see P0.10, load thresholds from the pkl. `[DB]`
- [x] **P1.26 ✔CONFIRMED(2026-07-09 T-028: the old ✘ annotation was WRONG — Step 2 was misled by old rows) ✅(2026-07-09 implemented, T-2026-CU-9050-033: the mitigation scan now ends before the current candle — `range(fvg['index']+1, curr_idx)`; guard test `backtest/test_smc_fvg_dead_code.py` with a divergence canary against the old range) SMC 16's FVG entry is unreachable (dead code)** (`16_smc_forex_metals_bot.py:164,430,458`): `find_unmitigated_fvgs` scans `range(fvg['index']+1, len(df))` — **including** `curr_idx = len(df)-1` (`16:341`) — and marks BULLISH as mitigated at `low[j] <= fvg['top']`. The trigger (`16:430`) checks **exactly the same predicate** `curr_candle['low'] <= fvg['top']`. An FVG that would satisfy the trigger is therefore no longer in `bull_fvgs` by construction → the entry can never fire (symmetric for BEARISH, `16:458`). P1.27's `iloc[:-1]` doesn't change this, it only shifts which candle `curr_idx` is. **Resolution of the proof contradiction:** the current code writes exclusively the literal `"SMC_FVG"` as the cooldown key (`16:431,459` — repo-wide the only two writers, verified). The 83 live-found rows are named `SMC_1H_FVG`/`SMC_4H_FVG`/… and come from an older, TF-prefixing code version; for the key this code actually writes, there are **0 rows**. The dead-code proof rests purely on the code and doesn't need the DB at all. **Fix:** `range(fvg['index']+1, len(df)-1)`.
- [x] **P1.27 ✅(2026-07-04 implemented: the forming candle dropped centrally via `df.iloc[:-1]` for both DB+yfinance before the signal logic; TF-dependent cooldown `COOLDOWN_HOURS` 1d→24h/1w→168h/default 12h) SMC 16 decides on the forming candle** (both data sources; `16:329-344,131-134`); a forming 1d/1w candle holds the condition for days → the 12h cooldown → refires all week. **Fix:** `iloc[:-1]`, cooldown ≥ candle duration.
- [ ] **P1.28 ABR1: 11/18 feature bug** → see **P0.12** (severity classification there).

### Trainer / backtest validity
- [x] **P1.29 ✅(2026-07-04 implemented, branch feat/t-2026-cu-9050-016: chronological 70/15/15 split along entry_time with a purge gap [ORDER_EXPIRY resp. 100 bars], threshold on validation, test set untouched + reported separately; artefacts now only written to staging_models with a meta block) Random `train_test_split` on time series + overlapping duplicates = contamination; "optimal threshold" chosen on the test set** (`qm_ml_trainer.py:261,290-325`, `smc_ml_trainer.py:262,276-314`) → optimistically biased operating point in the pkl. **Fix:** chronological split with a purge gap, threshold on the validation slice.
- [x] **P1.30 ✅(2026-07-04 implemented, branch feat/t-2026-cu-9050-016: an SL breach of a pending order = fill-then-stop loss instead of invalidation [trainer + qm_backtest]; TP evaluation only starts with the candle after the entry candle) QM fill logic deletes guaranteed losers + awards same-candle TP wins** (`qm_ml_trainer.py:121-179`, `qm_backtest.py:167-184`) → label distribution shifted upward. **Fix:** fill-then-stop conservatively, no TP win on the entry candle.
- [x] **P1.31 ✘/~(Step2: 0/529 coins without tables — the truncation trigger doesn't currently apply; the code bug remains) ✅(2026-07-04 implemented, branch feat/t-2026-cu-9050-016: try/finally conn.close() + a WARN log per skip in the qm/smc loaders; a hard SystemExit at <80% coin coverage in qm/smc/BT2 datagrepper; BT2 workers report ok/no_data/error separately) Silent exception + pool leak → trainers silently run on a truncated coin universe** (`qm_ml_trainer.py:67-94` etc.): a missing indicator table → `except: return empty` leaks the connection; after 8 leaks the pool is exhausted → **every** further coin is silently skipped → a model trained on 0-8 coins gets **saved over the production pkl**. **Fix:** `try/finally: conn.close()`, log skips, abort if too few coins. `[DB]`

### Core / infra / dashboard
- [x] **P1.32 ✅(2026-07-04 batch 4: `_returned` flag, close() idempotent; error path `putconn(close=True)`) `PooledConnection.close()` not idempotent** (`core/database.py:83-95`) → a double close poisons the pool / rolls back another thread's transaction (HOTFIX_README documents exactly this incident). **Fix:** `_returned` flag; in the error path `putconn(conn, close=True)`.
- [x] **P1.33 ✅(2026-07-04 batch 4: `putconn(close=True)` in the except + a SELECT-1 liveness check with a 30s TTL cache and retry in get_db_connection) Pool slot leak when `rollback()` throws on a dead connection** (`core/database.py:86-95`) → after a DB restart the pool is permanently exhausted per process → bot "healthy", produces nothing. **Fix:** `putconn(conn, close=True)` in the except; a liveness check in `getconn`. `[DB]`
- [x] **P1.34 ~(Step2: max_connections=200) ✅(2026-07-04 batch 4: KYTHERA_DB_POOL_MIN/MAX env-overridable, defaults behaviour-identical 2/8 — caution: psycopg2's minconn is the idle cache, too small a MIN causes reconnect churn; lowering it is an ops decision via env) Fleet connection budget** (`core/database.py:22-23`): 27 × maxconn 8 + process-pool workers > 216 vs a default `max_connections=100`; a crash-restart burst → "too many clients" → a restart storm. **Fix:** MIN 0-1/MAX 2-3 env-overridable, or pgBouncer. `[DB]`
- [x] **P1.35 ✅(2026-07-04 implemented, branch feat/t-2026-cu-9050-016: splitext target name `{root}_v2.json` + refuse if target==source OR the target already exists) `core/update_model.py:35` overwrites `.pkl`/`.joblib` in place and destroys the original** (`replace(".model", ...)` is a no-op for `*_model.pkl`). **Fix:** an `splitext` name + refuse if `new==old`.
- [x] **P1.36 ✅(2026-07-04 batch 4: fail-closed deny-all + encoding=utf-8; consumer verified — !open/!ping are cleanly refused, get_target_channel reports a missing mapping) The Telegram permission system falls back to OPEN on a config load error** (`core/bot_utils.py:12-18`, `{"*":["*"]}`; `open()` without `encoding="utf-8"` → cp1252 crash on emoji). **Fix:** fail-closed (deny-all) + alert, utf-8.
- [x] **P1.37 ✅(2026-07-10 T-029) The watchdog backoff `time.sleep()` freezes the monitor loop** (`main_watchdog.py:299-303`, up to 900s) → during that time no other restarts/park/dashboard checks; after the sleep, `start_process` runs without an `is_parked` recheck. **Fix:** a per-process `not_before` timestamp instead of sleep.
  - 2026-07-10 T-029: a `_restart_not_before` dict instead of `time.sleep()`. The loop keeps turning and only skips the affected bot. Because the park check therefore reruns every 10s cycle, a park **during** the backoff window still keeps the bot down — the second part of the finding falls out of the same restructuring, not from an extra guard. A dashboard restart beats the backoff (explicit operator action). Backoff schedule unchanged (0/15/60/300/900s), pinned by a test.
  - **Refactor with touch context:** the per-process body of the monitor loop now lives in `supervise_process(p_info, current_time)`; every `continue` became a `return` (equivalent for a loop body). Only this way is the deadline testable without driving `main()`.
  - **Evidence, honestly:** `backtest/test_watchdog_backoff.py` (new, DB-free, 6/6) are regression guards on the new behaviour, **not** witnesses of the old bug — on the pre-fix state they *error* (the function didn't exist), they don't fail. The old bug sits unmistakably in `main_watchdog.py:443-447` (pre-fix). To keep it from coming back, the fixture patches `time.sleep` with a mock that throws: any future blocking wait in the supervision path turns the suite red.
  - 2026-07-09 T-028 proven against the code, still OPEN: `main_watchdog.py:443-446` calls `time.sleep(delay)` in the crash branch of the single monitor loop; `_crash_history` (`:263`) computes the delay (max. 900s, `:276`), but `grep not_before` returns 0 hits.
- [ ] **P1.38 ~(sub-aspect "process list drifts" ✅ 2026-07-11, T-2026-CU-9050-091: done via `core/fleet.py` as the single source — see R2(a). Sub-aspect "CSRF" ✅ 2026-08-02, T-2026-KYT-9050-056: an origin check on state-changing methods + a host allowlist against DNS rebinding in `core/dashboard_security.py` — the plain same-origin comparison the finding proposed would NOT have caught rebinding, because the attacker's `Host` and `Origin` match. The two other individual fixes remain OPEN: log-streaming handle, `/api/status` psutil sweeps) Dashboard process list drifts + CSRF + log streaming blocks Windows rotation + `/api/status` 25 psutil sweeps/tab/6s.** `dashboard.py:31-62,290-317,244-256,104-179` — see R2 (the list) and the four individual fixes: CSRF origin check, log streaming with re-open instead of an open handle, one `process_iter` sweep + `cpu_percent(interval=None)` + server cache.

### Market intelligence
- [x] **P1.39 ✅(2026-07-10 T-029) Pump/dump timestamp fix incomplete** (`10_pump_dump_detector.py:522-529,552-558`): volume explosion + ML features still index-based → after a restart, false "VOLUME EXPLOSION" alerts + skewed ML features. **Fix:** route via `_find_bucket_before/range`.
  - 2026-07-10 T-029: both sections switched to `_find_bucket_before`/`_find_bucket_range`; the flat `prices`/`volumes_10s` lists are gone with no replacement (every consumer now pulls its window from `data` and filters `v10s_valid` itself). Additionally found: `volumes_10s` was **filtered** on `v10s_valid`, `prices` was not — so `volumes_10s[-18:]` and `prices[-18:]` already pointed to different points in time with a single invalid bucket. **All** bucket lookups of the function anchor on `bucket_anchor` (the timestamp of the newest bucket), not on the wall clock `now` — including the three pre-existing ones of the price-spike path, otherwise sibling lookups of the same function would sit on two different time bases. Reason (found in the z-code-reviewer pass, MEDIUM): the bucket timestamps are floored onto the 10s grid, `now` is the call time, and the detector iterates ~530 coins after a REST round trip — the offset δ = `now` − `latest_ts` lies in [0,60) (capped from above by the staleness check) and grows with batch position. Measured against `now`, the 60s window silently shrank to 6, then 5 buckets from δ>5s: `buy_pres`/`volat` described ~50s, while `p_chg_60s` (tolerance 20) kept measuring real 60s — three features meant to describe the same span didn't. `WINDOW_EDGE_GUARD = 5` (< half the bucket spacing) now only absorbs parse noise at the anchor and applies symmetrically to `_find_bucket_before` **and** `_find_bucket_range`. Deliberately NOT switched (wall-clock semantics): the staleness check, the two alert cooldowns, `pump_dump_events.spike_time`. If the bucket from 60s ago is missing, the tick is **skipped** instead of writing an invented `0` as a feature into the model.
  - **Measurement (proof, not claim):** in the test's gap scenario, the old index calculation reported `p_chg_60s = +100.0` (it reached across a 10-minute hole to a bucket at half the price), the time-based variant reported the true `0.0`. Exactly these values also ended up in `pump_dump_events`.
  - **⚠ Retrain coupling (operator decision 2026-07-09, Michi):** `vol_ratio`/`p_chg_60s`/`buy_pres`/`volat` are model inputs **and** are logged into `pump_dump_events` this way, which `tools/epd2_build_dataset.py` trains on. The deployed EPD2 artefact was fitted on the old definition — until the retrain rollout, serving runs against a slightly shifted distribution. On gap-free ticks old and new are identical (proven by control tests), the drift only affects gap ticks. Follow-up task: **T-2026-CU-9050-035** (EPD2 retrain on the new feature definitions). `[DB]`
  - **2026-08-01 T-2026-KYT-9050-004 — retrain measured, NOT executable (calendar), reopen 2026-11-09.** Report `docs/T-2026-KYT-9050-004-epd-retrain-feasibility.md`, numbers `staging_models/replay/epd4_feasibility.{json,md}`. Cut point proven on the data set (the hourly `pump_dump_events` count drops on the restart hour 2026-07-10 17:00Z from 56–170/h to 10–33/h — a gate, not a feature effect: the revived hourly warmup removes the single-sample denominator events). The post-cut data set is clean (4698/4712 written, 0.3% loss, 4327 labelled), but only **22.0 days** long: `chrono_split` gives val/test each the 15% band = 3.3 days, the 7-day purge gap (= label horizon) eats it entirely ⇒ `1664/0/0` resp. `1364/0/0`. The hard upper bound is **`ticker_10s` from 2026-07-07 11:19Z** (entry source since T-035), not the cut point — the Feb–July data set is no longer reproducible with today's builder; retention 365 days, the window grows. **The drift itself is smaller than ordinary market drift:** KS per feature (14 days before/after) sits within the null band of neighbouring 14-day window pairs for all four inputs (0.036–0.174 against a null-band median of 0.058–0.080). **The deployed model holds up:** `epd3_model_LONG.pkl` out-of-sample on post-cut events AUC 0.586, calibration monotonic (38.3 → 66.7% TP1); SHORT 0.537 ⇒ bot 10 unchanged. Tag **EPD4** reserved (EPD1/2/3 taken), deliberately NOT yet registered in `core/shadow_gate` (dead config without an artefact; occupancy pinned in `backtest/test_retrain_model_id.py`, gate default is LIVE). Code: `--model-id` on `retrain_from_replay`/`retrain_pump` (tag + filename prefix together, rule 6) and the degenerate split now reports the actual numbers instead of just "skipped". `[DB]`
  - 2026-07-09 T-028 proven against the code, still OPEN: the bucket helpers (`_find_bucket_before` `:273`, `_find_bucket_range` `:311`) exist and are only used in the price-spike part (`:408,428,446`). Left index-based: volume explosion (`:528` `volumes_10s[-18:]`, `:532` `sum(volumes_10s[-360:])/360`) and the ML features (`:557` `prices[-7:]`, `:563` `data[-30]` as "5 min ago").
  - **2026-07-10 T-035 — addendum, two latent regressions from exactly this fix.** P1.39 was merged at measurement time, but **not effective**: bot 10 had run uninterrupted since the fleet start 2026-07-08 00:32 and kept holding the old module code (the log banner "ML model loaded" is an *hourly* cache reload, not a startup — its cadence drifts monotonically without a reset). Both findings would therefore only have become live at the next restart. Measured against 421 350 real anchors from the live `1minute.json`: the bucket cadence is **bimodal** — median 10s, but p90 = 70s, only 62.7% of gaps ≤ 15s (the detector polls ~530 symbols per REST round trip). (a) `WINDOW_EDGE_GUARD = 5` resolved `p_chg_60s` for only **61.3%** of ticks; the remaining 38.7% returned early and were never scored. (b) More severe: the constant `360` migrated from `len(volumes_10s) >= 360` (warmup over the *whole* 1440-item deque, practically always true) to `len(hour_vols) >= 360` — a density requirement of one bucket per 10s over a full hour. Real density: ~193/h. The gate held for **0 of 421 350** anchors, the volume-explosion alert would never fire again. **Fix (T-035):** `_find_bucket_nearest` picks the bucket with the *real* closest distance to the target within a band and returns it; `p_chg_60s`/`p_chg_3m` are normalised to a rate per 60s/180s (identical on a dense grid, the 60/dt scaling has a median of 1.00). The hourly warmup now gates on **covered time span** + a sample floor instead of bucket count. Coverage `p_chg_60s`: 61.3% → **97.7%**. No switch to `tolerance=20`: crediting an 80s-old bucket as "60s" would be a watered-down recurrence of exactly the bug P1.39 removed.
- [x] **P1.40 ✅(2026-07-04 implemented: CREATE once in main(), insert gated on the retention complement vol_ratio>=3.0 AND |Δp60s|>=1.5; thresholds centralised in core/config.py) `pump_dump_events`: unconditional CREATE+INSERT per symbol per 10s tick** (`10:569-578`) → ~108 stmt/s, ~4.6M rows/day (rsi/tsi columns never populated). **Fix:** CREATE once, sample/batch the insert. `[DB]`
- [x] **P1.41 ✅(2026-07-09 T-029, PR #23) Shadow inserts into `ml_predictions_master` without a cooldown** (`10:625-635`) → up to 8640 rows/day/symbol, counted by the market tracker as an "opened signal". **Fix:** a per-symbol shadow cooldown; consumers filter `posted=TRUE`. `[DB]`
  - 2026-07-09 T-029: instead of a new per-symbol cooldown, reused the existing `core.signal_post.log_prediction()` — it already dedupes 4h per module/coin/direction and is the path bots 30-33 use (operator decision 2026-07-09). `last_alert_time` is **deliberately not** set in the shadow branch: the timer also gates real signals, a reset here would suppress live EPD2 trades of the same coin for 900s. Caller-commit contract preserved. The consumer side was already correct (`15_ai_master_bot.py:134` filters `posted = true`); the market tracker was the only reader without a filter and, since P1.44, no longer reads the log for opens at all. **Known, not fixed here:** `log_prediction` dedupes against `NOW()` (PG local time) on UTC rows — the window shifts, but still throttles. Belongs in the R3/TZ cluster (P2.1-P2.6), not a point fix. Verification: `backtest/test_shadow_prediction_cooldown.py` (new, DB-free, 4/4).
  - 2026-07-09 T-028 proven against the code, still OPEN — and the cause is more precise than noted: the 900s gate (`10:583`) exists, but the shadow branch (`10:651-661`, band `0.25 ≤ p < 0.60`) **does not update `last_alert_time`**; the only timer reset sits at `:750` in the live-trade branch. A coin that permanently only predicts in the shadow band therefore never throttles itself and inserts on every qualifying 10s tick.
- [x] **P1.42 ✔✔(Step2: 49/529 symbols; the logger has written zero files since 18.04.) (2026-07-04 implemented: 3-connection sharding at ≤180 streams each with per-connection reconnect; 09-W2 fixed along with it — backoff reset after a successful connect) Whale logger: 538 aggTrade streams on one futures WS connection** (`19:334-336`, fapi cap ~200/conn) → ~340 symbols silently not delivered. **Fix:** shard across 3 connections. `[DB]` (whale files vs coins.json)
- [x] **P1.43 ✅(2026-07-09 T-029, PR #18) Market tracker: pool leak on a query error + missing rollback** (`23:395-429,749-831`) → 1 leak/hour → after ~8h all tracker jobs are dead until a restart. **Fix:** `try/finally close`, `rollback` before the fallback. `[DB]`
  - 2026-07-09 T-029 implemented as `with get_db_connection() as conn:` at both hard leak sites — `PooledConnection.__exit__` calls `close()`, which does `rollback()` + `putconn()`, i.e. exactly the `try/finally` semantics of the ledger fix, in the form of the five clean call sites. The third site (`_regime_conn`) got a real `try/finally`, because it spans all bots and can be `None`. `rollback()` added before the `ai_signals` fallback. **Also found and fixed along with it:** `_get_regime_fit_label` swallowed its exception without a `rollback`, even though the caller shares ONE connection across ~25 bots — the first failed lookup degraded the regime-fit column of **all following** bots to `---`. Verification: `backtest/test_market_tracker_conn.py` (new, DB-free, 7/7; the 4 bug tests demonstrably fail on the pre-fix state). Takes effect at the next regular restart, no deploy.
  - 2026-07-09 T-028 proven against the code, still OPEN — PR #13/#15 didn't touch this spot. Two hard leak sites: `23_market_tracker.py:418` (`job_signal_summary`) and `:805` (`job_per_bot_performance`) take the connection bare and call `conn.close()` at `:470` resp. `:904` **in the try body right before the `except`** — on a query error the `PooledConnection` is never returned. A third, marginal site: `_regime_conn` (`:1343/:1383`). The five remaining call sites use `with get_db_connection()` and are clean. The missing `rollback` is also confirmed: `:892-894` catches the `ai_signals` JOIN error and fires the fallback query in the same aborted transaction.
- [x] **P1.44 ✅(2026-07-09 T-029, PR #23) Market tracker "opened" counts double-count AI trades + count shadow predictions** (`23:399-425`) → distorts every per-bot statistic (the decision basis). **Fix:** a `posted=TRUE` filter, opens only from `ai_signals`+`closed_ai_signals`. `[DB]`
  - 2026-07-09 T-029 implemented as described in the ledger fix: AI-side opens now come from `ai_signals` ∪ `closed_ai_signals` (per-signal, disjoint — the monitors DELETE from `ai_signals` on close and INSERT into `closed_ai_signals`, exactly the shape of the classic side). `ml_predictions_master` is therefore no longer an opens source at all; the `posted = TRUE` filter now sits in the `created_at` JOIN, so a shadow row can never supply an opening timestamp. **Rejected alternative** (`ml_predictions_master WHERE posted=TRUE` as the source, operator decision 2026-07-09): the log is **deduplicated**, not complete — `log_prediction()` and bots 11/24 skip the insert for the same module/coin/direction within 4h, so a legitimate re-post in that window would have no row and opens would be **under**counted. Both posts now share a `_load_open_ai_signals()` helper — exactly the drift between the summary and per-bot post that enabled the finding in the first place. **Live semantics change deliberately:** with 1 open + 1 closed AI signal in the window, "opened" now reports 2 instead of 3, and a shadow prediction doesn't show up at all anymore. Closed counts, win rate and Kelly stay untouched (`df_all_closed` continues to draw only from the `closed_*` tables). Verification: `backtest/test_market_tracker_opened.py` (new, DB-free, 7/7; the core test demonstrably fails on the pre-fix state with 3L instead of 2L, i.e. it measures the double-count rather than erroring).
  - 2026-07-09 T-028 proven against the code, still OPEN and **orthogonal to the 023/025 dedup work**: that DISTINCT-ON dedup acts exclusively on the *closed* tables and collapses re-close artefacts; it neither touches the open source nor introduces a `posted` filter. `get_o_stats` (`:615`) counts from `df_all_created` (`:573`), a concatenation of four sources — including `ml_predictions_master` (`:424`) **without a `posted` filter** (shadow rows count as opens) and, in parallel, `closed_ai_signals` (`:455`), whereby a posted, closed AI signal lands in "opened" twice. Repo-wide there is not a single `WHERE posted` in the file (the four `posted` hits are the timestamp column resp. `posted ASC` in the dedup order).
- [x] **P1.47 ✅(2026-07-10 T-2026-CU-9050-062) ATB1 writes `posted=False` even on the live-trade path** (`14_ai_atb_bot.py:720-723`, new 2026-07-09 T-029; found in the z-code-reviewer pass on P1.44). The insert into `ml_predictions_master` sits right before the `ai_signals` insert of the same live signal, but hard-sets `False` — unlike ATS1/RUB1/MIS1/SRA1, which write `posted=True` on their live branch. Consequence since P1.44: the `created_at` JOIN (`m.posted = TRUE`) never matches ATB1 rows anymore, open ATB1 positions permanently fall back to `NOW()` and appear forever fresh in the opened buckets. **Effect is display-only** — Kelly/WR pull `created_at` from `closed_ai_signals.open_time`, not from the JOIN. **Defused, not harmless:** ATB1 is parked (OPUS-HANDOFF §3), so it writes no new rows; only old rows in `ai_signals` are affected. **The fix belongs in bot 14** (live branch to `posted=True`, the pattern from 12/13), NOT in the tracker filter. Do before unparking. `[DB]`
  - 2026-07-10 T-062 implemented: the `posted` value now comes from `_atb1_posted_flag(ml_prob, threshold)` — exactly `True` when the prediction triggers the live trade (`ml_prob >= threshold`, the same predicate as `send_signal`). Shadow predictions (`0.25 ≤ ml_prob < threshold`) stay `False`. Extracted as a pure function, because `run_trendline_detector` as a whole isn't drivable — this makes the boundary testable (it's `threshold`, NOT the 0.25 shadow gate). Verification: `backtest/test_atb1_posted_flag.py` (new, DB-free, 5/5). Evidence honestly stated: the tests check the new helper — on the pre-fix state it's missing, so they error (`AttributeError`), they don't directly measure the insert bug (`run_trendline_detector` not drivable). The value is the forward guard on the boundary (`threshold`, not 0.25) and the `bool()` wrapper (numpy input). Takes effect at the next restart; the bot stays parked.
  - Renumbering history (2026-07-10 from P1.46 to P1.47. This entry merged first, but only has one immutable anchor (commit `1113095`, which calls it P1.46); the Sniper finding (T-036) has three (CHANGELOG.md, docs/CANDLE_CALL_SITES.md, commit title `97fc12d`) — so this one yields. Commit `1113095` still says P1.46 and means this entry.)
- [x] **P1.45 Post paths discard the available artefact `model_id` → a latent hard-rule-6 violation at the next retrain** ✅ 2026-07-09 (T-2026-CU-9050-030, PR #24 — MIS/RUB/QM now pull the tag from the artefact meta, one guard test each; **the EPD2/SRA2 side finding stays open**, see last sub-point) (new 2026-07-09, T-028; error-class sweep after T-2026-CU-9050-026/PR #16). The Sniper bug was **not an isolated case of the class**, just the only one already actively posting wrong (its tag was *derived* from `f"{strategy_code}_{tf}"` and could structurally never hit the retrain `model_id`). Currently **no** other bot posts wrong — but three paths discard the `model_id` and post under a source-code constant that today only *coincidentally* matches the artefact generation:
  - `11_ai_mis_bot.py` — loads the MIS2 artefacts (`:88`), reads `model`/`optimal_threshold`/`features`, **never** `art["meta"]["model_id"]`; the tag is the constant `MODEL_GENERATION = "MIS2"` (`:49`) → `f"{MODEL_GENERATION}-{best_horizon}"` (`:271`), written into `ai_signals`, `ml_predictions_master` and the cooldowns. Aggravating: the artefact **filenames** are also hardcoded (`f"mis2_model_{key}.pkl"`, `:52`). A MIS3 retrain that follows the filename convention makes `meta.model_id` the only generation marker — exactly what the bot ignores. **Highest value, because MIS is the load-bearing live AI family.**
  - `13_ai_rub_bot.py` — `load_artifact(...)` (`:74`) correctly computes `RUB2_SHORT["tag"]` from the meta, but the bot uses `module_tag = "RUB2"` (`:219`). Caution when fixing: the LONG side deliberately reuses `"RUB2"` for a legacy model that doesn't even load the artefact (`:216`) — the fix is direction-dependent, not a drop-in.
  - `24_quasimodo_bot.py` — a structural twin of the Sniper: tag `f"QM_{tf.upper()}"` (`:77`), the artefact loaded but `model_id` never read (`:63`). Today `qm_ml_trainer.py` doesn't write a `model_id`; but the orchestrator was already made QM2-capable in `ff8e01e` (a `QM\d*_` pattern in `BOT_IDENTIFICATION_PATTERNS`, `28_signal_orchestrator.py:72`). As soon as a QM2 follows the established convention, the bot will silently post it as `QM_1H`. **Close preemptively, before QM2 is built.**
  - Correct pattern to copy: `18_ai_abr1_bot.py:520` (`MODELS[dir].get('model_id', MODEL_ID)`) and the research bots 30–33 via `core/model_artifacts.load_artifact` → `ARTIFACT["tag"]`.
  - **Fix:** wire MIS + RUB to the ABR pattern, QM preemptively; one guard test each analogous to `backtest/test_sniper_tag.py`. **Mandatory before rolling out MIS3/RUB3/QM2** (B7/C2 touch exactly these bots).
  - **Done 2026-07-09 (T-2026-CU-9050-030, one bot per commit):** MIS pulls the generation per horizon from `meta.model_id` (filenames stay generation-free slots — operator decision; missing `model_id` → `logger.error` + fallback). RUB is direction-dependent: SHORT from `RUB2_SHORT["tag"]`, LONG keeps the named constant `RUB_LONG_TAG` (a legacy model without meta). QM reads `meta.model_id` with a derived fallback, and `send_cornix_signal` takes `module_tag` as a mandatory keyword (Sniper pattern: fail loudly instead of silently wrong). Guards: `backtest/test_mis_tag.py`, `test_rub_tag.py`, `test_quasimodo_tag.py` (mutation-tested). **Additionally, a review finding:** the tag is also the dedupe key — on a generation switch, an open old position would no longer have blocked the refire (a second live position). Hence transitional dedup like the Sniper: MIS/QM extend the active-trade check to `model IN (neu, legacy)`, RUB (which has **no** active-trade check) additionally checks the cooldown against the old tag. `legacy_tag` is in each case the tag the bot would have posted before the fix → today a no-op. **No live semantics change** — all three tags stay identical (MIS2-*, RUB2, QM_1H). This unblocks B7/C2.
  - Side finding (its own fix moment, not a current bug — deliberately not part of T-030) **✅ done 2026-07-10 (T-2026-CU-9050-042)**: `retrain_from_replay.py:723` (EPD2) and `retrain_sra2.py:281` (SRA2) write artefacts **with `model_id`**, while the live bots `10_pump_dump_detector` (`:60`) and `9_ai_sr_bot` (`:38-42`) load **raw** models and read no meta.
    - **Finding correction against the code (trap 13):** SRA2 does **not** emit a dict artefact, but native XGB JSON + `_meta.json`/`_calib.pkl` — the ABR2 format. The format mismatch only existed for EPD; for SRA "only" the meta read was missing.
    - **Done:** `core/model_artifacts.load_artifact_json` lifts the JSON sidecar loader, previously baked into `18_ai_abr1_bot._load_model_contract`, into the shared contract. Both bots pull tag **and** threshold from the meta; without a meta sidecar the named legacy contract remains.
    - **The real effort was under the tag.** EPD loaded a raw **3-class** model with a positional 10-feature array, the EPD2 artefact is **binary** with 16 named features incl. funding — serving now via `funding_features_asof` as-of the event (parity with `epd2_build_dataset.py:231`), the legacy branch unchanged. SRA built features under the same names with a **different formula** than its trainer (`pct_ema9` = /close instead of /ema9; `macd_dif_pct`/`macd_dea_pct`/`atr_pct` were missing entirely) — the builder now lives once in `core/sra_features.py`, imported by both bot and trainer (X-R1).
    - Transitional dedup per bot wherever it really locks: EPD shadow-log dedupe (`log_prediction(legacy_tag=…)`), SRA master-log duplicate check + cooldown. Guards: `backtest/test_model_artifacts.py`, `test_sra_tag.py`, `test_epd_tag.py`, all mutation-tested.
    - **No live semantics change** — without a deployed artefact both bots keep running on their legacy contract. The EPD2/SRA2 rollout is thereby unblocked (remains an operator decision, C-Gate).
    - **New findings ✅ done 2026-07-10 (T-2026-CU-9050-055, see P1.49):** neither EPD nor SRA had an active-trade check against `ai_signals` — the same class as P1.48/RUB.
    - **Performance risk funding load ✅ closed 2026-07-10 (T-2026-CU-9050-055, see P1.49).** Correction of the original annotation: the 900s timer does in fact run BEFORE the ML path — the repeat case is the shadow band, because the timer is only set on the live-trade branch. And a cache is indeed a drop-in, as long as the key is bound to the settlement instead of a TTL.

- [x] **P1.46 ✅(2026-07-10 T-2026-CU-9050-036) SMC Sniper: pivots on the forming candle** (new 2026-07-10 from the C1 call-site inventory, T-2026-CU-9050-034 → `docs/CANDLE_CALL_SITES.md` §3). `25_smc_ml_sniper.py:230-231` let `argrelextrema` run over the **full** frame — the last row is the running candle. Its high/low moves, so the pivot set repainted **within** the running candle: the drives of a three-drive and the level of a breaker block changed after the signal had already gone out. The sister bots had had the drop for a while (`24:138` from P1.24, `16:334` from P1.27, `21:126`); 25 was the only gap — and the only one of the four that posts live on the money path. **Fix:** `c_highs, c_lows = highs[:-1], lows[:-1]` before `argrelextrema`, the pattern from `24_quasimodo_bot.py:138`. Pivot indices stay aligned to the full arrays, all `len(df)-1`/`len(df)-2` offsets (the BB feature row, the breakout window) are untouched. `current_price = closes[-1]` stays the **live** price (CMP entry + BB level proximity, not an analytical input) — the R1 end state `include_forming=False` for the price side hangs on operator question 4/6 of the inventory and on migration block 4. Verified DB-free: `backtest/test_sniper_forming.py` (4/4), signal-rate delta over the guard fixtures −4.5% (154 → 147 geometry triggers on 3,608 scan points). **Residual repaint deliberately left open:** `argrelextrema(mode='clip')` still allows unconfirmed pivots at the right edge (the edge filter `max_confirmed_idx` from P1.24). For 25 this isn't a drop-in — the TD freshness gate (`len(df) - p3 <= PIVOT_WINDOW + 2`) specifically looks for these edge pivots; a filter there is a strategy change, not a bugfix. Its own task.
  - **✅ Residual repaint done 2026-07-11 (T-2026-CU-9050-093, operator-approved strategy change).** A **shared** edge-pivot filter right after `argrelextrema` discards unconfirmed edge pivots for both consumers (the TD gate + `find_breaker_setup`): `peak_idx[peak_idx <= last_closed - PIVOT_WINDOW//2]` (analogous for `trough_idx`), `last_closed = len(df) - 2`. **Option B** was chosen (at least `PIVOT_WINDOW//2 = 5` confirming closed candles), not the full bot-24 filter (option A): A would have emptied out the TD freshness gate. Proven via the delta harness `tools/sniper_edge_pivot_delta.py` (current geometry, TD gate + T-089's `find_breaker_setup`, 4 coins × 1h/4h, 3,608 scan points): **BB unchanged (0.0%)** — a breaker requires a breakout + follow-through *after* the pivot and is therefore already structurally confirmed; the entire effect sits in **TD** (−40% LONG / −47% SHORT), **overall −5.9%** (221 → 208, purely subtractive, no new trigger). Option A would have cut TD by ~90% (overall −11.8%). The residual repaint window is thereby halved (≤ `PIVOT_WINDOW//2` instead of `PIVOT_WINDOW` candles), not zeroed — deliberately, for TD's fresh reversal entry. **Retrain coupling:** TD2/BB2 are fitted on the old pivot policy — until the retrain rollout, serving sees a slightly shifted TD pattern distribution (BB untouched). Verified DB-free: `backtest/test_sniper_edge_pivots.py` (7/7); `test_sniper_forming`/`test_sniper_retest_level`/`test_sniper_tag` unchanged and green.

- [x] **P1.48 `13_ai_rub_bot.py` had no active-trade check → a cooldown expiry could open a second live position on an open coin** ✅ 2026-07-10 (T-2026-CU-9050-043) [renumbered 2026-07-10 from P1.46 to P1.48 — three sessions had assigned the same number in parallel. P1.46 stayed with the Sniper (T-036), which has three immutable anchors; this entry had one (merge commit `4b27061`), which still says P1.46 and means this entry] (new 2026-07-10, a code-review finding from T-2026-CU-9050-030/PR #24 — pre-existing state, not a regression). RUB's only refire lock was the 4h cooldown (`:252`); the whole bot only ever wrote to `ai_signals` (INSERT `:376`). A cooldown is a **frequency** lock, not a position guard: a mean-reversion trade regularly outlives its 4h, after which the same coin was allowed to fire again in the same direction — Cornix opened a **second full position with its own SL** next to the first. The sibling bots all had the guard (`11_ai_mis_bot.py:318`, `24_quasimodo_bot.py`, `25_smc_ml_sniper.py:116`); RUB was the only one without, which is why the T-030 dedup there had to fall back onto the cooldown. **Done:** `SELECT 1 FROM ai_signals WHERE symbol/direction/model IN (module_tag, RUB_LEGACY_TAG)` before the ML prediction, skip on a hit; the bound `module_tag` is the same direction-dependent tag the post path writes (LONG `RUB_LONG_TAG`, SHORT `RUB2_SHORT["tag"]`), the legacy tag carries the transitional dedup across the RUB3 generation switch. The cooldown stays untouched alongside it (MIS pattern: both locks in parallel). Guard: `backtest/test_rub_tag.py` (two new tests, mutation-tested). **Live semantics deliberately change here** — exactly the second position drops out; the first position and any signal on a free coin remain unchanged.

- [x] **P1.49 EPD and SRA had no active-trade check → a second live position on an open coin; plus a funding load per 10s tick in EPD** ✅ 2026-07-10 (T-2026-CU-9050-055, Michi operator order) (new 2026-07-10, follow-up findings from T-2026-CU-9050-042/PR #39 — pre-existing state, not a regression). This closes the P1.48 class fleet-wide: **every** posting AI bot now has the position guard.
  - **EPD** (`10_pump_dump_detector.py`): only ever wrote to `ai_signals`. The only refire lock was `pd_state["last_alert_time"]` (900s, `:736`) — an **in-memory** timer and a frequency lock. An EPD trade regularly outlives 15 minutes. **Done:** `SELECT 1 FROM ai_signals WHERE symbol/direction/model IN (module_tag, EPD_LEGACY_TAG)` **after** the prediction (the direction only emerges from the argmax — operator decision 2026-07-10: `symbol+direction` like the siblings, no direction-agnostic key, so the reversal stays allowed), **before** the shadow/post branch: the guard, as with MIS/RUB, also suppresses the shadow row.
  - **SRA** (`9_ai_sr_bot.py`): had the 4h cooldown (frequency) and the `trade_id` duplicate check — the latter only protects against the same setup, not against a **new** setup on a coin with an open trade. **Done:** the same check, direction-dependent, **before** the indicator fetch and `predict_proba` (the direction is already fixed from `active_trades_master`, so the guard also saves work).
  - Both bind the post tag **plus** the old tag (transitional dedup across the EPD3/SRA2 generation switch; a no-op as long as the tags match). The cooldown and the 900s timer stay untouched alongside it.
  - **Funding load (performance):** the 6 funding columns are **model input** — the load cannot move behind the trade decision ("load only on trades" isn't buildable). It got repeated because the 900s timer locks before the ML path, but is **only set on the live-trade branch**: a coin in the shadow band passes the gate on every tick. **Done:** `core/funding_features.funding_features_cached`, a key derived from the **data**: an entry is valid until the settlement that could next change the result (the next `funding_time` in the history, otherwise the last one + an estimated interval). The first draft cached per hour and was refuted in adversarial review with two counterexamples — settlements aren't guaranteed to land on the hour (`backfill_funding_rates.py` writes millisecond-precise), and the 120s ingestion guard was a bet on an SLA. The data-bound key covers both and self-corrects on ingestion delay. A re-review then found a further real staleness bug: the interval estimate behind the last row now takes the **minimum** of the most recent gaps, not the median — estimating too short costs one round trip, estimating too long delivers wrong numbers across a real settlement. A **naive time TTL** remains a parity break — it can span a settlement.
  - Guards: `backtest/test_funding_cache.py` (first pins the invariant itself, above `MIN_HISTORY`, so two empty dicts aren't compared), `test_epd_tag.py`, `test_sra_tag.py` — all mutation-tested (incl. a time TTL instead of an hour key, a day- instead of hour-key, a missing lag guard).
  - **Live semantics deliberately change** at exactly one point per bot: a signal on a coin with an already open trade in the same direction drops out. First position, free coin, opposite direction and the funding value remain unchanged.

---

## P2 — Medium (selection of the most impactful; details in the agent reports)

### Timezone (work through after R3)

**R3 policy laid down (2026-07-09, T-2026-CU-9050-032):** `core/time.py` is the only time source, ruff `DTZ` keeps new naive datetimes out.

**Pool flip merged (2026-08-01, T-2026-KYT-9050-005):** `-c timezone=UTC` sits together with the P2.3 writer and the six removed compensations in ONE changeset — rolled out individually, either would have created new drift. Effective from the next fleet restart. The training history is two-domained from this instant on; the reading now hinges on exactly one constant (`core.time.R3_CUTOVER_UTC`), and which of the two worlds applies is the **open operator decision** with numbers in `docs/UTC_POLICY.md` §6. Inventory (measured live 2026-08-01) and prepared `timestamptz` DDL in the same place.

The offset is **+2/+3h** (VPS TZ `Europe/Bucharest`, measured 2026-07-05), not the "1-2h CEST" of the entries below.

- [ ] **P2.1 The classic strats' cooldown circuit breaker compares naive local time against UTC `posted`** (`strat_fast_in_out.py:42-48`, `strat_5_percent.py:25-29`) → in CEST the 3h window only covers 1-2h. `[DB]` — **not** healed by the pool flip (a Python-side comparison); `strategies/` is ruff-excluded, DTZ doesn't reach there.
- [ ] **P2.2 ✔resolved(Step2: the timestamptz variant won live) ⚠️(second drift dimension 2026-07-09, T-2026-CU-9050-024: live `module` is `varchar(10)` — verified via information_schema —, the repo DDLs say VARCHAR(50)/TEXT; cost Volume Indicator 5 days of signals + a Mayank refire. Gated code-side via a `COOLDOWN_MODULE_MAX_LEN` guard in core/market_utils; the clean fix = `ALTER COLUMN module TYPE VARCHAR(50)` → operator escalation) 🔸(2026-08-02, T-2026-KYT-9050-009: **the width dimension is done** — live, `module` is today `character varying(50)` and `last_posted_at` `timestamp with time zone`, re-measured via `information_schema`; the ALTER has run [approved per a predecessor task 2026-07-12, not observed by the measuring session] and was nowhere recorded in the CHANGELOG → caught up now. **The checkbox still stays open, though:** the core of the drift still sits in the code — `26_regime_detector.py:242` creates `module TEXT` + `TIMESTAMP WITHOUT TIME ZONE`, while 11/24/25/30 say `VARCHAR(50)` + `WITH TIME ZONE`. On a fresh DB, bootstrap order still decides. Addendum: the `COOLDOWN_MODULE_MAX_LEN = 10` comment was justified with "live is varchar(10)" — the premise is wrong since the ALTER, comment corrected, the value deliberately NOT raised [changes cooldown keys on the money path → operator decision]) `trade_cooldowns` DDL drift ×4 (WITH vs WITHOUT TZ)** — bootstrap order decides cooldown semantics (`26:194-200` vs `11/24/25`). `[DB]`
  - **The checkbox stays deliberately open** (2026-07-09 T-028): the TZ dimension is resolved, the **width dimension is not** — live `varchar(10)`, the repo DDLs say VARCHAR(50) (`24:442`, `25:590`, `11:479`, `30/31/32`) resp. TEXT (`26:197-199`). `CREATE TABLE IF NOT EXISTS` never widens, the drift cements itself. **A clue to the origin (not proof):** the only place in the repo that even has `module VARCHAR(10)` is a **commented-out** legacy DDL block in `legacy_trainers/zzz.py:13443` (`# module VARCHAR(10) NOT NULL, -- 'SRA1','TBA1','AIM1'`). This is consistent with the live width and suggests the table once originated from this form — the executing DDL isn't it and doesn't exist anywhere in the repo. The code guard only defuses the symptom path; the point is only done with the live `ALTER` (operator decision, VPS).
- [x] **P2.3 ✅(2026-08-01, T-2026-KYT-9050-005: `write_signal_atomic` now stamps `utc_now_naive()` instead of naive server-local time — ONE call feeds both columns, `time` and `posted`. Together with `-c timezone=UTC`, `NOW()` casts into the same domain, so the DB-side windows of `33_ai_fif1_bot` (1h/24h) and the classic cooldown line up again. Effective from the fleet restart; the `noqa: DTZ005` is gone.) `active_trades_master.time/posted` written naive-local** (`3_detectors.py:54,117`), compared against `NOW()`/aware-UTC → the 60-min window becomes 2h+. `[DB]`
- [ ] **P2.4 `closed_ai_signals.close_time`: `NOW()` vs Python UTC mixed** across 3 writers (`8:247`, `6:201`, `28:729`). `[DB]`
- [x] **P2.5 ✅(2026-08-01, T-2026-KYT-9050-005: resolved by the pool flip, with no code change in `market_utils` — `NOW()` casts naive-UTC into the column under `timezone=UTC`, exactly the domain the reader assumes. Addendum on the evidence: `trade_cooldowns.last_posted_at` is live `timestamptz` anyway [measured read-only 2026-08-01] — the column itself was never the problem, only the cast of the repo DDL variant WITHOUT TZ. Effective from the restart.) `update_cooldown` writes `NOW()` (session TZ) into a naive column, the reader reads it as UTC** (`core/market_utils.py:104-135`) — R3 resolves this centrally. `[DB]`
- [x] **P2.6 ✅(2026-08-01, T-2026-KYT-9050-005: resolved by the pool flip — the aware-UTC value is now cast into the naive column under a UTC session, so it lands as naive UTC instead of a Bucharest wall clock. Exactly the "fix ineffective if session TZ ≠ UTC" caveat of the entry is thereby gone. Effective from the restart; the column's history stays two-domained until the UTC_POLICY §6 decision.) `5_trade_monitor.posted`: tz-aware UTC in a `TIMESTAMP WITHOUT TIME ZONE`** (`5:22-25,59`) → fix ineffective if session TZ ≠ UTC. `[DB]`

### Monitors / data integrity
- [x] **P2.7 ✅(2026-07-04 implemented: an in-memory `last_checked` watermark per trade ID in both monitors; candles since the watermark scored forward candle by candle `ORDER BY open_time ASC`, first-touch/trailing logic unchanged; the first run per trade = the newest candle, no retroactive scoring; the watermark is lost on a process restart — a DB column as a second stage remains open) ✔quantified(Step7: 17.8% missed TP1; overall monitor↔replay agreement only 63.4%) Monitors only inspect the newest 5m candle** (`5:152-176`, `8:82-110`, `ORDER BY open_time DESC LIMIT 1`) → SL/TP hits during downtime permanently missed. **Fix:** `last_checked_open_time`, scan forward. `[DB]`
- [x] **P2.8 ✅(2026-07-04 implemented: `DELETE FROM active ... RETURNING` first, insert into the closed table only for a returned row, both in one transaction; a double close is logged instead of double-written — in 5 and 8) Unguarded close race** (`5:31-65`, `8:245-276`) → duplicate rows in closed tables, lost updates. **Fix:** `DELETE ... RETURNING` first, insert only on a hit. `[DB]`
- [x] **P2.9 ✅(2026-07-04 implemented: an `sl>0` guard; corrupt trades with no SL AND no target are now closed neutral at entry instead of becoming zombies) A SHORT trade with `sl=0` is immediately "stopped out" at price 0 (+100% PnL)** (`5:216-225`). **Fix:** guard `sl>0`. `[DB]`
- [x] **P2.10 ✅(2026-07-04 implemented: claim `pending→sending` via `FOR UPDATE SKIP LOCKED` + an immediate commit — the lock only covers the status transition; a second consumer skips instead of double-sending) No `FOR UPDATE SKIP LOCKED`/singleton on the outbox** (`4:183-194`) → two consumers would send everything twice.
- [x] **P2.11 ✔quantified(Step7: 800 discarded messages, all 'timed out', 212 in the FIFO trading channel) ✅(2026-07-04 implemented: the last retry without parse_mode; final discard logged as ERROR with channel_id + error text; failed status set cleanly — the alert comes from the existing core/health_monitor, not duplicated) Permanently failing messages silently discarded after 3 attempts, no alert** (`4:22,124-144`) → a lost Cornix signal is invisible. **Fix:** retry without parse_mode, operator alert. `[DB]`

### Indicator engine / ingestion / housekeeping
- [ ] **P2.12 ✔(Step2: stored==ewm(span), Δ to Wilder avg 4.8 points) ✅(2026-07-11 code fix in T-2026-CU-9050-095: `calculate_rsi` [`2_indicator_engine.py:354-369`] switched from `ewm(span=period)` [α=2/(period+1) ≈ Wilder-7.5] to true Wilder `ewm(alpha=1/period, adjust=False)` — consistent with `calculate_smma`/ATR in the same file. Pinned against an independent, hand-rolled Wilder RMA recursion [`backtest/test_wilder_rsi.py`, bit-exact ≤1e-9]; the old span formula fails as a regression. NaN warmup [P1.13/T-054] and the flat case [0/0→NaN, T-060] structurally preserved [only α changes, not the NaN handling]. **Golden deliberately refreshed** [rule 9]: exactly 120 `numeric_drift` breaches, **exclusively RSI_6/9/12/14/24** across all 24 fixtures, **zero non-RSI columns** — the change is fully encapsulated, no engine column derives from rsi. **Signal-rate delta** [`tools/wilder_rsi_signal_delta.py`, 24 fixtures, 12,468 bars]: the 70/30 extremes drop the most — RUB2 overbought rsi_14>70 −4.84 pp [9.28→4.44%, ~−52% rel.], oversold rsi_14<30 −5.61 pp [12.28→6.67%, ~−46%]; that's the measured "70/30 fires too often". SHORT gates strat_5_percent −2.61 pp / fast_in_out −2.40 pp; the central 55-75 LONG bands stay ~flat [±0.7 pp]. **Retrain coupling** [C-Gate, VPS]: `rsi_14` is a direct model input of TD2/BB2/QM2 [`ABSOLUTE_INDICATORS`], rsi_6/9/12/14/24 of MIS2, rsi_9/14/24 of SRA2, rsi_6/14 of AIM2, rsi_14 of the research bots; derived features [mis `rsi_*_delta_1`, `rsi_14_above_50`, `rsi_14_cross_above_30`, TD/BB three-drive RSI pivot monotonicity] shift along with it. The deployed artefacts saw the old span RSI → retrain on the shifted distribution BEFORE trusting it. **Mixed-history risk** [a C-Gate consequence, like the R3 pool flip]: from deploy onward the DB history carries two RSI domains [old span pre-deploy, Wilder post-deploy]; until a VPS recompute, trainers read mixed values. **Caution:** the T-061 tool `recompute_indicators.py` **only nulls warmup heads, it does NOT recompute values** — deliberately, because a full recompute isn't position-stable [written incrementally over months by older engine builds; even with the same formula, up to 48% mid-band drift on rsi_14]. A Wilder recompute of the rsi_* columns is therefore a **real full recompute** [position-unstable, decouples training from serving] — NOT a trivial T-061 extension, but a bigger operator decision. Sequencing [P1.13 doctrine, "never isolated"]: (1) code fix + golden refresh [done], (2) VPS recompute rsi_* → single-domain [operator, heavier than T-061], (3) TD2/BB2/QM2 + MIS2/SRA2/AIM2/research retrain, (4) ONLY then retune the 55/70/75 thresholds. Box stays OPEN until recompute+retrain.) 🔧(2026-07-12, T-2026-CU-9050-099: **tool for step (2) built** — a `--rsi-rewrite` mode in `tools/recompute_indicators.py`: a full Wilder rewrite of ONLY the rsi_* columns [5 columns], a tail guard, batched unnest UPDATEs, idempotent [cells ≤1e-3 points apart are skipped — a second run = no-op], its own resume state, an **engine parity self-check** [a witness series; a pre-T-095 checkout is hard-rejected, so it can never write the history back to span]. Read-only smoke [--sample 6]: 134,717 cells across 5 tables, avg delta 5.43 RSI points [max 27.2] — matches the Step-2 measurement of avg 4.8. A full run ≈224 min single-thread. Tests `backtest/test_rsi_rewrite_plan.py` [9, DB-free]. **Execute stays a C-Gate**; then step (3): the T-061 retrain from 2026-07-12 00:13 still ran on ewm history → rerun runner v2, THEN promotion.) 📏(2026-08-01, T-2026-KYT-9050-008 — **step (2) HAS run, and the mixed-history risk is now measured instead of predicted.** Two independent pieces of evidence for the execute: `logs/rsi_rewrite_execute_20260712.log` — `mode=RSI-REWRITE EXECUTE | tables=4197 | cols=5`, completion "3831 tables, 88,426,142 cells written" on 12.07. 11:26–21:02; and a recalculation by this session, in which today's stored `rsi_14` over 8 coins × July matches the Wilder recursion bit-exactly, not the old span formula. The sentence above, "execute stays a C-Gate", is thereby **outdated** — the ledger line was stale, not the run. **Measured impact** of the domain jump on a production model: RUB2-SHORT, 229 matched (symbol, candle) signals against the 14.07. replay — live-vs-replay mean|Δp| 0.009–0.015 for candles from 07 to 11.07 (the bot read span, the replay reads the overwritten Wilder value), and from 12.07. onward **exact** agreement on 92–100% of rows. The risk is therefore real but small: ≈1 percentage point of probability, and it ends on the rewrite date. Step (3) remains open; details `docs/T-2026-KYT-9050-008-rub2-replay-skew.md`.) 🛑(2026-08-01, T-2026-KYT-9050-006 — **step (3) is done for TD2/BB2/QM2 and reads NO-GO.** The retrain on the Wilder distribution ran on 14.07.; it **worsened** the cohort compared to the pre-Wilder run (TD_4H from "test +185.8, promote" to a selection below the base rate; TD_1H stays anti-calibrated; BB_4H stays filter-only). No candidate is deployable, nothing gets promoted, so step (4) — retuning the 55/70/75 thresholds — drops out as well. The artefacts themselves were overwritten on 14.07. by a legacy trainer run (a filename collision in the shared `STAGING_DIR`, now blocked by `core/staging_guard`); only the metrics survived. For MIS2/SRA2/AIM2/research, step (3) remains untouched and open. Details: `docs/T-2026-KYT-9050-006-td-bb-qm-retrain-verdict.md`.) RSI is not a Wilder RSI** (`2_indicator_engine.py:336-337`, `ewm(span)` instead of `alpha=1/period`) → RSI_14 ≈ Wilder-7-8; thresholds 70/30 fire too often (ATR in the same file is correct). **Fix:** a deliberate migration. `[DB]`
- [x] **P2.13 ✅(2026-07-11, T-2026-CU-9050-092: `find_contaminating_gap` in the DB worker `process_coin_task` — after the P1.12/P1.13 rework the path no longer lives at :551-561. If the engine finds a candle gap in the lookback that feeds a row to be persisted, the symbol/TF is skipped this cycle + logged, instead of computing a rolling window over the hole. Bounded: only gaps within MAX_INDICATOR_LOOKBACK=200 bars before the first written row count — an old, rolled-out gap does NOT freeze the coin, `MAX(open_time)` keeps advancing. Self-heals via the nightly 6_housekeeping gap filler. A pure helper, tested DB-free [`backtest/test_gap_continuity.py`, 8 cases incl. boundary]; regression guard 24/24 green WITHOUT a refresh [golden fixtures are gap-free, the check lives in the DB worker, not in calculate_indicators_optimized]) The indicator engine rolls its window over candle gaps with no continuity check** (`2:551-561`). **Fix:** a gap check over the lookback, skip+log on a violation. `[DB]`
- [x] **P2.14 ✅(2026-07-09 implemented: a RetryBudget max_attempts=8/deadline=300s per symbol×TF batch — only failed attempts count, successful pages paginate freely; 418 via core/http_retry.backoff_seconds never <120s, exponential, Retry-After only raises it; on an exhausted budget, use partial data, the next 12h run picks up at MAX(open_time); standalone backtest/test_http_retry.py) `fetch_ohlcv_batch` can loop forever; 418 ban handling hammers straight into the ban** (`1:94-120`) → one stuck symbol blocks every 12h catch-up. **Fix:** a max retry, 418 → ≥120s exponential.
- [x] **P2.15 ✅(2026-07-11, T-2026-CU-9050-092, full version [Michi operator decision]: both processes periodically re-read coins.json [`core.market_utils.load_coins` — a pure re-read, no third writer, respects P2.16; 6_housekeeping refreshes the file daily at 03:00 UTC] and pick up new symbols ADDITIVELY. `chart_data_service` [COIN_REFRESH_INTERVAL_SEC=300]: its own WS worker per batch. `1_data_ingestion` [900s]: tables + a one-time 730d catch-up + its own WS worker, coordinated across the 3 concurrent loops [catch-up/freshness/WS fleet] via a shared `tracked` set snapshotted by the loops each cycle → new coins also get a 12h catch-up + freshness. Conservative: removed coins are never torn down [teardown=restart], a torn/empty read=no-op [`compute_new_symbols`]. Sharding [P1.42] preserved: new streams in ≤180-item chunks with staggering, running worker IDs. The atomic single writer from P2.16 (core/coins.py) makes the re-read tear-free. Tested DB-free [`backtest/test_coin_refresh.py`]) ✔(2026-08-02, T-2026-KYT-9050-009, **real listing verified**: GRVTUSDT was recognised by `1_data_ingestion` on 2026-08-01 06:01:38 [`logs/DATA_INGESTION.log`: "🆕 1 neue Coins in coins.json"] — the last restart before that was 2026-07-30 07:25, the next only at 19:33, so the additive path applied to a RUNNING fleet. `candles` carries GRVTUSDT/1h from 2026-07-31 15:00 to now [catch-up + ongoing writes]. The empty `GRVTUSDT_1h` is not a defect but the C-Gate state [legacy tables end 2026-07-16, see T-2026-KYT-9050-002]. **Named remainder:** the first `ticker_10s` row only appeared after the restart — the writer is `10_pump_dump_detector.py`, which still freezes its coin list at start and was never part of the P2.15 scope.) Coin list frozen at process start** (`1:579-591`, `chart_data_service.py:356-374`) → newly listed coins get no data until a restart. `[DB]`
- [x] **P2.16 ✅(2026-07-11 implemented: a new core writer `core/coins.py` — ONE filter definition (quoteAsset=USDT + status=TRADING + PERPETUAL) and an atomic write via a tmp file + os.replace; `1_data_ingestion.update_trading_pairs` + `6_housekeeping.update_coins_json` now both call `refresh_coins_json` → filter drift structurally ruled out, no more torn reads. Annotation correction: the filter divergence had already been closed after the ETHU incident (CHANGELOG 2026-07-06, both already PERPETUAL) — only the duplicate definition + the non-atomic write remained open. Tests: backtest/test_coins_writer.py) Two processes write `coins.json` with different filters, non-atomically** (`1:31-56` incl. quarterlies vs `6:24-47` PERPETUAL). **Fix:** one writer via core, tmp+os.replace. `[DB]`
- [x] **P2.17 ✅(2026-07-11 implemented: shape guard `core.coins.looks_like_usdt_perp` (`<BASE>USDT`, uppercase-alnum) as an additional condition in both delisted selections (classic + AI) — symbols without a Binance USDT perp shape (XAUUSD, ETHBTC, forex) are no longer force-closed; together with the single writer from P2.16 the "universe wobbles" root cause also drops out. Tests: backtest/test_delisted_cleanup.py) Delisted cleanup closes trades on any symbol not in coins.json** (`6:128,186`) incl. metals/forex/ETHBTC → nightly false closes. **Fix:** restrict to the Binance perp shape. `[DB]`
- [x] **P2.18 ✅(2026-07-09 implemented: 429 → a Retry-After-aware budgeted backoff (5 attempts/120s); 418 → a process-wide ban window ≥120s, all further gap-fill calls return None immediately until it expires instead of hammering on; a MinIntervalThrottle of 0.25s/request desyncs the burst across ~9k tables; core/http_retry.py shared with P2.14) Housekeeping REST without 429/418 handling** (`6:508-522`) → after the gap-filler fix, a burst → an 418 IP ban also hits trading endpoints. **Fix:** mirror the Retry-After/backoff.
- [x] **P2.19 ✅(2026-07-04 implemented: WMA via sliding_window_view ~10x, KAMA er/sc vectorised ~20x, one executor/cycle, WARN >25min; regression guard 24/24 green, KAMA bit-identical) The indicator cycle risks the 30-min budget** (`2:585-626`, WMA via a Python lambda `apply`, KAMA a Python loop, a fresh process pool per TF) → an overrun silently skips the next trigger. **Fix:** vectorise WMA (`np.convolve`/`sliding_window_view`), one executor/cycle, WARN at >25min. `[DB]`
- [x] **P2.20 ✅(2026-07-11, T-2026-CU-9050-092: `_consume_with_watchdog` replaces `async for msg in ws` with `asyncio.wait_for(ws.recv(), CHART_WS_MESSAGE_WATCHDOG_SEC=120)` — a silent connection [handshake ok, 0 messages] returns after 120s of silence → reconnect instead of hanging forever; ConnectionClosed propagates as before. The snapshot dump [~12MB json.dump + os.replace] via `asyncio.to_thread` [`_write_snapshot_to_disk`] off the event loop; only the consistent buffer snapshot is briefly copied under the lock [candle lists never mutated in place → the thread reads stably]. Interval 60s→300s. Tested DB-free [`backtest/test_chart_service_watchdog.py`: the watchdog fires on silence, a message lands in the buffer, an atomic dump, to_thread offload]) chart_data_service: no message watchdog + gap handling** (`chart_data_service.py:184-232`); a 12MB JSON snapshot written synchronously on the event loop every 60s (`102-119`). **Fix:** `asyncio.wait_for(recv,120)`, `to_thread` for the dump, a 300s interval.

### Orchestrator/regime (remainder)
- [ ] **P2.21 TZ mix in the cooldown/outbox window** (`28:521-536`) — R3. `[DB]`
- [ ] **P2.22 The analyzer attributes on RAW `regime_history`, gating uses debounced `regime_current`** (`27:227-236` vs `regime_logic.py:263-422`) → wrong regime attribution around transitions; backfill look-ahead. `[DB]`
- [ ] **P2.23 ✔(Step2: TRANSITION share 44.5%, 2.9 raw changes/day) The "unreliable" heuristic counts RAW flaps** (`28:177-191`) → the system can permanently get stuck in the coarse overall fallback (a core feature disabled). `[DB]`
- [x] **P2.24 ✅(2026-07-11, T-2026-CU-9050-082: `run_startup_reconciliation` runs once before the main loop and checks all OPEN trades in `orchestrator_open_trades` against the CURRENT whitelist — independent of a remembered regime, which the in-memory tracker `_last_known_regime` doesn't know at boot anyway. The close body is extracted into `_close_non_whitelisted_open_trades` and shared with the regime-change handler: ROM1-only (`force_close_trades_for_regime_change` model='ROM1', P1.9), the existing close path, no new mechanism. Startup additionally seeds the baseline, so the first periodic check doesn't fire on the boot state, and only posts on an actual close/trail (no status spam at every watchdog restart). Fail-safe: its own short-lived connection, an error never blocks the loop start. DB-free tests: `backtest/test_orchestrator_startup_check.py` (6). Live verification (a real restart follow-up) = a VPS session follow-up.) A regime change during orchestrator downtime is never caught up** (`28:76-77,949-952`, in-memory state). **Fix:** at start, check all OPEN trades against the current whitelist. `[DB]`
- [x] **P2.25 ✔✔(Step2: raw-name rows computed_at=19.04. — exactly the ones the orchestrator reads) 🔸(2026-07-10, T-2026-CU-9050-046: the read side defused — `get_whitelist_decision` discards cells >48h and falls back to the overall fallback. The write side remains open: the cleanup in 27 still doesn't clear the rows) ✅(2026-07-11, T-2026-CU-9050-082: write side closed — a new `cleanup_stale_whitelist_rows` runs in `run_analysis` right next to `cleanup_stale_performance_rows`, before `compute_whitelist`. Two disjoint DELETE criteria (OR-combined, `build_whitelist_cleanup_query`): (A) raw-name keys `pretty_name(bot_name) <> bot_name` — exactly the 19.04. rows, provably orphaned (the analyzer writes normalised, the orchestrator reads normalised since P0.4), deleted regardless of age like in the perf table; (B) `computed_at < now - WHITELIST_RETENTION_DAYS` (14d) — normalised rows of retired bots. 14d deliberately conservative: the read gate (48h) has devalued everything older anyway, active bots get rewritten in the same run. Fail-safe: scan/delete errors swallowed (0 returned, no commit), never crashes the hourly run. DB-free tests: `backtest/test_whitelist_cleanup.py` (6). Live verification (Step-2 query 9) = a VPS session follow-up.) ✔(2026-08-02, T-2026-KYT-9050-009, query 9 run live: `bot_regime_whitelist` carries 1590 rows, **all** with the same `computed_at` from the last hourly run [2026-08-01 21:11], 53 bot names × 30 regime cells, **0** raw-name keys and **0** rows older than the read gate. Both DELETE criteria are therefore effective; nothing stale left in gating.) Stale `bot_regime_whitelist` rows never cleaned up** (`27:747-793` only the perf table) → the orchestrator gates (via P0.4 raw names) on month-old entries. **Fix:** extend the cleanup + a `computed_at` staleness gate. `[DB]`
- [x] **P2.26 ✅(2026-07-04 batch 4: an is_same_direction_open gate with a 72h age bound — a stuck OPEN row can no longer permanently exclude the coin from trading) No same-direction-open check** (`28:272-284`) → after the 4h cooldown, ROM1 stacks positions on the same coin. `[DB]`
- [x] **P2.27 ✔(Step2: p90=17.9%, max 65%; 20/133 >15%) ✅(2026-07-04 implemented: a 15% SL-distance cap in the ROM1 path) ROM1's SL has no distance cap** (`28:355-366`) → the next S/R zone is 30-50% away, past liquidation at 20x — R4.
- [x] **P2.28 ✅(2026-07-04 batch 4: window 60s→300s; novelty is carried by the id cursor from batch 2. Restart signal loss during start_delay stays by design — cursor init to MAX(id)) The 60s detection window + `start_delay=175`** (`28:35`) → every restart silently discards ≥3 min of signal flow. **Fix:** a 5-10 min window + a `stale_signal` log. `[DB]`

### Further AI/SMC/classic (selection)
- [x] **P2.29 ✅(2026-07-04 implemented: ORDER BY open_time ASC) `get_hvn_and_sr_levels` reads 95d with no `ORDER BY`** (`core/trade_utils.py:263-276`, used by SRA1/ATS1/RUB1 for SL/TP) → argrelextrema on possibly unsorted rows → phantom extrema as SL/TP prices. **Fix:** `ORDER BY open_time ASC` (one line). `[DB]`
- [x] **P2.30 ✅(2026-07-04 batch 4: process_ai_trade returns a bool, posted comes from the return value) SRA1 logs `posted=True` even when the cooldown suppressed the post** (`9:163-164,278-283`) → phantom posts in the performance evaluation. `[DB]`
- [x] **P2.31 ✔(Step2: targets_hit up to 21) ✅(2026-07-11 T-2026-CU-9050-083: the ai_signals insert of bots 9/11/12/13 capped to the published count — `json.dumps(targets[:n_show])`, n_show=3 resp. 5 as one source for both the Cornix loop AND the insert; the Cornix block byte-identical (rule 4). Pulled along on the same path: core/signal_post.post_ai_signal (bots 30-33), the same ai_signals→monitor-8 path. Monitor 8 untouched (scores len(stored)); existing data untouched (history = a VPS job). Guard backtest/test_published_targets.py, fails pre-fix) ⚠️✅(2026-08-02 T-2026-KYT-9050-012: **two emitters had been overlooked — and they were the two with the most volume.** The 083 fix hit `post_ai_signal` and bots 9/11/12/13; but `28_signal_orchestrator` (ROM1) and `15_ai_master_bot` (AIM2) write **their own** `INSERT INTO ai_signals` and bypass the shared path. ROM1 persists `t_cands[:20]` (`:525`) and posts `[:ROM1_PUBLISHED_TARGETS]`=3 (`:574`); AIM2 persists `json.dumps(targets)` (`:589`) and posts `targets[:3]` (`:544`). Measured over 30 days: ROM1 199 of 250 signals with the full 20 targets, 7,774 closed trades — the position model splits the stake across ~20 instead of 3 legs and **underestimates ROM1 by a factor of 1.41** (Σ 17,700 → 24,955%, avg 2.277 → 3.210%/trade; median 1.51 instead of 5.18%), AIM2 by a factor of 1.05 over 2,350 trades. Additionally **139 of 7,774** ROM1 trades had `targets_hit > 3`, i.e. credit for TPs Cornix never got. **Operator decision 2026-08-02: only correct the MEASUREMENT, the bots stay untouched** — trimming the persisted list would change monitor 8's scoring semantics for running trades (SL trailing, ALL-TARGETS close would see 3 instead of 20 stages). Implemented as one source in `core/realized_pnl.PUBLISHED_TARGET_COUNT` + `traded_targets()`; `weighted_move_pct`/`realized_pnl_pct`/`unlev_move` optionally take the model and cut **before** the leg count, `targets_hit` gets capped along with it. Byte-identical to before without `model` — no caller silently changes. Followed up: `23_market_tracker` (T-115 report), `tools/fleet_realized_audit`, `tools/whitelist_v2_realized_eval` (there the leg dict only carried `pretty_name`, not the raw tag → the lookup would have been a silent no-op). 13 tests in `backtest/test_traded_targets.py`. **Impact: all ROM1/AIM2 numbers in reports and roster decisions were too small** — retroactively also affects the T-007 verdict, but sign-neutral there, because both gate sides are equally affected.) Subscribers see TP1-3/1-5, the monitor scores up to 10-20 targets** (9/11/12/13) → live statistics ≠ Cornix reality. **Fix:** store exactly the published targets. `[DB]`
- [x] **P2.32 ✅(2026-07-04 batch 4: autocommit removed, signal writes atomic via the update_cooldown commit; a per-coin finally rollback prevents a NOW() freeze of the scan transaction — otherwise the outbox created_at would be backdated and the orchestrator staleness filter would have silently discarded MIS signals; a connection leak at both early returns fixed) MIS1 `autocommit=True`** (`11:190`) → outbox/ai_signals/master-log inserts not atomic. **Fix:** remove autocommit, one commit. `[DB]`
- [x] **P2.33 ✅(2026-07-04 batch 4: ranking by `prob - threshold`) MIS1's best-candidate comparison uses raw probabilities across differently calibrated models** (`11:252-271`) → a below-threshold candidate displaces an above-threshold signal. **Fix:** rank by `prob - threshold`.
- [x] **P2.34 ✅(2026-07-04 batch 4: `replace([inf,-inf],nan)` before fillna(0) at both feature sinks. ⚠ Known residual risk for the retrain (X-R2): inf→0 now encodes "volume spike from zero volume" the same as "no volume" — the trainer imputation must be identical when retraining, otherwise a train/serve skew on exactly the pump-precursor feature) MIS1's `fillna(0)` doesn't clean up `inf` from zero-volume divisions** (`11:131-133`). **Fix:** `replace([inf,-inf],nan)`.
- [x] **P2.35 AIM1 5-min window with no catch-up + context features count the candidate itself + the `conv_signal` dedup key collides** (`15:299,303-316,321-331`). Fixes for each: a 60-min window; exclude the candidate/AIM1; distinct signal_types. `[DB]` ✅(2026-07-11, T-2026-CU-9050-090, re-scoped against the AIM2 rebuild — AIM1 stays OFF/P0.13, fixes applied to the AIM2 carrier): **(a)** window 30→60 (the rebuild had already done 5→30 + a dedup table `master_ai_processed_signals`); catch-up is secured by the table, the window only bounds staleness. **(b)** context self-counting is already fixed on BOTH sides of the AIM2 code — `swarm_stats` (serving) and `aim2_build_dataset.swarm_stats` (trainer) both strictly filter `< ts`, AIM1/AIM2/TOPN are excluded from the stream → **no change, NO retrain coupling** (no model input feature changed, rule 7 not touched); the invariant is now pinned in `backtest/test_aim_context_features.py`. **(c)** the conv dedup was `(conv_signal, per-Tabelle id)` — but the id doesn't survive the active→closed migration (a new SERIAL id, same open `time`; `5_trade_monitor.close_trade`), hence (1) a double post of the closed form AND (2) the mentioned collision of unrelated rows. The brief's proposal "distinct signal_types" only fixes (2); the fix is therefore a table-agnostic identity key `conv_signal_identity(source,symbol,dir,time,entry)`→BIGINT (a diagnosis like `33_ai_fif1_bot.signal_key`), the AI side keeps the stable `ml_predictions_master.id`. No schema change (no live migration needed). Shadow-only until the gate flip. Tested DB-free; the full backtest suite green.
- [x] **P2.36 ATB1's "unknown" state-break trigger reactivated** (`14:52-56,660-673`) → a state loss = a mass event flood (the comment openly admits the bug). **Fix:** unknown = observe-only. `[DB]` ✅(2026-07-11, T-2026-CU-9050-086: event classification extracted into a pure `classify_trendline_event`, `prev_relation=="unknown"` now returns `None` — the first cycle after a state loss only rebuilds the relation, emits nothing; the caller keeps writing `prev_relation`, real transitions fire from the following cycle onward. Head comment 52-58 corrected: persistence alone isn't enough. Tested DB-free in `backtest/test_atb_unknown_state.py`, incl. a differential pre-fix assertion. Only takes effect on unparking.)
- [x] **P2.37 ATB1's main loop only catches KeyboardInterrupt** (`14:593,764`) → any scan exception kills the process + leaks the connection. **Fix:** try/finally close, a broad except+backoff. ✅(2026-07-11, T-2026-CU-9050-086: the `run_trendline_detector` scan wrapped in `try/finally` → `conn.close()` + `save_trendline_state()` run even on a mid-scan abort; the `main()` loop got an `except Exception` + 30s backoff added (the 3_detectors P1.15 pattern). P1.23 per-coin rollback + P1.22 forming slice untouched. Only takes effect on unparking.)
- [ ] **P2.38 ✔✔defused(Step2: LONG 67%/SHORT 59% WR; Step3: LabelEncoder+meta.json+num_class=3 — index 0 correct) ABR1: SUCCESS_CLASS_IDX + SHORT label semantics unverifiable** (`18:41-54`, the trainer nowhere on the machine). **Fix:** persist LabelEncoder.classes_ in the model + a load assert. `[DB]` (the outcome-vs-confidence join is the decisive test)
- [x] **P2.39 ✅(2026-07-11 T-2026-CU-9050-089) Break-and-retest (25) only checks `peak_idx[-2]`** (`25_smc_ml_sniper.py` BB section) → the wrong level on a fresh retest; feature timing breakout vs. retest. `[DB]`
  - **Level choice:** a new pure helper `find_breaker_setup(pivot_indices, level_arr, extreme_arr, closes, n_closed, current_price, direction)` replaces the blind `p_res = peak_idx[-2]` / `p_sup = trough_idx[-2]`. It walks the pivots **from new to old** and takes the first one whose level (a) lies within the retest band (±0.5%) around the current price, (b) was broken by a close within the last `MAX_BB_AGE`=20 closed candles, and (c) then ran ≥0.3% follow-through above/below the level. If the fresh retest belonged to `peak_idx[-1]` or an older swing, the old code scored the wrong level (or missed the signal entirely) — the divergence is pinned as a canary in the test.
  - **Freshness/follow-through/band** exactly as in the old code (0.5% band, 20-candle age, 0.3% follow-through) — only the level **selection** changes, plus `n_closed=len(df)-1` further excludes the forming candle (range semantics identical).
  - **Feature timing:** deliberately kept anchored at the **retest bar** (last closed candle, `len(df)-2`) and documented — the breakout is earlier (`breakout_idx`), the ML features describe the retest's decision moment (the BB model's pattern anchor; TD analogously anchors on p3). Changing the anchor bar would be a strategy redesign (the P1.46 residual class) and does **not** belong in this fix.
  - **The P1.46 fix (25) not regressed:** the forming-candle drop (`c_highs/c_lows = highs[:-1], lows[:-1]`) + the T-026 tag logic untouched; `backtest/test_sniper_forming.py` + `test_sniper_tag.py` stay green.
  - **Live semantics delta (money path):** the bot now scores the actually retested level instead of a fixed one; it can therefore fire on `peak_idx[-1]`/older valid breakers that the old code missed — only behind the same freshness/follow-through gates. BB_1H stays parked on both sides.
  - Tested DB-free: `backtest/test_sniper_retest_level.py` (9/9 — LONG/SHORT level choice with a blind-[-2] divergence canary, stale-breakout, no-follow-through, out-of-band reject, forming exclusion, source wiring).
- [x] **P2.40 ✅(2026-07-11 T-088: threshold 75→95/85 — the extremes loop list `[95,85,75]`→`[95,85]`, extracted into the pure helper `classify_funding_extreme`; edge cases pinned in `backtest/test_funding_threshold.py` incl. "75 no longer fires". Operator decision Michi 2026-07-11. A deliberate signal-rate reduction, noted in the CHANGELOG; only affects the info channel `CH_MARKET_DATA`) The funding "extreme" threshold of 75% fires positive in the normal state** (`20:164-186`, baseline +0.01%). **Fix:** 95/85, magnitude or transitions. `[DB]`
- [x] **P2.41 ✅(2026-07-11 T-2026-CU-9050-081, PR #69) Market tracker: a full-history load per hour + all "async" jobs actually synchronous + a regime-fit query with no rollback + the chunker doesn't split an over-budget block** (`23:755-776,64-183,646-712,1180-1231`). `[DB]`
  - 2026-07-11 T-081 re-scoped against the current code (the ledger lines were as of 07-03). Four sub-findings, handled differently:
  - **(d) chunker doesn't split an over-budget block — FIXED (a real robustness bug).** `_build_chunks` (now `23:~1050`, previously nested in `job_per_bot_performance`) emitted a single block over the budget on its own as ONE >4096-character chunk. `send_telegram` only writes to `telegram_outbox`; the dispatcher (`4_telegram_bot`) silently discards an over-limit message → the entire post would have disappeared. **Fix:** an `_hard_split_block` fallback splits an over-budget block first on line, then as a last resort on character boundaries; `body_budget` is computed against the larger of the two headers, so a piece fits in both the first and follow-up chunk. Every emitted chunk is now guaranteed ≤4096. The three chunker helpers (`_group_bot_entries`/`_group_table_entries`/`_build_chunks`) were lifted from nested to module level for this (pure, no closure state) — needed to test them DB-free. Verification: `backtest/test_market_tracker_chunker.py` (new, DB-free, 13/13; the core test builds an over-budget block and proves all chunks stay under the limit and no character is lost). `test_market_tracker_conn.py` stays 7/7 (a helper move with no behaviour change).
  - **(c) regime-fit query with no rollback — ALREADY DONE via P1.43/T-029 (no-op).** `_get_regime_fit_label` (`23:789`) already rolls back its exception (`except: conn.rollback()`), `_regime_conn` (`23:1407`) is wrapped in try/finally and cleanly closed. Verified against the code — nothing left open. Regression guard: `test_market_tracker_conn.py::test_regime_fit_label_rolls_back_on_error`.
  - **(a) full-history load per hour — DELIBERATELY NOT optimised, documented as a known risk.** `job_per_bot_performance` deduplicates (DISTINCT ON) hourly over the ENTIRE `closed_*` tables with no time window. This is required: the "all" column + the all-time avg PnL + Kelly need the full history, and the survivor pick (earliest close = original outcome) must run over the whole table, otherwise a re-close artefact would bump a months-old trade back up as freshly closed. A time filter here would be a behaviour change to the statistics (an operator decision, not a silent fix) → documented as a risk in the code (`23:~900`). Measured cadence 07-09: ~439k raw AI rows collapse server-side to ~81.8k, so pandas receives the deduplicated set. Uncritical today, grows with the trade history.
  - **(b) async jobs effectively synchronous — DELIBERATELY NOT fixed, documented as a known risk.** All `job_*` are `async def`, but do blocking sync DB I/O; the `async` is cosmetic. Not a concurrency bug: the six jobs are staggered across distinct minute/second offsets (`main()`), they don't overlap in normal operation. A real async conversion (`asyncio.to_thread` per query) would let them run concurrently and pull several pool connections at once — pool max is 8/process, that would trade a harmless scheduling delay for a real starvation risk. That's a rewrite, outside the task scope. The real residual exposure (one hanging job stalls the others) + minimal future hardening (a per-job timeout) documented in the code (`23:~1676`).
- [x] **P2.42 ✅(2026-07-11 T-2026-CU-9050-085: (a) `_classify_latest_volume_spike` now iterates backwards → the newest spike wins instead of the oldest; (b) an i==0 spike is discarded instead of silently classified as a sell; (c) `_is_near_high_volume_node` bins prices to 0.1% levels before the groupby → the HVN gate now also fires on fine-tick coins, where the raw `groupby('close')` never accumulated. DB-free tests: `backtest/test_volume_indicator_spikes.py` (9, incl. pre-fix reference asserts). ⚠ Signal behaviour deliberately changes (ledger order); the `[DB]` live-effect measurement remains a VPS step) Empty-zone / volume-spike classification / HVN tick-size dependency** (`strat_volume_indicator.py:22-63`): the oldest spike wins, `i==0` always a sell, the HVN gate degenerates by tick size. **Fix:** iterate backwards, bin prices. `[DB]`
- [x] **P2.43 ✅(2026-07-04 implemented: `ema_21<ema_55` + REQUIRED_COLUMNS extended with ema_200/wma_21/wma_26 — the columns exist in the engine DDL) 5% SHORT uses `ema_12<ema_55` where LONG uses `ema_21>ema_55`** (`strat_5_percent.py:86`, probably a typo) + `REQUIRED_COLUMNS` doesn't cover `ema_200/wma_21/wma_26` (`11-14`).
- [x] **P2.44 ✅(2026-07-11 T-2026-CU-9050-085: `get_live_prices_batch` fetches all symbols in ONE `/fapi/v1/ticker/price` call per cycle instead of ~530 serial klines calls; the loop uses `price_map.get(symbol)` with a fallback to the old per-coin HTTP→DB path if the batch fails or a symbol is missing → no coin gets skipped. Volume strat: the 4 gates (spike/active/cooldown/HVN) are all side-effect-free AND reads → reordered cheap-before-expensive, the 90d×30m HVN read now runs LAST (only if a signal would otherwise be emittable), signal volume invariant. The P1.16 cooldown contract untouched (12h/'VolIndic'/commit=False via the detector — only the read-only check_cooldown was moved earlier). Test: `backtest/test_detector_batch_ticker.py` (4). The `[DB]` load measurement remains a VPS step) 538 serial Binance HTTP calls per detector cycle** (`3:199-201`) + the volume strat reads 90d×30m as its first gate (`strat_volume_indicator.py:14-19`). **Fix:** a batch `ticker/price`, reorder the guards. `[DB]`
- [x] **P2.45 ✅(2026-07-11 T-2026-CU-9050-089) SMC/Mayank weekend refire + missing FVG age limits + missing SL/RR checks** (`16_smc_forex_metals_bot.py`; `17_mayank_bot.py`). `[DB]`
  - **(a) Weekend/stale-candle gate** (16 + 17): forex/metals sit idle on weekends — the last closed candle freezes and holds the structure/FVG condition, while the 12h cooldown underneath expires → a refire on a stale candle. A new pure helper `is_stale_candle(open_time, tf, now)`: a signal may only fire if **fewer than two candle durations** have passed since the last candle's close (a two-candle tolerance against a single yfinance live lag; a weekend exceeds that many times over on intraday TFs). Gate as a `continue` in `run_smc_analysis`/`analyze_strategy`. **24/7 crypto (METALS: BTC/ETH/…) is never stale → live semantics unchanged there.** The rest deliberately left open (conservative, justified in the PR): a 1d/1w signal can still refire **once** across a weekend before the 2-duration threshold kicks in — the dominant regression was the intraday 12h refire.
  - **(b) FVG age limit** (16): `find_unmitigated_fvgs` got `max_age=FVG_MAX_AGE` (50 bars) — never-mitigated gaps were otherwise triggerable over the entire 300-candle history. Conservative (1h ≈ 2d, 4h ≈ 8d, 1d = 50d); older gaps count as stale.
  - **(c) SL/RR sanity** (17): a new pure helper `passes_sl_rr_guard(entry, sl, tp1, direction)` before the send in both branches — rejects stops further than **15%** from the entry (liquidation risk, the same cap as the ROM1 path P2.27) and setups whose next TP doesn't offer at least **0.5×** the risk as reward (a sanity floor, no normal pivot ladder trimmed). SL/TP are FVG-independent per scan (from `curr_low`/`curr_price`) → a fail blocks the scan (`break`).
  - **The P1.26/P1.27 fixes (16) not regressed:** the FVG dead-code range, forming drop and TF cooldown untouched; `backtest/test_smc_fvg_dead_code.py` stays green.
  - **Live semantics delta (money path):** only signals drop out — stale WE candles (a), ancient gaps (b) and geometrically implausible trades (c). First position, fresh candles and the 24/7 crypto path remain unchanged.
  - Tested DB-free: `backtest/test_smc_weekend_refire.py` (14/14 — stale boundary, TF scaling, an FVG-age divergence canary via the `max_age` parameter, SL/RR reject cases, source wiring of both gates).
- [x] **P2.46 ✅(2026-07-11 T-088: a central `check_cooldown`/`update_cooldown` in `send_cornix_signal`, the check before the outbox insert, a cooldown upsert atomic in the same commit as the insert (`commit=False`+`conn.commit`); tag `BTCSMC_1H` (9, varchar(10)-safe), 12h ≥ 1h candle duration (P1.27). Wiring tested DB-free in `backtest/test_btc_smc_cooldown.py`, tag length in `test_cooldown_tags.py`. P0.5 untouched) `21_btc_smc` has no cooldown/dedupe** (`21:121-123,264`) → on filler lag, a double signal 1h apart. `[DB]`
- [x] **P2.47 ✅(2026-07-11 T-2026-CU-9050-077: the DB pool gets a `statement_timeout` (default 300s — caps runaways/hangs, spares the hourly analytics on the index-less trade tables; tightening to 30s only after a Z0 query measurement as an operator decision — env-overridable, 0=disable for trainer/housekeeping) + TCP keepalives in `core/database.py`; the watchdog gets a generic log heartbeat `check_heartbeat` in `main_watchdog.py` — alive-but-silent >20min → WARNING, auto-restart off by default/money path, mapping-free via open handles, bots without a log exempt. Data staleness remains DB-side in `core/health_monitor`. No TZ flip. Tests: `test_db_pool_options.py`, `test_watchdog_hang.py`. Open: a fleet-wide per-bot heartbeat needs unified logging — a follow-up topic.) ✔✔(Step2: the ingestion WS was dead for 6h with a green watchdog, the fleet traded on stale data) A watchdog with no hang detection + no `statement_timeout`/keepalives** (`main_watchdog.py:294-304`, `core/database.py:42`) → a wedged bot stays "green". **Fix:** statement_timeout+keepalives, a heartbeat. `[DB]`
- [x] **P2.48 ✅(2026-07-11, T-2026-CU-9050-087: `kill_process` sends a `CTRL_BREAK_EVENT` to the bot's whole process group instead of `terminate()` — every bot/dashboard now starts with `CREATE_NEW_PROCESS_GROUP`, so the signal reaches the bot AND process-pool workers (closes the double-compute window) without hitting the watchdog console; a `GRACEFUL_STOP_TIMEOUT_S` grace period before a hard kill, a fallback to `terminate()` when CTRL_BREAK isn't deliverable (no console) with a log entry. A job object deliberately NOT added — kept minimal, the orphan window is covered by CTRL_BREAK-to-the-group + the P0.2 orphan sweep at the next start. The P0.2 mutex/orphan sweep + the T-074 restart path (`Stop-ScheduledTask`) not regressed; `test_watchdog_backoff/hang` green. Live verification of no orphaned workers = a VPS step) Windows `terminate()` = a hard kill** (`main_watchdog.py:169-181`) → no graceful shutdown; the engine's process-pool workers survive the parent kill → a double-compute window. **Fix:** CTRL_BREAK_EVENT/a job object.
- [x] **P2.49 ✅(2026-07-11, T-2026-CU-9050-087: `atomic_write_json` now uses a unique `tempfile.mkstemp` name in the target directory instead of a fixed `.tmp` (parallel writers no longer collide) + a retry (5×50ms) on `PermissionError` at `os.replace`; if it stays blocked → an ERROR log instead of silent loss + temp cleanup. The same directory keeps `os.replace` atomic (the `core/coins.py` #68 pattern). DB-free test `backtest/test_atomic_write_json.py`, 12) `atomic_write_json` fails on Windows with an open reader** (`core/state_utils.py:50`) → the update is silently discarded; a fixed `.tmp` name → corruption. **Fix:** a unique tmp, retry.
- [x] **P2.50 ✔done(Step2: extract+refresh against the live DB, fixtures/goldens committed in `4765e25`; 2026-07-09 T-028 verified: 24 goldens + 24 fixtures + a manifest git-tracked, `armed = bool(golden_files)` satisfied, `verify` runs as a pre-commit hook on every commit and reports "OK - 24 fixture(s) match") The regression guard currently protects nothing** (`tools/regression_guard/guard.py:128-137`, unarmed → pass; a golden delete → a silent disarm). **Fix:** extract+refresh (Step 2!); a hard fail if the manifest exists but goldens are missing. `[DB]` ⚠️ The guard **is armed** — `docs/OPUS-HANDOFF.md` and the task audit (B5) claimed the opposite until 2026-07-09. The second required fix (a hard fail on a manifest without goldens) is still missing → split off as **P2.51**.
- [x] **P2.51 ✅(2026-07-11, T-2026-CU-9050-076: `mode_verify` now checks `os.path.exists(MANIFEST_PATH)` in the empty-goldens branch — the manifest is the "was once armed" marker [`refresh` writes it next to the goldens], so a manifest-present-but-goldens-gone case → **exit 1** instead of a silent pass; a manifest-absent case remains the legitimate pre-live-DB-freeze pass. The reverse case [`:139-140`] untouched. Proof: `backtest/test_regression_guard_disarm.py` — case 1 [manifest without goldens → exit 1] demonstrably fails against the pre-fix state with an AssertionError "got 0", case 2 [never armed → pass] and case 3 [goldens without fixtures → exit 1] pin the neighbouring invariants; `guard.py smoke` remains green) Regression guard: deleting `golden/` silently disarms it** (`tools/regression_guard/guard.py:132-137`) — `mode_verify` returns "NOT ARMED … pass" and **exit 0** on missing goldens, without checking whether `manifest.json` exists. Whoever deletes the goldens (or loses them in a merge) unknowingly turns the guard off; the pre-commit hook stays green. The reverse case is handled correctly (goldens without fixtures → exit 1, `:139-140`). **Fix:** if `MANIFEST_PATH` exists but there are no goldens → exit 1 instead of pass. (Split off from P2.50, 2026-07-09 T-028.)

---

## P3 — Low / cosmetic / cleanup (collected)

- [x] **P3.1 ✅(2026-07-11, T-2026-CU-9050-096: `load_coins` ×6 consolidated onto `core.market_utils.load_coins` [chart_data_service, fib_backtest, walkforward_sim, qm_backtest, smc_ml_trainer, qm_ml_trainer] via new `usdt_only`/`uppercase` flags; dead `write_to_active_trades`/`write_to_telegram_outbox` in `3_detectors.py` removed [grep-verified callerless, `write_signal_atomic` is the single-transaction path]; `_apply_keepalive` ×3 byte-identical → `core/ws_utils.apply_keepalive` [`import sys` kept local due to mypy platform=win32]; the TIMEFRAMES redeclaration in `6_housekeeping` → `core.config`) Duplicates/dead code:** ~~delete `tools/db_schema_analysis.py` (stale, can't even run) (root is canonical)~~ ✅ 2026-07-10 (T-2026-CU-9050-039: deleted; the "byte-identical" claim from `docs/CANDLE_CALL_SITES.md` was wrong — root carries the ruff cleanup from `052ba4c`, the `tools/` copy could never import `core.database` due to its `sys.path.insert`); pull `load_coins` ×6 with semantic drift onto core; remove the dead signal writers in `3_detectors.py:53-106`; `_apply_keepalive` ×2, the TIMEFRAMES redeclaration in housekeeping.
- [x] **P3.2 ✅(2026-07-11, T-2026-CU-9050-096: `indicator_calculation.log` [2_indicator_engine] + `watchdog.log` [main_watchdog] switched from `FileHandler` to `RotatingFileHandler` [10MB×3] at the SAME path — not `setup_logging`, because its `logs/<name>.log` renaming would break the readers [the watchdog-hang check reads `indicator_calculation.log`, dashboard.py + health_monitor read `watchdog.log`]; `logs/dashboard.log` [an append-only Popen pipe] via a new `truncate_oversized_logs` in the 03:00 housekeeping, trimmed to the last half above 20MB) Unrotated logs:** `2_indicator_engine.py` (`indicator_calculation.log`), `main_watchdog.py` (`watchdog.log`), the `dashboard.log` pipe → `setup_logging()` resp. a truncate in housekeeping.
- [x] **P3.3 ✅(2026-07-11, T-2026-CU-9050-096: a central `re.fullmatch(r'[A-Z0-9]+', sym)` validation in `core.market_utils.load_coins` — non-conforming symbols are dropped + ERROR-logged, never silently kept; a no-op on the live coins.json [530 uppercase USDT perps, all match], so `1_data_ingestion` [T-092] sees an identical list. Closes all ~40 f-string table names centrally, since the 6 callers now go through here. DB-free test `backtest/test_symbol_validation.py` [8 cases]) SQL identifier hygiene:** ~40 f-string table names from coins.json — a central `re.fullmatch(r'[A-Z0-9]+', symbol)` validation in `load_coins` closes them all. (No active injection path, but a second-order risk in `28:312` from parsed message text.)
- [x] **P3.4 ✅(2026-07-11, T-2026-CU-9050-096: major pins in `requirements.txt` for pandas [>=3.0,<4], python-telegram-bot [>=22,<23], xgboost [>=3.0,<4] — the current state pinned, NO upgrade. `requirements.lock.txt` = the dependency closure of requirements.txt against the installed state [52 packages], NOT `pip freeze` [the global env carries ~230 unrelated Cu tooling/editable installs]. Header flags INCOMPLETE: yfinance+pandas_ta aren't installed on the DB-free build machine [T-011] → an authoritative full lock = a VPS step. A model-pkl `xgb.__version__` assert deliberately NOT done [a bigger change, outside the P3.4 scope]) requirements.txt is completely unpinned** → `pip freeze` as `requirements.lock.txt`, at least major pins (pandas/PTB/xgboost). Model pkls: `xgb.__version__` in the artefact + a load assert.
- [x] **P3.5 ✅(2026-07-11, T-2026-CU-9050-096: 19_whale_logger's price display `:.2f` → `format_price` [sub-cent coins otherwise showed "$0.00"; purely informational, no Cornix block]; open_handler.py: the blocking `get_live_price` [requests.get] lifted from the async handler into `asyncio.to_thread` + `@None` attribution fixed [`user.username or user.full_name`]; describe_project.py: an info leak in the module docstring + a runtime warning documented, the ignore set extended to .git/.local/__pycache__/node_modules) Sub-cent formatting/`@None` attribution/blocking I/O in async** (`19:207,212`; `open_handler.py:95-96,20`); `describe_project.py`'s GNU-`tree` assumption + full source dump (an info leak).
- [ ] **P3.6 ◐ Docs part ✅(2026-07-11, T-2026-CU-9050-096: "known backtest limitations" blocks commented in — smc_pattern_backtester [FEE_RATE declared but never referenced + survivorship + no capital/concurrency model], fib_backtest [no fees/no capital/survivorship], qm_backtest [fees+equity present, but no concurrency cap + survivorship], the bfill leak at the call site in smc_ml_trainer/qm_ml_trainer; NO logic change. The `[DB]` part remains open: are delisted tables still there?) Fees declared in backtests but never applied** (`smc_pattern_backtester.py:20`); survivorship bias (today's coins.json over 1-2 years); no capital/concurrency model (summed parallel positions); a `bfill()` leak in trainers; thresholds in the pkl but bots hardcode them. `[DB]` (delisted tables still there?)
- [x] **P3.7 ✅(2026-07-11, T-2026-CU-9050-096: the coin×TF loop handler in 24_quasimodo_bot + 25_smc_ml_sniper was `logger.debug` → invisible; aligned to the bot-29 pattern: `logger.error(..., exc_info=True)` + `conn.rollback()` [a poisoned transaction would otherwise starve every following coin]) Coin-level exceptions on DEBUG** (`24:280-281`, `25:306-307`) → invisible; 29 does it right (ERROR+exc_info+rollback). Align them.
- [x] **P3.8 ✅(2026-07-11, T-2026-CU-9050-096: `import matplotlib; matplotlib.use('Agg')` added before the pyplot import in 17_mayank, 24_quasimodo, 25_smc_ml_sniper — one line each, the bot-16 pattern) The matplotlib backend is only set to `Agg` in 16** (17/24/25 without it) → would crash headless. One line per bot.
- [x] **P3.9 ✅(2026-07-11, T-2026-CU-9050-080: a verified no-op — the premise "identical block" doesn't hold. Bots 24/25/29 each post exactly TWO outbox inserts: the canonical `cornix_msg` (parsable) and a second info message resp. image caption, which does **not** repeat the Cornix block (`<pre>/<b>` HTML, labels "Coin/Action/Entry/Take Profits/Stop Loss" resp. prose, no `📈 Signal for`/`Direction:`/`CMP Entry:`). Proof DB-free: all three info strings through the repo guard `28_signal_orchestrator.parse_cornix_signal` → each `None` (not parsable); even tag-stripped, all canonical markers are missing. Control: the canonical `cornix_msg` parses correctly. This means 24/25/29 are covered by HARD RULE 4 (exactly one Cornix-parsable message per signal, the 06.07. fleet double-trade fix) — the fix is present in all three + substantially correct. Consistent with the fixed fleet 7/9/10/11/12/13/15/18 and the reference 16 (embeds everything in one message). No code change, no PR.) Cornix double-parse risk:** 24/25/29 insert a plain Cornix block AND a second HTML message — the second, however, does **not** repeat the Cornix block and fails the `parse_cornix_signal` guard, `16` embeds everything in one message (safe). ✔ checked.
- [x] **P3.10 ✅(2026-07-11, T-2026-CU-9050-096: verified against the code [two audit claims corrected], then documented. REGIME_ORCHESTRATOR.md: (a) regime_current is set on the FIRST check/cold start, not after the second [the doc claim was wrong]; (b) per-cell ↑/↓ markers are NOT implemented [`_cell` only returns `{wr}%`/`---`, the legend is orphaned — a note in the docs + at the legend code 27]; (c) `fallback %` is NOT missing [the audit claim was wrong] — it lives in 26_regime_detector, but aggregates ALL fallback reasons, not isolated regime_unstable → docs refined. Scheduler comments in 18_abr1/12_ats/13_rub said minute 10/8/12 vs code 2/13/10 → comments corrected, the `now.minute` guards [money path] untouched. `ml_predictions_master.trade_id` = hardcoded 0 on the log_prediction path + every bot except 9_ai_sr_bot → documented on core/signal_post) Various spec-drift doc points** (`REGIME_ORCHESTRATOR.md`: the regime_current init, ↑/↓ markers never implemented, the fallback rate missing from the status post); scheduler comments vs. code in ABR1/ATS/RUB; `ml_predictions_master.trade_id` always 0 (a dead column).
- [x] **P3.11 ✅(2026-07-11, T-2026-CU-9050-096: checked against the code — housekeeping cleaned up `generated_charts` [7_pattern_detector] + `charts` [core/charting.py], but NOT `institutional_charts` [22_ip_pattern_bot] → unbounded growth. `cleanup_generated_charts("institutional_charts")` added to the 03:00 block [the same outbox-reference protection logic]) Chart directory growth** (`7:27-28`, `22:29-30`) — check whether housekeeping cleans up exactly these dirs.
- [ ] **P3.12 Price columns `REAL` (float4)** in `active_trades_master` + indicator tables → precision loss on sub-cent coins. `[DB]`
- [x] **P3.13 ✅(2026-07-11, T-2026-CU-9050-076: `backtest/test_cooldown_tags.py::test_mis_horizon_tags_fit` added — parses `MODEL_GENERATION` + the `MIS_CHANNELS` horizon keys from `11_ai_mis_bot.py` and reconstructs the derived tag `f"{generation}-{horizon}"` [a mirror of `:301`], which no string-literal test sees; `MIS2-168H`=10 keeps within varchar(10), a new generation [MIS10] or a 720H horizon would tear the assertion instead of silently vanishing into a swallowed ValueError. The static test is the load-bearing safeguard, since the `COOLDOWN_MODULE_MAX_LEN` guard raises `ValueError` and would be swallowed by broad excepts) The cooldown tag-length net only covers Mayank** (new 2026-07-09, T-028). The sweep for further `varchar(10)` breakers (the error class from T-2026-CU-9050-024) found **no second active instance**: all 18 `trade_cooldowns.module` writers were resolved down to the tag value, the longest is 9 characters (`MAYANK_4H`, `MIS2-168H`), all tags are distinct — even a 10-character truncation produced no cooldown collision between two bots. Two residual risks remain: (a) `MAYANK_4H`/`MIS2-168H` are **tight** at 9/10 characters — a `MAYANK_15M` (10) or a generation `MIS10-168H` (11) would tear the limit; (b) `backtest/test_cooldown_tags.py` statically checks only the Mayank tags, **not** the MIS horizons. **Fix:** add the MIS tag assertion to the test. — Additional finding: the `COOLDOWN_MODULE_MAX_LEN` guard **doesn't truncate, it raises `ValueError`** — and would be swallowed by the same broad `except Exception: logger.error` blocks that silently killed Volume Indicator. The load-bearing safeguard is therefore the DB-free static test, not the runtime guard.

---

## Z — Future / roadmap (not audit-driven)

- [ ] **Z0 — VPS CPU permanently at 100% ⚠ MOST IMPORTANT POINT.** (Observed again on 2026-07-04, right after the immediate-measures restart.) A systematic load analysis instead of individual patches: measure the top consumers per process (Task Manager/`Get-Process` sampling over 10 min) and hold them against the known audit candidates — full-table scans with no indexes on `closed_trades_master`/`closed_ai_signals` (~33 billion tuples/day, report 18), 9,782 plain tables + WAL 8.5 GB/day instead of hypertables (report 18), the indicator engine's Python-lambda WMA/KAMA + process-pool respawn (P2.19), 538 serial HTTP calls/detector cycle (P2.44), `pump_dump_events` ~108 stmt/s (P1.40), the dashboard's 25 psutil sweeps/6s (P1.38). Measure first, then fix the 2-3 biggest items — target: baseline load <50%, so the fleet survives restarts/backtests without starvation. 🔧(2026-07-19, T-2026-CU-9050-174: the indicator-engine item addressed — an early skip in `process_coin_task` when no new CLOSED candle lies above the watermark [~2/3 of the cycle's work drops out], ONE persistent DB connection per pool worker instead of ~3,160 connects/cycle + a definitions/table_exists cache, MACD-EMA reuse/`np.fmax` true-range/`chunksize=8`; the DB end state proven byte-identical [guard 24/24 with no refresh + a float64 bit-pattern comparison old↔new across all 24 fixtures]; active after an engine restart, Michi-gated.) 📏(2026-08-02, T-2026-KYT-9050-009: **first measurement with a tool** — `tools/ops/measure_cpu_baseline.ps1`, 10 min / 35 samples / 10 cores: box average **78%**, not 100% [the 100 figure came from 3-second snapshots]. Breakdown: fleet-python 18.5% [41 PIDs under the watchdog] + 10.6% [pool workers], postgres 14.1% [120 distinct PIDs in 10 min = connection churn], Symantec 5.5%, system 2.7% — **and ~34 percentage points is the measuring agent session itself** [claude alone 16.4%, the sampler via WmiPrvSE 4.4%]. Subtracting the observer, the baseline load sits at ≈48–50%, i.e. at the Z0 target; but that's a subtraction, not a reduction. **The next step isn't optimisation, but a clean measurement without a session** [a planned task for a quiet hour]. Per-bot attribution isn't possible unelevated: `Win32_Process.CommandLine` is `$null` for the elevated fleet, the watchdog only logs the dashboard PID.)
- [ ] **Z1 — Web UI overhaul (dashboard rewrite).** Modernise `dashboard.py` fundamentally instead of just patching it. Handle the open audit points along with it: auth (P0.8), process-list drift → `core/fleet.py` as the single source (R2/P1.38), CSRF, log streaming, `/api/status` performance. Clarify scope: framework (keep Flask vs. FastAPI+HTMX/React), real-time updates (SSE/WebSocket instead of 6s polling), mobile-friendliness.
- [x] ~~**Z2 — Rollout via a Cloudflare tunnel.**~~ **STRUCK 2026-08-02 (T-2026-KYT-9050-074, operator decision): „Dashboard sehe ich ohnehin nur via RDP.“** This makes Z2 moot and answers the open dashboard-exposure question from T-009/T-056. The loopback default from PR #237 fits unchanged — **NO `.env` entries are needed** (neither `KYTHERA_DASHBOARD_HOST` nor `KYTHERA_DASHBOARD_TOKEN`); from the next dashboard start, the UI is only reachable from an RDP session, i.e. exactly the desired state. `cloudflared` is **not** installed: remote access isn't needed, and compared to "not reachable at all", a tunnel enlarges the attack surface (an access policy as an extra failure path, TLS termination at the provider, one more service). **Reopen exactly on one trigger: the Z1 quick actions (F4)** — an auth layer remains a precondition for live leverage in the web UI. If F4 doesn't happen, Z2 doesn't happen. Not to be confused with T-2026-KYT-9050-070 (the Symantec rules, its own wontfix): the loopback bind takes port 5000 out of the network independent of the firewall. A note for execution: `dashboard.py` does **not** sit in `core/fleet.py` (its own scheduled task) — the marker-based fleet restart doesn't catch it, the hardening only takes effect at the next start of this process. Original text kept for traceability:
  ~~**Z2 (original).**~~ Possible under Windows: `cloudflared` exists as a Windows binary and runs as a Windows service (`cloudflared service install`). An outbound-only connection → no port forwarding, port 5000 can stay entirely bound to `127.0.0.1` (incidentally resolving the P0.8 exposure). Access protection via Cloudflare Access (a zero-trust login in front of the dashboard, the free plan covers 1 user). Prerequisite: your own domain on Cloudflare. Order: Z2 can go live BEFORE Z1 — the tunnel + Access protect the old dashboard immediately too. ⚠️(2026-08-02, T-2026-KYT-9050-009, **current state measured — the blocker isn't the domain**: `cloudflared` isn't installed on SRV02 at all [no binary, no service], `dashboard.py` still binds `0.0.0.0:5000` unchanged, and the port **is** reachable from the internet right now — `netstat` showed an ESTABLISHED connection from a foreign public IP at measurement time, `logs/dashboard.log` proves ongoing scans incl. `GET / → 200` from strangers and exploit paths like `/v404/exec?jwt=…`. `grep -i auth dashboard.py` → **no hit**, i.e. `POST /api/system/stop_all` is exposed unauthenticated [P0.8]. In the reviewed log history, no foreign hit on a control endpoint — luck, not a measure. Delivered with no behaviour change: the bind address is now controllable via `DASHBOARD_BIND_HOST`, **the default stays `0.0.0.0`** [a silent switch would have cut remote access at the next restart], plus a startup warning and a `.env.example` entry. The flip is a Michi decision: `DASHBOARD_BIND_HOST=127.0.0.1` closes the surface immediately, but costs remote access until the tunnel is in place. Details + option matrix: `docs/T-2026-KYT-9050-009-vps-audit-rest.md` §2/§7.) 🔧(2026-08-02, T-2026-KYT-9050-056: the code half is built and effective on its own — a loopback bind, a host allowlist, a CSRF guard, a fail-closed start policy that also covers the tunnel case [an off-box hostname without a token = no start]. **Correction to the rationale:** "the tunnel + Access protect the old dashboard immediately" no longer describes the bottleneck — it's secured from the next dashboard start onward even WITHOUT a tunnel. Z2 remains a precondition for the Z1 quick actions (F4) and for remote access, but net-increases the attack surface. `cloudflared`/Access itself = a live intervention, a Michi decision with a quantified residual risk in `docs/DASHBOARD_SECURITY.md` §4. **Merge note:** the T-009 lever `DASHBOARD_BIND_HOST` is thereby superseded — the same lever is now called `KYTHERA_DASHBOARD_HOST`, sits in the namespace of the token and allowlist, and is hardened by default; `.env.example` updated to match. **And the exposure cause has since been named** — not the hoster, but two Symantec firewall rules, see the correction at P0.8.)

---

## Step 2 — live DB validation catalogue (VPS + DB access)

These queries confirm/measure the `[DB]` points and pull the **strategy results** into context, to check every strategy thoroughly.

**Foundation (decides the severity of many findings):**
1. `SHOW timezone;` + `SELECT NOW(), NOW() AT TIME ZONE 'UTC';` + the VPS OS TZ → all TZ findings (R3, P2.1-2.6).
2. `\d trade_cooldowns` · `\d active_trades_master` · `\d closed_ai_signals` · `\d telegram_outbox` · `\d ai_signals` · `\d ml_predictions_master` → column types (TEXT vs INT for `current_target_hit` → P1.5; `image_path` present → R2/P2; `posted` timestamp vs bool), constraints/indexes, unique backstops.
3. `SHOW max_connections;` + `SELECT count(*), application_name FROM pg_stat_activity GROUP BY 2;` → P1.34; idle-in-transaction/stale sessions → P1.33/P2.47.
4. `SELECT MAX(open_time) FROM "BTCUSDT_1h_indicators";` at the :35 wall clock + `"BTCUSDT_1d"` intraday → proves R1/the forming candle live.

**Double-trade/duplicate proofs:**
5. An identical `message` in `telegram_outbox` twice within minutes (`attempts>0 AND sent=TRUE` = a smoking gun) → P0.1.
6. `suppressed_signals` with `original_outbox_id` → `channel_id=CH_REGIME_TRADING` → P0.3 self-echo.
7. `orchestrator_open_trades`: `coin+direction` duplicates with a time overlap → P2.26; OPEN >30d → lifecycle holes.
8. Watchdog logs: two orchestrator/bot processes at once → P0.2.

**Regime gating effectiveness (a core feature):**
9. `SELECT DISTINCT bot_name FROM bot_regime_whitelist` vs `orchestrator_open_trades` vs `suppressed_signals(reason,bot_name)` + `MIN/MAX(computed_at)` → proves the P0.4 mismatch + P2.25 stale keys.
10. The `wl_reason` distribution over 30 days: % of forwards via a real 4D decision vs `no_whitelist_entry`/fallback → P0.4/cross-cutting.
11. `regime_history` 2h window `COUNT(DISTINCT regime)` over 30 days → how often is `reliable=False`? + the TRANSITION share → P2.23.

**Strategy scrutiny (the actual purpose of Step 2):**
12. **Calibration per model tag** (SRA1, MIS1-8/24/72/168H, ATS1, RUB1, QM, TD/BB, ABR1 LONG/SHORT, ROM1): `ml_predictions_master.confidence` (shadow+posted) bucketed against the realised outcome from `closed_ai_signals`. Uncorrelated ⇒ the forming-candle/skew findings empirically confirmed (P1.17-25, P0.10-13). **ABR1 LONG vs SHORT separately = the decisive SUCCESS_CLASS_IDX test (P2.38).**
13. **ROM1 WR vs overall bot WR** — the orchestrator's actual success KPI; + the distribution of `|sl-entry1|/entry1` over ROM1 rows → P2.27.
14. **Per-strategy realised WR/PnL** from `closed_trades_master`/`closed_ai_signals`, **fee-adjusted** (P3.6), against the published backtest numbers (UFI1 54.2%/+278R → P0.11; SMC thresholds → P0.10). `targets_hit` distribution: how often a close goes beyond TP3/TP5 (P2.31).
15. **Data quality:** a gap census per TF (a generate_series anti-join) → R1/P2.13; `ma_200=0` rows on young coins → P1.13; `COUNT(DISTINCT poc)` over old rows → P1.12; a boundary overwrite: ~100 recent closed candles DB vs Binance REST → P1.11.
16. **Corrupt/risky trades:** `active_trades_master WHERE (direction='LONG' AND target1<=entry) OR target1=0` → P0.7; `sl<=0 OR NULL` → P2.9; DELISTED closes on non-USDT → P2.17.
17. **Gap-filler proof:** the housekeeping logs never contain "candles filled" → P0.9.
18. **Vocabulary reconciliation:** `DISTINCT model_name`/`strategy` vs the master model dummies (expected: near-0 overlap) → P0.13.
19. **Whale coverage:** distinct symbols in `whale_data/*.json` vs coins.json → P1.42.
20. **Missing tables:** how many coins.json symbols have no `_1h/_4h(_indicators)` → P1.31 (≥8 = silent trainer truncation); do the METALS tables (`XAUUSDT` etc.) even exist? → P2.45.

~~**Arm the regression guard** (Step 2): `guard.py extract` + `refresh` against the live DB, commit fixtures/golden → P2.50.~~ **Done** (commit `4765e25`: 24 goldens + 24 fixtures + a manifest committed; `verify` runs as a pre-commit hook). Remaining: P2.51.

---

### Raw reports (details per subsystem)
Full individual findings with evidence/snippets live under
`audit_reports/01_core_infra.md … 12_cross_cutting.md`
(01 core, 02 data pipeline, 03 Telegram/monitors, 04 orchestrator/regime, 05 classic strats, 06 AI 9/11/12/13, 07 AI 14/15/18, 08 SMC bots, 09 market intelligence, 10 dashboard/tools, 11 ML trainers/backtests, 12 cross-cutting).
Plus: 13 the ML trainer audit (`Documents\_X`), 14 live performance from the DB, 15 strategy proposals, 16 strategy/model concept evaluation (grades A–F + portfolio recommendations), 16b regime orchestrator overall analysis, 17 monitor replay & gap check, 18 DB architecture/performance/calculation consistency (TimescaleDB, indexes, ATR/RSI/win variants), STEP2_DB_VERIFICATION (live evidence).

- [x] **#T66-1 The ROM1 net gate ALREADY EXISTS — nothing to build
  (T-2026-KYT-9050-066, 2026-08-01).** Checked before building: `_v2_whitelist_decision`
  in `27_bot_regime_analyzer.py:677` already computes exactly the requested gate — a lower bound of the
  **net expected value** with hierarchical EB shrinkage (bot×regime×alt →
  bot×regime → bot×ALL), written into the shadow columns `whitelisted_v2`/`reason_v2`,
  NOT read by the live gate (T-2026-CU-9050-048). The code explicitly notes:
  "The flip to v2 is Michi's call after the counterfactual comparison — never here."
  **Measured divergence (1 590 cells):** v1 opens **94%**, v2 only **6%**, they
  contradict each other in **88%** of cells. That's not fine-tuning, it's a
  different regime — ROM1s pass rate (28% today) would drop drastically.
- [ ] **#T66-2 Counterfactual run open — blocked by the CPU guard.**
  `tools/rom1_counterfactual.py --side both` is the tool intended for the v1→v2
  flip (scores BOTH gate sides, bucketed by suppression reason and gate path).
  It self-aborts: "system CPU at 100% — don't further load the fleet". The cause
  was `tools/backfill_funding_rates.py` (PID 85492, since 19:35, 959 CPU-s) — **a foreign
  job, not aborted.** Run this once the VPS has headroom; only then is the
  v2 flip decidable at all.
  ✅(2026-08-02, T-2026-KYT-9050-007: **decided — NO-GO, and without this run.**
  Instead of the counterfactual, the gate was re-decided over 45 parametrisations (z × k × break-even)
  and scored against the realised ROM1 legs
  (`tools/whitelist_v2_recalibration.py`, verdict
  `docs/T-2026-KYT-9050-007-whitelist-v2-recalibration.md`). Three findings: (a) **the lever
  named in the order is the wrong one** — break-even moves the opening rate by 1.7 pp,
  shrinkage by 1.3 pp, the z multiplier by 10–47 pp; (b) **even at the most permissive end,
  v2 opens only ~53% of cells against v1's 94%** → the −55% throughput from PR #239 is
  structural, not a tuning artefact; (c) **out-of-sample, 42 of 45
  configurations remove WINNERS** — blocked legs average +0.55 to +0.60%/trade, that's
  ~80% of the window's realised ROM1 profit. The best in-sample region
  (`z 0,67 / be 0,1`, avg blocked −0.076) reverses out-of-sample (avg blocked +0.594):
  a pure selection effect, because the cells are chosen by their own most recent
  results. **The "zero out-of-sample" gap from PR #239 is thereby closed** —
  the new tool needs no `wl_reason` and can evaluate the 4,359 forwards from April to
  early July. **Stop-B is satisfied: v1 stays, v2 is neither flipped nor
  further tuned.** A permanent limit: `bot_regime_performance` is a snapshot with no
  history (0 cells with >1 row, measured per run) — the leakage test above is the best
  approximation achievable, not the right test; historisation is logged as
  **T-2026-KYT-9050-072**. Box `#T66-2` stays open: the counterfactual run
  itself was not made up, it's just no longer needed for the decision.)
- [ ] **#T65-1 ROM1 gates on HIT RATE instead of return — measures wrong for 32% of legs
  (T-2026-KYT-9050-065, 2026-08-01).** `27_bot_regime_analyzer.py:789` decides
  `wr_bot >= wr_overall`; hit rate ignores the win/loss magnitude ratios.
  Measured over 57 legs (window 02.07.–01.08., fees + funding realistic):
  **18 contradictions.** Let through and losing: ATS2 LONG (WR 45.2%, −0.903/trade —
  also the biggest roster loser at −23 162), ROM1 SHORT itself (45.9%, −0.174),
  EPD3 LONG (43.0%, −0.392), SRA1 LONG, FIF1 both directions. Blocked and profitable:
  MIS2-168H LONG (WR 32.8%, +0.002). The fix would be a gate on the net expected value after
  fees instead of WR — money-relevant, an operator decision.
- [ ] **#T65-2 ROM1 structurally cannot see shadow legs.** It scans `telegram_outbox`
  (`28_signal_orchestrator.py:731`); a shadow leg by construction writes NO
  outbox row, only `ai_signals`. The assumption "ROM1 can use shadow bots when the
  regime fits" (operator) does not hold for today's code — that would be a rework
  of the source, not a switch.
- [ ] **#T65-3 ROM1: 1 059 unidentified signals in 7 days (~9%).**
  `orchestrator_suppressed_signals.reason='bot_unidentified'`, `bot_name=NULL`.
  `identify_bot` uses regex on the message text plus a channel fallback; changed
  formats or channels fall through. Overall pass rate 28% (3 393 of 11 906).
  The whitelist itself is fresh (daily, a 30-day window) — the data is current, the
  metric is the problem (#T65-1).

- [x] **#T69-1 The loser-leg strike list was built on the WRONG metric —
  not executed (T-2026-KYT-9050-069, 2026-08-02).** Operator-Auftrag „Verlust-Beine rausnehmen“;
  checked and rejected before execution. `tools/roster_proposal.py`
  ranks via `leg_net_economics`, which measures a leg under **its own**
  TP/SL exit. But the trailing arm only takes over the **signal** and lays its
  own trail on top. The right metric for the roster question is
  `tools/short_leg_trail_value.py`.
  **Of the six DROP candidates, the trail turns FOUR positive:**
  MIS2-168h SHORT −1.490 → **+9.074** (the best leg in the whole field), BR4H LONG
  −0.445 → +0.512, BB_4H LONG −0.392 → +0.460, SRA2 LONG −0.036 → +0.236.
  BR1H LONG sits at residual −0.009 (top-5 share 57.9 → noise, n=98).
  Execution would have removed four legs the arm profits on. A warning now sits
  in the docstring of `roster_proposal.py`.
- [ ] **#T69-2 ATS2 LONG — a direction decision, not a quality question (Michi).** The only
  leg that also loses absolutely under the trail: **−0.342/trade over 1 252 trades**.
  But it beats the market by **+1.987** (t = 9.31), because the index gave longs −2.329
  in this window. This means the decision doesn't hinge on the leg, but on the
  target metric: wanting to be **absolutely profitable** (then out — and then consistently
  review the LONG side as a whole) or wanting to **beat the market** (then it stays in).
