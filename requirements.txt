# Pinning policy (P3.4, T-2026-CU-9050-096): the three libraries whose major
# versions carry breaking API/model-format changes get a major pin so an
# accidental `pip install -U` cannot silently bump the fleet across a major
# boundary (pandas 3 copy-on-write, PTB async API, xgboost model format). These
# are the current-state versions — NOT an upgrade. The full transitive lock
# lives in requirements.lock.txt.

# --- Core & Database ---
requests
pandas>=3.0,<4.0
numpy
psycopg2-binary
websockets
websocket-client
pytz
python-dateutil
aiohttp
python-dotenv

# --- Math & Indicators (Ebene 2) ---
scikit-learn
scipy
xgboost>=3.0,<4.0
yfinance
pandas_ta
joblib

# --- Charting & Telegram (Ebene 3 & 4) ---
mplfinance
matplotlib
python-telegram-bot>=22,<23

# --- Dashboard (Flask Web-UI + Prozess-Monitoring) ---
flask
psutil

# --- Analytics substrate (Z1, T-2026-CU-9050-131) ---
# DuckDB is the columnar analytics store + native Parquet reader/writer for
# tools/analytics_export.py — no pyarrow needed. Runs on the VPS export job and
# behind the read-only success-rate endpoint; never touches the live PG path.
duckdb>=1.0,<2.0
